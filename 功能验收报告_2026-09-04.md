# 功能验收报告（2026-09-04）

## 范围与证据规则

本次覆盖HumanEvalJava加速、知识智能体对话与后台补学、R1/R2/R3路由、共享TSG多智能体协作、Java生成与工具执行、监控和运行说明。未编辑论文、论文图片或历史实验日志，未把本机模拟模型当成另一台电脑的真实模型。

## 核对结果

| 项目 | 结论 | 证据 |
|---|---|---|
| HumanEvalJava自动识别、隔离工作区、两槽并行 | 已修复自动模式未冻结知识的问题；项目目录和直接单类入口不误入隔离路径 | test_audit_requirements.py、test_execution_acceleration.py |
| 每轮知识冻结与合并 | 新经验、已有经验验证增量、中断补合并与幂等重放均有验证 | test_round_merge_keeps_existing_concept_updates_and_is_idempotent、test_interrupted_round_journal_is_recovered |
| 模型+知识联合回答 | 提示包含命中知识与最近对话历史；长系统目录不截断知识 | test_long_catalog_does_not_cut_off_knowledge_or_conversation_history；HTTP模型适配集成测试 |
| 模型不可用时自主回答与待学习 | 已知知识可回答；陌生概念进入队列；落盘失败不标为已学习；相同问题在不同会话不丢订阅 | test_monitoring_dashboard.py、test_audit_requirements.py |
| 后台补学 | 持久队列、退避、恢复调用、知识写回与原对话补发已接入；仅在监控服务运行时执行 | test_pending_learning_is_persisted_and_completed_after_model_recovers |
| R1/R2/R3 | R2只恢复D/E，R3只优化C，不进入R4；保留最佳测试与失败回退 | 12项逐等级路由检查、test_three_round_policy_recovers_de_in_r2_then_optimizes_c_in_r3、test_r3_failed_optimization_preserves_best_c_artifact |
| 测试状态引导协作 | 分析建图、状态自修复、知识检索、生成执行、验证、评估规划、知识更新、条件路由均有真实代码 | test_structured_fusion.py、test_stateflow_system.py |
| 学习证据口径 | 未执行的Java候选不写成负面经验；筛查报告不提升掌握次数，正式重复证据不重复计数 | test_java_pending_generation_is_not_written_as_negative_knowledge、test_screening_and_duplicate_evidence_do_not_promote_knowledge |
| Java参数与超时 | 修正ArrayList、泛型字符串集合、数组与float等参数；增加工具心跳与进程树超时清理 | 参数化类型测试、test_java_timeout_terminates_spawned_child |
| 续跑与前端 | 新摘要附源码哈希；源码变化时不复用新摘要；对话轮询不会覆盖已切换会话 | test_modified_source_cannot_reuse_new_summary、前端测试与构建 |

## 不能宣称已完整实现或已验证的事项

1. 本地HumanEvalJava为163份源码，参考规模164。新增dataset_integrity如实报告缺口，不能把扫描数视为官方完整数。本次未凭空补写数据集。
2. ValiFixTest只有旧界面入口，没有独立执行器，现已禁用并标注未接入。这不是已完成的外部基线。
3. SQLite检查点有持久记录，但当前入口重新提交初始工作流状态；实际续跑以任务结果、轮次进度及最佳产物恢复为主，不是任意节点无损恢复。
4. R3可能根据正式PIT反馈继续优化，因此不是机械限制每个正式轮次只能调用一次PIT。强制减少会改变既定优化决策，本次不这么做。
5. 知识自主回答依靠可用事实与规则检索；无足够证据必须待学习。模型答复写入“正在学习”，并不意味着内容已被证明正确。
6. 未在另一台正式电脑运行真实模型全量数据集，不能据本机接口模拟、少量类或回归通过宣称全数据集成功率、全量加速比例或所有任意输入均正确。
7. 历史摘要若没有源码哈希仍按旧兼容策略续跑；需要严格复验历史证据时使用现有强制重跑选项。源码哈希核对适用于本次起生成的新摘要。
8. time_budget_seconds仍是工作流边界处检查的软预算，不是覆盖全部模型排队、网络和子进程的统一硬截止。Java单次工具超时已能清理进程树，但不能因此宣称整个单类任务必定在同一个总秒数内结束。
9. 生成的测试会在本机执行，现有代码不是针对不可信恶意代码的安全沙箱。正式使用应限定可信源码/数据集。

## 本次验证产物

- 定向测试XML：.test_tmp/audit_20260904/targeted.xml。
- 完整测试XML：.test_tmp/audit_20260904/full.xml（最终计数以本报告末尾记录为准）。
- 新测试集中在tests/test_audit_requirements.py；旧协作、轮次、Java与监控测试继续保留。
- 前端6项测试通过，TypeScript及Vite正式构建通过。
- 新增真实HTTP对话链路验收1项通过：离线未知问题→持久待学习→模拟模型恢复→长期知识落盘→原对话补发→再次离线仍能使用已学知识回答。证据：.test_tmp/audit_20260904/conversation_http.xml。模型服务为隔离的协议模拟器，不是正式实验电脑的大模型。

## 只读数据发现复核

| 数据集 | 发现源码 | 可测试任务 | 过滤文件 |
|---|---:|---:|---:|
| TestEval | 210 | 210 | 0 |
| HumanEval | 164 | 164 | 0 |
| HumanEvalJava | 163 | 163 | 0 |
| Defects4J_cli | 34 | 34 | 0 |
| Defects4J_csv | 19 | 16 | 3 |
| Defects4J_lang3 | 56 | 55 | 1 |
| Defects4J_gson | 80 | 69 | 11 |

可测试任务合计711。文件发现数、过滤后任务数和正式参考任务数是三个不同口径；不可把Java项目中接口/说明类等过滤文件误认作漏跑任务。

## 日志同步方式

更新当前README、另一台电脑运行说明、修改日志与记忆中的生效规则；保留日期较早的历史条目，明确以本次条目为准。旧runs、知识记录、论文实验数据和原始模型错误不追溯改写。Java新运行日志增加start/waiting/done/timeout阶段信息，测试报告单独落盘。

## 真实Java复跑结果

运行目录：.test_tmp/audit_20260904/java_smoke。参数为language=auto、limit=2、workers=2、max_iterations=0、retry_failed_rounds=0，关闭LLM，采用独立测试知识文件，未改变原实验知识。

- 两个Maven任务实际并发启动，工作区分别属于ADD与ADD_ELEMENTS；java_parallel=true，knowledge_visibility=frozen_per_formal_round。
- ADD：JUnit通过，3个测试，LC=0.50、BC=0、AE=0.90、PIT=1.00，最终为C，存在full正式证据和源码SHA256。
- ADD_ELEMENTS：已跨过此前的集合类型编译错误；本次无模型无修复的保守候选使用空列表和索引上限1，导致IndexOutOfBoundsException，最终D，未伪造覆盖和变异结果。
- 完整性报告为reference_expected_tasks=164、discovered_testable_tasks=163、complete=false。
- 该运行验证调用链、真实失败归因与隔离，不代表完整163类的生成成功率，也不作为本次性能对照。

## 最终验收记录

- 2026-09-04完整回归：173 passed，0 failures、0 errors、0 skipped；终端耗时684.84秒，XML累计测试套件耗时684.003秒。
- 完整回归启动后新增的HTTP对话生命周期验收：1 passed，独立XML留存；不是将协议模拟器当成真实大模型。
- 定向回归：117 passed；前端6 passed；TypeScript/Vite生产构建通过；全部运行模块compileall通过。
- 最慢用例为test_system_iterates_by_quality_thresholds_with_llm（171.26秒），其余长用例集中在真实Python生成/执行/变异与工作流循环。不能把整套测试的11分钟耗时误归因于HumanEvalJava锁。
- 监控服务已用当前代码启动，/api/health返回ok=true、frontend_ready=true；/api/catalog返回测试状态、测试生成、测试知识三个正式智能体，Java策略为HumanEvalJava默认2槽、其他Java全局串行。
- 新服务输出/错误日志分别为.monitoring/server_20260904_130201.out.log和.monitoring/server_20260904_130201.err.log；验收时错误日志为空。
- 结论：本报告覆盖的核心设计与修复已有回归及集成证据，但“所有任意输入均正确、所有候选都通过、所有外部基线都接入”不成立。必须保留上文列出的数据完整性、独立基线、节点恢复、软预算和正式电脑模型验证边界。
