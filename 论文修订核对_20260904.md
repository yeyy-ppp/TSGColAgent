# 论文修订核对记录（2026-09-04）

## 当前状态

本次以9月1日12:44版为基础，遵循四份新提示词的最小必要修改原则。已保存《论文备份_20260904_修改前.md》；主论文暂未覆盖，修订内容存于《论文修订稿_20260904_待确认.md》。该稿已安排五张方法图的位置，暂引用原图，裁剪与图内标题/解释文字清理尚待确认采用本地无损处理方式。

正文中不插入编辑标签。本记录集中列出代码与原论文、新图片之间的技术冲突；未擅自更换公式定义、权重、算法条件或实验数据。旧图的技术说明中无法由当前代码证实的内容保存在本记录末尾，供确认。

## 已完成的内容检查

- 18个公式转换为行间数学环境，编号、参数和上下标中的数值顺序与修改前逐式核对一致。
- 3个算法改为带Input、Output和英文结构化操作的伪代码，保留原步骤与条件。算法1中轮内执行策略仍作为RunRound的策略描述；未将策略说明额外执行一次。
- 8张实验表翻译表内文字，百分数单位移入表头，百分点改为pp；逐单元格核对数字、符号与缺失值，全部一致，未重算数据。
- 保留第4节各子节及原有5张系统界面截图，编号调整为图6—图10。
- 保留程序节点、证据节点和关系类型枚举；清理“与代码一致”等核对语言，不据此删除定义。
- 移出主要论证的结果文件名称对应：总实验摘要为runs/all_datasets_summary.json，数据集汇总为各batch_summary.json，等级历史为grade_history，逐轮恢复统计为retry_summary.by_round。原复现证据文件保持不动。
- 主实验摘要SHA256为2F62114D48D4C619D6BB1C310FAA8B21E9D47B487F1815F8231224FC3B04C720，与既有实验证据文件一致。
- 修改前论文SHA256为1F8D1CD826BE89CA52DC6EB653ECA32BBD238CCB6189C850AA3FC12BBD10249E，大小97469字节。

## 代码核对与实际测试

以下均来自本地实现，未用新实验替换历史成绩：

| 核对内容 | 代码依据 | 结果 |
|---|---|---|
| 三个顶层智能体及职责边界 | agents/test_state_agent.py:60；agents/test_generation_capability_agent.py:12；agents/test_knowledge_agent.py:28 | 状态感知/评估/规划、生成/验证/修复/优化、知识检索/记录的分工有实现依据 |
| TSQ权重及缺失MS归一化 | metrics/state_quality.py:15 | 0.30、0.20、0.15、0.15及可适用MS的0.20保持不变 |
| A-E可行性与质量分级 | main.py:33、main.py:51、main.py:64、main.py:76 | 可行性门、可靠变异体下限3及质量阈值有实现依据 |
| 状态缺口、传播与调度 | graph/graph_query.py:181；graph/graph_defect_propagation.py:34；graph/adaptive_scheduler.py:135 | 五维缺口、8步/0.85衰减及经验增强权重有实现依据；根因候选域另见待确认项 |
| 两类知识检索评分 | graph/experience_memory.py:723 | 普通经验0.35/0.35/0.20/0.10，对话概念0.55/0.15/0.30；90天衰减及0.20得分门有依据；过滤范围另见待确认项 |
| 轮内修复收益 | graph/experience_memory.py:792 | 0.40/0.30/0.30、恢复0.10及回归0.25有依据；成本仅在正收益时扣减的说明保留 |
| 历史实验表 | 论文实验数据/实验结果证据_2026-08-31.md及原始总摘要 | 数据源未变，表内数值未重算 |
| 当前实现相关定向测试 | tests/test_stateflow_system.py；tests/test_execution_acceleration.py | 7 passed，42 deselected，2.85s |

实际执行的7项测试涉及：缺失MS时TSQ归一化、不可用变异证据分级、断言有效性、测试通过门、Python筛查8个变异体预算、完整20个变异体预算，以及筛查证据在正式结果前完成完整评估。测试仅验证相关实现，不代表完整数据集重跑。

## 需作者确认：符号与方法定义

1. **根因选择域。** 原式（12）在全部节点集合V_t上求最大值；graph/graph_defect_propagation.py:108先筛选程序节点，再取最大缺陷分数。程序集合与包含执行证据的全集不同。公式未改，需确认以哪一个域为论文定义。
2. **M_t重复使用。** 式（1）中M_t为元信息，3.3.1又用M_t表示最近执行关联的有效变异体集合。未更换任何一个定义。
3. **测试时间下标。** 式（2）采用T_{t+1}，3.3开头又以T_t描述生成测试。两处原有含义未自行统一。
4. **共享状态名称。** 原文将TSG同时用于方法简称及共享状态结构名称。新提示词禁止改变技术定义，因此此次未用另一个名称覆盖其含义；需确认术语定义后再统一。
5. **SFC统计域。** graph/graph_query.py:11的程序节点集合含Method和Constructor；metrics/coverage_metric.py:6的覆盖统计类型集合不含这两项。原文“参与质量计算的状态节点”与SFC具体统计域可能产生歧义，未通过改写公式掩盖差异。
6. **AE描述。** 原文以等值、异常、集合内容和状态变化说明断言多样性；metrics/assertion_metric.py将具体断言分类为exception、approximate、comparison和property，并以三种类别为饱和分母。原式（6）权重未变，其分量解释是否需要进一步精确定义，待确认。
7. **算法3过滤范围。** 原算法写明按语言、模型和任务层级执行兼容性筛选；graph/experience_memory.py:723的混合检索入口显式过滤模型，语言匹配参与结构得分，未见同等的语言/任务层级硬过滤。普通带任务签名的应避免记录也不同于被排除的应避免对话概念。原算法条件保留，仅改为英文伪代码，未擅自删除筛选步骤。
8. **轮内A/B停止条件。** 原算法1与算法2将达到A/B概括为停止；orchestration/test_workflow.py:630—678对正式完整评估、筛查状态及C级优化采用不同条件，筛查阶段重点判断A级。需确认原算法为抽象策略还是逐步骤实现；此次不改条件。

## 需作者确认：五张方法图

图片内技术文字、符号、箭头、布局和节点均未擅自改写，以下问题不能通过裁剪解决：

- **图1（用户第3张图）**：Q_t同时出现在测试状态智能体及执行状态处，G_t指测试生成智能体，P_t指输出；与正文Q_t为测试目标、G_t为缺口集合、P为程序、O_t为执行证据的定义不同。共享状态中出现Intention节点，现有正式节点枚举不含该类型，测试意图由元信息表示。
- **图2（用户第2张图）**：P_t用作状态覆盖向量，T用作历史状态，Q_t/G_t/K_t标记智能体，与正文已有变量含义不同；节点A—F是示意标号，尚无具体代码对象映射，不应据此新增节点类型。
- **图3（用户第4张图）**：出现Intention节点，以及P_t、A_p、Q_p、q_t的上标序列等未在原文统一定义的符号。截图中“选定任务目标（由q_t映射）”等流程标签属于技术内容，此次不更改。
- **图4（用户第5张图）**：T_c为候选测试符号，原文的主流程使用T_{t+1}。图将语法、执行、覆盖、变异和断言画为统一顺序；Python与Java的实际验证路径存在差异，正文保留其分别说明。图片内部“图2”属于误放图题，可清理并以外部图4编号呈现，其他符号待确认。
- **图5（用户第1张图）**：兼容性过滤的图示强于混合检索入口的实际硬过滤范围，见算法3待确认项。底部“正在学习→待复核→已掌握→应避免”的连续箭头可能被理解为必经生命周期；代码按证据设置或更新学习状态，非所有记录均必经四阶段。“状态缺口q_t”与正文q_t的根因状态含义也存在差异。

## 需作者确认：当前代码与历史实现范围

- batch_generate_tests.py:212对符合条件的HumanEvalJava独立类目录默认使用2个任务槽，并隔离工作区；其他Java项目串行。原文概括Java串行。是否将实现说明更新到该策略，需确认；历史711项实验表保持原值。
- orchestration/test_workflow.py:31在run入口构造初始状态并调用graph.invoke(initial)。SQLite检查点存在，但现有任务入口主要通过任务/轮次结果恢复。原文“从最近状态恢复”可能超过已验证的恢复粒度，未直接替换成另一项实现承诺。
- 既有系统测试记录为128项验证链，当前功能验收记录为173项完整回归加独立HTTP测试。修订稿保留历史测试数字，没有用较新的本机验收替换旧实验说明。
- HumanEvalJava本地选择任务为163，当前完整性检查的参考任务数为164；本次不补造第164项、不改711项历史总数。

## 旧图2说明的保留记录

以下是替换旧“断言—变异证据关系”图时移出正文的图专属说明。当前graph_defect_propagation.py直接按边类型和节点缺陷分数迭代，源码检索未发现Weak、Unobserved两种正式边类型及正负关系矩阵R⁺/R⁻的对应构造。故没有将这些内容迁入新图的解释，也没有据此修改代码。是否保留其中的理论抽象，需作者确认。

> 图2进一步展开共享TSG中与断言质量和变异检出直接相关的证据关系。阶段Ⅰ把程序状态、断言和变异体放在同一关系空间：Observe与Kill表示具体断言成功观察程序状态或测试杀死变异体，Weak、Survive与Unobserved表示弱断言、存活变异体或缺少观察的负证据。阶段Ⅱ分别构造正关系矩阵R⁺和负关系矩阵R⁻，负证据沿程序关系传播并形成节点缺陷分数，最大缺陷节点q_t成为当前联合增强的根因目标。阶段Ⅲ以q_t为共同目标构造边界或类型输入，补充具体断言，再通过变异测试检查语义变化是否被区分；只有AE、MS及其他适用指标不退化的候选才被接受。增强结果作为O_t回写共享TSG，使弱观察与存活关系转化为有效Observe和Kill关系，并驱动下一测试目标。

## 图像处理范围（待确认后执行）

只裁去截图外围灰色界面、图外说明栏、误放的图内总标题，以及清除图内正文式解释文字。模块名称、节点、变量、公式、数值、阶段编号、箭头及图例保持不变。正文式文字从图中移除时，其具有实际技术含义的信息保留在对应段落；如涉及上述技术冲突，只在本记录中列出，不自行改写。
