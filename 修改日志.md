# My_Mutil_Agent 修改日志

## 2026-09-17 前端方法名与报告展示修正

将用户可见方法名改为“ValiFixTest & TSGColAgent”，并把数据集级论文评估指标区接入实验总览。报告模板移除SFC和TSQ展示，state_flow_graph.html/SVG、source_coverage.html及batch_quality_report.html改为展示LC、BC、AE、MS、PassRate、AvgExec、Time和TIR；TIR同时显示改善数、可比较数及缺证数。内部旧指标计算和决策逻辑未改。前端构建及三项报告定向测试通过。

## 2026-09-13 论文指标独立采集与展示

新增独立指标计算和工具会话记录，覆盖缓存不重计、重试累计、轮次回退、历史缺证、TIR预登记与分类/排除统计；补充经验写入标识和上下文提供频次。接入任务/批量/总摘要、终端及前端，增加只读导出入口。AE和MS沿用运行算法，按用户明确要求修订论文公式及测量口径，保留全部已有表格数值。六份写作要求已执行于本次修改，实质方法/图文/实验冲突独立列入审查记录；本次未改变智能体策略或新增消融执行器。定向检查、无模型小型完整流程、前端构建及语法检查通过；原始实验结果和知识资产保持不变（验证使用隔离临时目录）。

## 2026-09-12 TSQColAgent展示与历史结果核对

统一方法显示名称，移除前端SFC/TSQ指标，以BC/MS替换对应图表并修正四轴雷达图；任务指标新增累计Time/s。ValiFixTest移除“未接入”文字，但保持禁用。保留内部智能体算法及原始实验产物。主论文仅统一方法名、增加历史数据补录边界说明，不修改已有表格数值。新增历史审计脚本与核对报告，记录模型标签冲突、PassRate差异及TIR/AvgExec缺证情况。验证：生产构建成功、3项定向测试通过、两种语言历史任务数据适配检查通过、论文表格逐行保护检查通过。没有重跑实验或实施全链路新指标采集。

## 2026-09-04 论文表达与格式修订草稿

保存修改前备份与独立修订草稿，主论文暂未覆盖。18个公式的数值/下标与8张表的逐单元格数字、缺失值保持一致；3个算法转为英文结构化伪代码，保留原有条件；系统章节和界面截图保留。五张用户新图已保存原图副本，等待无损图像处理方式确认。代码核对发现的技术冲突独立列于《论文修订核对_20260904.md》；定向测试7项通过，未改代码、知识库和历史实验成绩。

> 当前架构统一为“LangGraph运行底座 + 共享TSG与测试状态中心 + 测试状态/测试生成/测试知识三个顶层智能体”。旧的PlanningAgent、ExecutionAgent、EvaluationAgent、PTEG、SFQ和CSG内容仅用于记录历史演进，不代表当前设计。

## 2026-09-04 当前生效规则与验收边界

本次最终验收：完整回归173项、独立HTTP对话生命周期1项、前端6项均通过；生产构建与运行模块编译通过。原始结果保存于.test_tmp/audit_20260904/，完整细节见《功能验收报告_2026-09-04.md》。未修改论文和历史实验结果。

以下内容优先于本文中较早的历史条目；旧实验日志与原有成绩不追溯改写，论文由其他窗口维护，本次不修改论文及图片。

- HumanEvalJava：仅扁平独立Java类目录采用默认2槽、每任务独立工作区、每轮冻结知识视图；自动语言识别与显式java一致。有pom.xml/build.gradle的项目目录和直接单类入口不自动进入批量隔离路径。其他Java项目仍串行，Python仍按原规则执行。
- 同轮任务写入独立知识日志，轮末合并新记录及已有记录的验证增量；合并有幂等标识，任务中断后补合并尚未处理的日志，不把同轮其他任务的新知识引入当前生成。
- R1建立基线，R2只处理D/E，R3只处理C，不开启R4；保留最佳产物回退、原screening/full与A-E评价。不能声称PIT每轮绝对只调用一次：R3可能依据正式PIT反馈继续优化。
- 对话优先模型+知识，模型失败时知识自主检索回答，知识不足进入持久待学习队列。后台服务运行期间每15秒检查，失败后按1/2/4/8/16/30分钟退避；关闭服务期间不会自行运行，重启后读取队列。
- 知识自主回答是证据检索、规则判断和已知内容组合，不等同于另一个无需模型的通用推理模型。陌生概念不能因包含Java/测试等词而被误判为已知。
- 对话保留最近历史、已绑定任务和命中的知识；提示不会整体截断而丢掉检索结果。写入失败不得标为已学习。模型提供的新内容先进入正在学习，不直接当成已验证事实。
- 只有正式full测试证据参与掌握验证；同一证据不重复计数。未编译的Java候选不是负面经验。
- Java工具增加阶段心跳、超时进程树清理；暂时性超时不进入正式工具缓存。新增结果记录源码SHA256用于续跑核对。
- 本地HumanEvalJava仍为163份源码，参考规模164；数量缺口会标记完整性不足并阻止“完整数据集跳过”。本次不凭空补写基准源码，不改历史711项实验成绩。
- ValiFixTest独立执行器尚未接入，按钮已禁用并标注未接入。LangGraph保存节点检查点，但当前入口恢复主要为任务/轮次级，不能宣称任意节点无损恢复。
- 最新证据索引见《功能验收报告_2026-09-04.md》；外部电脑实际模型服务、全量基准成绩和加速比例不由本机模拟接口测试替代。

## 2026-09-02 HumanEvalJava低风险专用加速

- 仅对扁平、独立类组成的HumanEvalJava启用默认2任务并行，最大限制为4；TestEval/HumanEval并行规则、Defects4J及普通Java目录/项目的串行规则保持不变。
- 每个HumanEvalJava任务在自己的结果目录使用独立`.java_workspace`，源码、测试、`target`、JaCoCo和PIT报告互不共享；仅复用项目内Maven依赖缓存。目标源码未变化时保留Maven增量编译产物。
- Java全局锁改为HumanEvalJava每工作区独立锁；其他Java仍使用原全局锁，不改变项目级Java的隔离与执行原理。
- HumanEvalJava每个正式轮次冻结一份知识快照，任务经验写入独立日志，轮次屏障后统一去重合并，避免并行完成顺序影响同轮生成。
- JUnit、JaCoCo、PIT、screening/full、R1/R2/R3和A-E分级逻辑保持不变。两个真实类的串行/并行对照得到相同C/D构成及相同正式指标；本次短样本墙钟时间由31.09秒降至27.11秒。

## 2026-08-31 图形化方法图、完整实验表与公式编号收口

- 方法图1至图3再次重绘为抽象图形表达，删除大段说明文字。图1用语言图标、共享TSG核心、三智能体节点和R1/R2/R3分支表达总体方法；图2用静态图、五类证据、五边形缺口、能力符号和质量门表达状态引导；图3用三个智能体圆形节点、结构化箭头、LangGraph路由和知识生命周期表达责任边界。
- 所有方法图继续同时保留可编辑SVG和论文PNG，并完成白边裁切、PNG格式转换和逐图视觉检查。
- 直接读取711项现有任务的`batch_summary.json`、`grade_history`、`retry_summary`和`final_report`，建立可复核实验数据文件`论文实验数据/实验结果证据_2026-08-31.md`，附八份源摘要SHA256、计算口径和全部中间表。
- 实验章节改为“标题说明+数据表格+结果分析”：新增七数据集最终结果、Python/Java分组、R2恢复与R3优化、正式轮次机制消融、R3六指标增益和TSQ分量消融。
- 现有证据显示：R1有386项有效结果；R2恢复27项，使有效数增至413；R3在153项C级任务中改善92项，并使LC、BC、SFC、AE、MS和TSQ分别提高3.41、6.85、4.18、2.82、5.52和2.86个百分点。
- HumanEvalJava的163项结果明确标为修复前旧链路、待重新验证；当前没有同环境直接LLM、Pynguin或EvoSuite结果，论文不虚构外部基线优越性结论。
- 参考既有ValiFixTest期刊稿和状态图会议稿，将公式收敛为18个必要公式，统一采用“公式居中、编号右置、正文前后解释变量”的格式；四个算法改为“算法题名+输入/输出+编号步骤表”，实验表拆分为表1至表8并在表后逐项分析。

## 2026-08-31 论文全文与方法图代码一致性重构

- 以当前源码为唯一实现依据，重新核对论文摘要、引言贡献、问题定义、共享TSG结构、质量指标、状态缺口、调度逻辑、三个算法、智能体职责、实验指标、讨论与结论，不沿用旧版概念说明。
- 共享TSG形式化改为与`StateFlowGraph`、`PROGRAM_NODE_TYPES`和`EVIDENCE_NODE_TYPES`一致：删除代码中不存在的Coverage、Repair、Fixture、Mock、GenericInstantiation和CheckedException等独立节点声明；覆盖与修复分别归入节点属性、元信息和状态事件。
- 增加五维总体缺口的实际公式，分别对应覆盖、变异、断言、异常和运行时类型；节点级根因传播改为`GraphDefectPropagation`实际执行的8步、0.85衰减、边类型权重与累积截断公式，并明确总体缺口决定能力、节点缺陷决定具体目标。
- TSQ公式改为与`StateFlowQualityMetric`完全一致的条件归一化：SFC、AE、PV、NRR基础权重总和0.80，只有正式full评估、MS非空、有效变异体不少于3且可靠性为normal时才加入0.20 MS。PV和NRR改为代码中的单任务证据门，不再写成不存在的跨测试比例。
- A-E分级补充实际可行性门、可用维度、关键弱项和阈值：LC/BC/SFC可接受阈值0.80/0.70/0.65，高阈值0.90/0.80/0.75，AE 0.75，可靠MS 0.60/0.75，TSQ 0.70/0.82；D和E按已有执行证据与未完成状态区分。
- 算法1重写为“数据集正式R1/R2/R3 + 单轮LangGraph screening/full闭环”两层伪代码；算法2改为一次选择最小可解释范围、生成局部操作、语义门和最多6个筛查变异体的真实补丁搜索；算法3删除不存在的结构硬阈值，并加入普通经验与对话概念两套真实权重及0.20总分门。
- 调度公式改为`GraphDrivenAdaptiveScheduler`实际得分：0.45缺口、0.30预测奖励、0.15诊断置信度、低样本探索奖励，再扣除能力成本、失败率和连续重复惩罚；可执行性失败时的OracleRepair、Repair和TestValidity覆盖规则单独说明。
- 知识部分加入主JSON与进程分片持久化、普通经验reward公式、失败固定负奖励，以及“正在学习→真实成功验证两次→已掌握”的实际生命周期。实验章节的HumanEvalJava路径、711项任务范围和NRR聚合公式同步当前代码与数据目录。
- 重新绘制论文方法图1至图3并同时更新SVG与PNG：图1展示多语言输入、轮内闭环和正式轮次分支；图2展示建图、动态证据、五维缺口、8步传播和调度回写；图3展示LangGraph节点、三智能体内部能力、结构化输入输出与责任边界。

## 2026-08-31 全代码检查、持久化修复与论文系统界面同步

- 对实验入口、Python/Java执行链、R1/R2/R3工作流、续跑计时、质量计算、知识持久化、监控缓存、任务队列、产物授权和前端状态切换进行了逐段源码检查，不以运行日志代替代码审查。
- 修复已完成实验快照长期不失效、存在状态图即误判为运行中、运行中数据集卡被长期缓存和快速切换时旧请求继续占用资源的问题。结果摘要、运行时、完成标记或关键任务文件发生变化后，页面会立即重建对应快照。
- 修复Windows中文用户路径下注册表、对话、候选测试、评估缓存、共享TSG和知识记录可能保存失败的问题。写入统一采用临时文件替换并在Windows替换失败时可靠降级；长期知识继续以进程分片避免并发覆盖，同时生成可迁移、可直接查看的主JSON。
- 修正Java语义质量判定：异常断言专用测试不再算正常行为测试，恒真和存在性断言继续作为弱断言，参数化、重复和模板测试能够正确计数。Python测试类中的`test_`方法也纳入语义质量分析。
- 修复内部筛查过早进入正式评估的问题。低质量可用候选先在当轮允许范围内按最大缺口修复，达到目标、停止条件或修复上限后再进行完整Mutation评估；正式A-E和知识学习仍只使用`full`结果。
- 知识状态统一为“正在学习、待复核、已掌握、应避免”。重复出现或正奖励不能直接升级，真实成功验证达到要求后才成为“已掌握”；页面、HTML知识报告和测试知识智能体问答使用同一口径。
- 修复Ollama新旧客户端参数兼容、监控服务重启后排队任务丢失模型与服务地址、任务进程日志句柄未可靠关闭以及结束进程残留问题；PIT工作区恢复Defects4J各包深度的默认测试匹配，单任务运行继续使用精确测试类覆盖。
- 验证结果：Python全量回归中127项直接通过，唯一旧断言与当前中文命名、外置知识路径和定向修复策略对齐后，完整工作流单项重跑通过，合计128项验证链通过；Python全模块编译通过；前端TypeScript生产构建和6项测试通过。
- 重新启动本地服务并检查实验总览、HumanEvalJava独立页、测试状态中心、任务与报告、测试知识引导库和数据集切换，页面控制台无警告或错误。当前总览正确显示7个数据集、711项任务、413项A/B/C有效结果和15:05:51累计实验时长；HumanEvalJava的163项旧D级结果继续标为待重新验证，未虚构新指标。
- 论文第4章“系统界面”已同步当前实现：更新实验总览、协作台、测试状态中心、数据集结果矩阵和知识引导库说明，并用真实实验页面重新截取图4至图8。

## 2026-08-26 旧实验知识提炼与智能体界面增强

- 直接读取旧`runs.rar`和代码压缩包中的TestEval总摘要、任务失败与长期知识。822条旧知识被压缩为42类模式，并增加不存在方法调用、原地修改预言、C级整文件替换、无效源码状态和变异超时五类专项规避；与当前基础知识合并后，在`D:\wn\evo`生成92条记录的外置主库、JSON摘要、可视化HTML和迁移说明。
- 测试知识智能体升级为系统助手，掌握四层架构、命令入口、代码职责、数据集、Python/Java工具链、文件/类/项目范围、三轮策略、指标、产物、前端页面和实时实验事实。对话接口不再标记为只读；单元测试概念仍需真实任务成功验证2次才能进入已掌握。
- 三个正式智能体节点升级为TSG能力终端，增加专属数据通道、状态脉冲、能力证据、事件计数和进度光带；1280宽度下自动切换协作区布局，确认节点无重叠、页面无横向溢出且无浏览器错误。

## 2026-08-25 效率根因修复与真实计时

- 最终轮次优先级调整为固定三轮：R1全部基线，R2仅恢复D/E，R3统一优化全部C；R2后仍为D/E直接失败，不允许R4。R2恢复得到的C会与R1原有C一起进入R3。
- C级R3增加专项优化契约：基线指标、最大质量缺口、未覆盖行、存活变异行、目标状态/方法、检索知识和不可退化约束全部注入提示词；第一次筛查后不再允许通用调度覆盖专项计划。Python LLM只可返回最多3个局部操作，不能删除通过测试或退化为整文件重写；Java提示要求保留通过测试并最多修改1个或新增2个目标方法。所有候选仍需工具验证，无增益恢复基线。
- 测试知识智能体增加系统自认知：能区分自身、测试状态智能体和测试生成智能体，理解共享TSG、三轮优先级、双语言/多范围能力、并行和质量指标；回答必须基于实时系统目录、实验、报告与知识事实，不虚构能力。
- 多智能体协作台升级为实时TSG控制阵列，增加三层状态环、按智能体着色的数据链路、事件脉冲、中心焦点任务和并行活动数；动画仍由真实事件状态驱动。
- 明确Python并行语义：默认4个工作槽用于同一数据集目录内的独立源码任务，不是4个数据集并行；数据集目录继续串行。Ollama最多2并发只限制模型推理阶段，其他任务仍可同时进行分析、pytest、覆盖率和变异验证。
- 四个正式入口新增`--knowledge-store`。推荐使用`D:\wn\evo\test_knowledge_guidance.json`作为外置主库；首次指定时从项目默认知识初始化，已存在文件永不被代码包覆盖。终端登记、网页启动、排队恢复、知识可视化和测试知识智能体对话均继承同一路径。
- 新增轻量`batch_runtime.json`，只记录工作槽、模型通道、任务总数和外部知识路径。实验总览与测试生成过程实时展示活动/等待任务；多智能体协作台合并活动任务事件并标注任务名，测试状态中心仍聚焦单任务共享TSG。运行任务不再提前统计为E级。
- 该阶段曾把每轮限制为一次处理；后续已由最新规则覆盖：R1、R2、R3内部最多3次状态定位修复或状态目标优化，并在达标、代码未变化或质量无增益时提前停止。筛查证据复用和正式评估分层继续用于避免重复完整变异评估。
- Python筛查变异结果在正式阶段按变异编号复用，完整预算最多20个但不再重跑已筛查的8个；补丁候选最多筛查6个变异体，并按源码、现有测试、候选补丁和基线指标缓存重复候选结果。
- Python数据集任务默认4线程并行，结果目录、LangGraph检查点和批量结果槽相互隔离；长期知识库写入增加进程内锁和原子替换。本地Ollama最多2请求并发。Java数据集、项目内任务和评估工具继续全局串行，并保留Maven/Gradle增量编译产物。
- 质量门槛按“更佳而非强求最佳”重新校准：A级为行覆盖0.90、分支0.80、SFC 0.75、AE 0.75、可靠Mutation 0.75、TSQ 0.82；B级基础门槛为0.80、0.70、0.65、0.75、0.60和0.70。A仍严格，B允许一个非关键弱项，C仍须通过且具有正常行为和有效断言。
- 修复C级定向优化潜在的事件写入未导入错误、补丁搜索SFC口径不一致、无增益候选丢失既有有效报告、最终完整评价再次触发修复、共享TSG持久化重复保存回滚快照等问题；该能力现统一用于R3。
- D/E恢复契约现在包含失败类别、阶段、原始错误、已尝试措施、建议措施、原失败测试和知识指导；生成提示必须针对原因修复，不能原样重复失败测试。证据完整的D/E续跑直接进入后续轮次，只有缺少原因和诊断的旧残缺摘要重跑R1。
- 批量统计统一为失败总数D+E，同时保留`invalid=D`与`unfinished=E`；Java非主源码统一标记`java_non_main_source`。
- 真实TestEval双任务无LLM实验由优化前925.21秒降至131.50秒，两个任务分别由174.97/749.62秒降至131.18/99.73秒，结果为A和B。旧轮次策略下的真实C级定向优化用128.32秒将Mutation由0.05提高到0.20并由C升为B，该能力现移至R3；真实D/E原因驱动恢复用142.20秒生成A级结果。

## 2026-08-24 三轮实验、结果分级与测试知识引导库

- 实验总览新增开始时间、实时耗时和结束时间。运行中耗时由浏览器按秒递增，完成后固定为真实总耗时；实验范围列表也展示每个目录实验的开始、结束和耗时。后端以任务进程生命周期为准，不再把登记时间或某一轮的中间结束时间误当作整个实验的开始、结束。
- 修复同一输出目录重新运行时沿用旧时间的问题：重新排队会清空上一次的开始、结束和执行结果，真正启动后重新记录本轮时间。
- Java项目级数据集继续默认串行执行，不启用跨项目或同一项目内源码任务并行。原因是项目内类可能共享构建上下文和依赖，多个Maven/JUnit/JaCoCo/PIT任务并发还会争用CPU、内存、磁盘和JVM；RTX 5090只能加速本地大模型推理，不能消除这些构建评估开销。
- 左侧导航统一为“实验总览、启动实验、测试状态中心、多智能体协作台、测试生成过程、任务与报告、测试知识引导库、系统信息”，并同步修改对应页面标题。核对Python变异实现后确认当前默认是每个任务最多尝试20个变异体且串行执行，项目中没有“全局上限8”或“8个并行”的有效配置。
- 实现七项安全缩时策略：Python内部迭代最多串行筛查8个状态相关变异体，正式结果前最多串行评估20个；D/E在基线可运行前跳过覆盖与变异；C级R3保留现有测试并携带存活变异行定向优化；新增LangGraph最终完整评估节点；Java合并JUnit与JaCoCo、稳定候选才执行PIT并保持串行；新增联合哈希缓存与完整目录跳过。
- 系统信息页新增可点击的“执行与缩时策略”，明确区分Python筛查8个、正式20个、Java串行和哈希缓存，避免再把候选上限误解为并行数。
- 新增7项缩时专项测试；结构与批量回归48项、分级与规划定向回归10项通过。一个真实Python冒烟任务依次产生`screening`与`full`报告，最终为A级、pytest通过、`mutation_complete=true`，完整阶段命中覆盖缓存；临时实验目录已删除。
- 该阶段曾采用伪代码块和数学源码块组织算法与公式；2026-08-31参考用户既有期刊稿后，最新格式已改为居中公式右编号、算法输入/输出/编号步骤表、表题在上及表后结果分析。
- 论文新增第4章“单元测试多智能体协作可视化系统”，包含需求分析、系统设计、功能实现、界面展示和系统测试；原实验、讨论、结论顺延为第5至第7章，并统一图表与正文引用编号。
- 新增五张真实系统截图，覆盖实验总览、三智能体协作、测试状态中心、任务质量报告和测试知识引导库。截图来自一次Java真实执行，临时实验目录和监控登记已在截图后删除，长期知识保留。
- 修复有效任务的`valid_baseline`内部标记被前端误显示为失败原因的问题；B级任务现在只展示真实质量弱项，不再出现“未形成有效执行结果”的错误提示。
- 修复第一轮E级任务只有等级、没有失败详情的问题。工作流摘要现在保留`last_error`、候选验证阶段和错误证据；每轮D/E立即生成独立JSON/HTML失败报告，前端同步显示失败阶段、具体原因、原始错误、已采取措施、建议措施和知识记录编号。
- 调整失败学习时序：测试状态智能体当轮归因后，测试知识智能体立即将失败模式记录为“应避免”，后续轮次按任务和失败类别检索后再生成或修复，不再等整批实验结束才形成经验。
- 修正E级详情中的误导提示：缺少覆盖率、AE、变异等数据时显示“未形成可用质量指标”，不再显示“未发现阈值失败”。
- 为旧实验增加兼容恢复：历史D/E摘要没有失败详情时，监控端会从该任务已有的共享TSG中提取候选验证、模型调用和状态修复证据，尽量恢复真实失败原因。
- 修正报告用语：`unique_tasks`改为`followup_task_count`，页面显示“进入后续轮次的任务数”；“后续轮次累计执行次数”单独表示同一任务跨轮重复执行产生的总次数。
- 批量总览继续显示全部有效任务的平均覆盖率、平均AE、平均变异和平均TSQ；单任务HTML只显示当前任务实际值，并明确标注不是批量平均值。批量表格中的“单任务报告”现在直接打开对应HTML，不再优先链接JSON。
- 将结果统计统一为A高质量、B良好、C可用、D无效、E未完成；总任务数恒等于A+B+C+D+E，完整执行数恒等于A+B+C，避免成功、失败、重试次数相互混淆。
- 建立最多三轮的正式实验策略：R1运行全部任务，R2只恢复D/E，R3统一优化C，且不允许R4。报告展示`B·R2`、`B·R3`和`D→C→B`等轨迹，并保留各轮最佳有效结果。
- 每个实验轮次都按“测试知识智能体检索、测试状态智能体规划、测试生成智能体生成执行并验证、测试状态智能体评估分级与规划后续、测试知识智能体写入新经验”运行。验证属于测试生成智能体，最终质量评估属于测试状态智能体，二者都不是新的顶层智能体。
- 新增长期独立知识文件`knowledge_store/test_knowledge_guidance.json`和可视化页面`knowledge_store/test_knowledge_guidance_library.html`，统一名称为“测试知识引导库”。页面展示已掌握、正在学习、待验证和应避免的经验及其使用次数、成功率、质量收益和置信度。
- 批量报告新增A-E总量、轮次统计、等级轨迹，以及D/E任务的具体失败原因、此前解决措施和独立失败报告。总数据集报告同步汇总这些信息。
- 新增`dataset_completion.json`目录级完成标记。只有数据集内全部应处理任务均为A/B/C，续跑时才会在源码遍历前跳过整个目录；存在遗漏或D/E时只续跑未解决任务。
- 修复Java断言生成过弱的问题：对简单算术、范围限制和除法方法生成具体返回值、边界和异常类型断言，不再主要依赖`assertDoesNotThrow`。
- 新增`examples/sample_java/SimpleCalculator.java`并完成一次真实端到端实验。JUnit全部通过，行覆盖和分支覆盖均为100%，AE为92.75%，PIT Mutation为71.43%，TSQ为92.84%，最终为`B·R1`；共享TSG、状态流、JaCoCo/PIT证据、批量报告、完成标记和知识库均有效生成。
- 最终报告明确记录`validation_owner=TestGenerationAgent`、`result_evaluation_owner=TestStateAgent`；状态图中`ValidationPassed`由测试生成智能体传给测试状态智能体，`EvaluationFeedback`由测试状态智能体传回测试生成智能体。
- 快速相关回归24项通过，并完成一个Java真实实验和一次目录级跳过验证；本次按用户要求没有重新执行全部Python和Java数据集。

## 核心功能文档

### 三智能体大框架

当前系统顶层只保留三个主智能体，所有流程围绕三者闭环运行：

1. `PlanningAgent`：规划智能体，负责全局信息管理和流程安排。它读取历史经验，查看 PTEG 状态图和状态流证据，理解失败反馈，识别当前主要问题，制定测试计划、修复计划、优化计划或停止计划。它像“大总管”一样决定做什么、为什么做、交给谁做。
2. `ExecutionAgent`：执行智能体，负责按规划落地操作。它内部包含 `SourceAnalysisAgent`、`TestGenerationAgent`、`RepairOptimizationAgent` 和 Coverage、Assertion、Mutation、Boundary、RuntimeType、TestValidity、OracleRepair 等专项工具。规划智能体要求分析源码、构建状态图、生成测试或修复测试时，都由执行智能体调用对应工具完成。
3. `EvaluationAgent`：评估智能体，负责实时维护状态流证据、评估测试文件质量、归因失败原因、生成反馈和更新经验。若测试不合格，它会把失败项、指标缺口、根因位置、建议动作和经验提示交回 PlanningAgent，由 PlanningAgent 决定下一轮让 ExecutionAgent 修复还是优化。

三者共享同一份 PTEG、`agent_experience.json` 和长期知识库 `agent_knowledge_base.json`。每轮都会交换 `TestIntentPlan`、`ExecutionResult`、`EvaluationFeedback`，形成真正的协作闭环。

### 状态图与状态流

状态图 PTEG 是一个“灰色图”：它把 Python 源码和测试证据统一建模成节点、边、分支和关系。节点包括函数、类、分支、循环、异常、调用、返回、测试用例、断言、运行状态、运行类型和变异体；边包括控制流、调用、执行、观察、依赖、产生、类型流、杀死变异、存活变异、触发和修复目标等。

状态流表示每次智能体操作在图上的流转。pytest、coverage、runtime observation 和 mutation 运行后，GraphUpdater 会把被执行、被观察、被断言、被变异验证的位置写回 PTEG。经过的位置不再只是灰色，而会变成已覆盖或已有证据；未覆盖、断言弱、变异存活、异常缺失、类型未知、超时原因等会保留为待处理证据。图越接近“全覆盖且证据充分”，测试文件越接近合格。

### 三个核心算法

1. 状态流提交、传播和状态图更新算法：
   - GraphUpdater 把本轮 pytest、coverage、runtime observation、mutation 结果提交到 PTEG，新增或更新 Execution、TestCase、Assertion、RuntimeState、RuntimeType、Mutation 等证据节点。
   - 状态流覆盖公式：`SFC = 0.45 * 节点覆盖率 + 0.35 * 边覆盖率 + 0.20 * 分支/路径覆盖率`。
   - GraphDefectPropagation 初始化覆盖缺失、断言缺失、变异存活、异常未覆盖、类型未知等缺陷，再沿 PTEG 边传播：`传播值 = 源缺陷分 * 边权重 * 衰减系数`。最终选择缺陷传播分最高的程序节点作为候选根因。

2. 最小约束修复策略：
   - RootCauseRepairPlanner 从 PTEG 根因、状态流、失败项错误信息、存活变异和断言映射中定位最直接的问题位置。
   - 它只给 LLM 或本地修复工具提交必要上下文和最小修复目标，避免把整个测试文件反复交给大模型重写，减少时间和 token 消耗。
   - 具体修复仍由 LLM 结构化 RepairOperation 或已有工具执行，动作包括替换测试、删除错误测试、追加小测试片段。Patch Search 会在临时副本中验证 pytest、coverage、mutation 和质量指标，只有有效且不降质的补丁才写回正式测试文件。

3. 奖励机制与经验学习算法：
   - AgentExperienceMemory 对每次生成或修复计算收益，记录正确项、失败项和无收益项。
   - 修复收益公式：`Reward = 0.40 * ΔSFC + 0.30 * ΔAE + 0.30 * ΔMutation`，pytest 从失败变成功额外加分，成功但耗时、LLM 调用多或 token 多会扣少量成本；导致 pytest 退化会扣分。
   - 候选生成收益会额外考虑是否通过验证门、失败类别、运行时间和 LLM 调用成本。经验会写入 `agent_experience.json` 和 `agent_knowledge_base.json`，后续相似任务会被检索出来，反馈给 PlanningAgent 用于更快选择修复策略。

### 轮次约束

- 修复机会：默认 `--max-iterations 3`，表示最多 3 轮真实修复。
- 重试机会：默认 `--retry-rounds 2`，只用于 pytest 超时的重复运行。
- 超时不会被一味重试。系统会分析超时根因：若是网络、API 或外部服务等待，会报告外部依赖原因；若是目标函数调用、测试等待、收集阶段或本地执行卡住，会进入最小约束修复闭环。

## 修改记录

## 2026-07-23 真实修复闭环与重试语义修正

- 调整三智能体职责边界：PlanningAgent 不再直接持有或调用分析/生成智能体；源码分析、状态图构建、测试生成和修复工具统一由 ExecutionAgent 内部的执行子工具完成，PlanningAgent 只负责安排计划和根据反馈再规划。
- 增强 ExecutionAgent 初始执行能力：新增 `prepare_initial_test()`，按 PlanningAgent 的初始安排调用 SourceAnalysisAgent 构建 PTEG，再调用 TestGenerationAgent 生成候选测试，并把 AnalysisResult、GenerationResult 写入协作交换记录。
- 修正“把修复理解成重试”的问题：pytest 超时才走重试，默认 `--retry-rounds 2`；测试文件错误、断言错误、低质量测试和覆盖/变异缺口会进入真实修复流程。
- 修正修复轮次语义：`--max-iterations 3` 现在表示最多 3 轮真实修复，主循环最多执行“初始执行 + 3 轮修复后再执行”，不再把 3 次 pytest 执行误当成 3 次修复。
- 增强 EvaluationAgent：pytest 失败时会把失败测试名、失败类别和错误摘要写成结构化修复建议；语法、导入、fixture、收集失败优先交给 TestValidityAgent，断言错误优先交给 OracleRepairAgent，其他失败进入根因约束修复。
- 增强 PlanningAgent：如果初始候选测试被验证门拒绝，不再直接结束；系统会把被拒绝候选或最小 loader 种子写成可修复测试文件，并生成 `validity_repair_plan`，让后续修复智能体有真实对象可操作。
- 增强 ExecutionAgent：`apply_plan()` 不再只调用单个工具后停止，而是按失败类别尝试规划指定工具、相邻专项工具和通用 Repair Optimization Agent；每个工具是否执行、是否修改文件、是否报错都会写入 PTEG 的 `execution_repair_attempts`。
- 增强 pytest 超时诊断：超时重试后仍失败时，系统会分析测试文件中的目标函数调用、`sleep`/等待代码、网络/API/外部服务访问等线索，记录 `timeout_kind`、`timeout_reason`、`likely_location` 和 `should_repair`。
- 调整超时调度策略：非网络原因的超时会优先交给 Repair Optimization Agent，走 PTEG 根因定位和最小约束修复；网络或外部依赖等待会在报告中说明原因，避免盲目改测试代码掩盖环境问题。
- 增强 OracleRepairAgent：修复断言失败时会优先定位失败测试实际调用的目标函数，而不是简单选择源码中的第一个函数，从而更接近“最小导致失败点”的修复。
- 保留并强化最小约束修复策略：Root-Cause Guided Minimal Test Repair 作为策略层负责找根本原因和具体位置，并约束补丁尽量小；具体修复动作仍交给 LLM 修复操作或本地修复工具完成，候选补丁必须通过 pytest、coverage、mutation 和质量指标验证后才能写回。
- 成功修复案例会在下一轮评估中记录到 `agent_experience.json` 和长期知识库 `agent_knowledge_base.json`，包含修复前后指标变化、失败类别、采用智能体和收益，用于后续相似任务学习复用。
- 更新 `README.md`，说明最新三智能体职责、状态图/状态流含义、三类核心算法、3 轮修复、2 轮超时重试、失败信息进入修复逻辑、最小约束修复和经验库记录方式。
- 已新增回归测试，覆盖 PlanningAgent 委派分析/生成给 ExecutionAgent、pytest 超时重试、超时原因和位置记录、非网络超时进入最小修复、失败项进入修复建议、专项修复无变化时自动回退到通用修复、修复轮次语义等场景。
- 验证结果：`python -m compileall -q .` 通过；完整自检 `python -m pytest -q` 通过，结果为 37 项全部通过。

## 2026-07-21 变异诊断、批量总览和源码热图筛选修复

- 修复“变异样本偏少但未说明原因”的问题：`MutationRunner` 现在会输出 `candidate_count`、`attempted_mutants`、`low_sample_reason`、`autonomy_action` 和 `timeout_retries`，明确说明是源码可变异位置少、基线测试不稳定、变异超时、无效变异、环境错误还是有效变异体少于 3 个。
- 修复“超时未再次尝试”的问题：变异基线测试和单个变异体超时时，会自动用更长 timeout 重试一次；仍然超时的样本会保留为 `timeout`，并在报告中说明已重试和后续处理建议。
- 增强 EvaluationFeedback：当有效变异体不足 3 个时，评估反馈不再只说 low-sample，而是把变异低样本原因和系统处理动作一起写入 PTEG，供 PlanningAgent 下一轮补边界、补分支区分断言和补杀变异断言。
- 增强批量经验：经验库中的批量建议改为中文，并补充原因和处理动作；`unknown` 失败会保留任务名、状态和错误摘要，避免只显示 `unknown: 1` 而没有解释。
- 增强批量测试质量总览：`batch_summary.json` 新增 `quality_overview`，统计平均行覆盖、平均分支覆盖、平均 SFC、平均 AE、平均变异检出、平均 SFQ、有效断言总数、弱断言总数、平均有效变异体和低变异可靠任务；HTML 总报告同步展示这些总指标。
- 增强执行报告断言数据：`ExecutionReport` 新增 `assertion_count`、`weak_assertions`、`effective_assertions` 和 `assertion_quality_explanation`，批量原始数据和报告都能说明有效断言来源。
- 规范源码证据热图筛选：移除容易混淆的“只看运行时”；保留并规范为“只看行覆盖、只看分支覆盖、只看有效断言、只看变异检出、只看关注度”。选择某一类后会隐藏其它标签，并去除非当前视图的行背景干扰。
- 规范热图颜色：行覆盖使用绿色/灰色；分支覆盖使用黄色/灰色；断言使用强断言/弱断言标签；变异显示变异已杀、存活变异、变异超时、无效变异、环境错误、疑似等价；关注度使用百分比标签。
- 验证结果：`python -m compileall -q .` 通过；新增报告测试 3 项通过；完整自检 `python -m pytest -q` 通过，结果为 31 项全部通过。

## 2026-07-21 三智能体协作闭环改造

- 新增 `agents/planning_agent.py`，把源码分析、历史经验读取、初始测试意图生成、反馈后再规划和调度决策统一收束到 `PlanningAgent`。旧的 `SourceAnalysisAgent`、`TestGenerationAgent` 和 `GraphDrivenAdaptiveScheduler` 没有删除，改为 PlanningAgent 内部复用能力，避免改变原有生成效果。
- 增强 `agents/evaluation_agent.py`，新增结构化 `EvaluationFeedback`。评估智能体现在会把质量状态、可用性、主要缺口、修复建议、执行证据、根因证据和经验学习结果统一写回 PTEG。
- 增强 `agents/execution_agent.py`，新增 `last_execution_result` 和协作交换记录，并增加 `apply_plan()`，让执行智能体负责按 PlanningAgent 的计划调用原有局部修复工具。
- 调整 `agents/specialized_agents.py`，Coverage、Assertion、Mutation、Boundary、RuntimeType、TestValidity、OracleRepair 等原有专项能力继续保留，但作为 ExecutionAgent 执行计划时可调用的内部工具，不再作为顶层主流程角色。
- 改造 `main.py` 主循环为真正的三智能体闭环：`PlanningAgent -> ExecutionAgent -> EvaluationAgent -> PlanningAgent`。每轮报告现在包含 `test_intent_plan` 和 `evaluation_feedback`，总摘要中新增 `test_intent_plans`。
- 更新 `README.md`，说明当前顶层主智能体只有 PlanningAgent、ExecutionAgent、EvaluationAgent，三者通过 PTEG 和 `agent_experience.json` 共享计划、执行证据和反馈。
- 新增/更新回归测试，验证 PlanningAgent 能从 EvaluationFeedback 重新规划，主流程会写入协作模型、测试意图计划和评估反馈。
- 验证结果：`python -m compileall -q .` 通过；关键闭环测试 3 项通过；完整自检 `python -m pytest -q` 通过，结果为 29 项全部通过；单条样例 `examples/sample_target.py` 跑通，输出目录 `_collab_loop_check` 中已生成三智能体闭环字段和报告。

## 2026-07-20 智能体代码增强：批量经验、变异数量、失败原因和断言质量

- 将原 `记忆.md` 改名为 `修改日志.md`，用于记录项目代码修改历史，避免和智能体运行时机器记忆混淆。
- 修改 `agents/generation_agent.py`：测试生成智能体现在会优先从函数 docstring 示例中提取真实调用输入，并通过实际运行目标函数得到期望输出，再生成明确的返回值断言；同时扩大启发式输入候选，避免把 `l`、`a`、`b` 这类泛化参数名错误当成数字，减少只生成异常测试的问题。
- 修改 LLM 生成提示词：要求优先生成正常行为测试，每个公共函数只要可能就必须有非异常用例，并优先使用明确返回值、长度、布尔值或近似数值断言。
- 修改 `executor/mutation_runner.py`：默认最大变异体数量从 5 提高到 20，并新增数字常量、布尔逻辑、`not` 等变异类型，使变异测试样本更多，降低“少量变异体导致分数虚高”的风险。
- 修改 `executor/test_quality.py`：增强弱断言识别，`assert True`、`result == None`、`result is None` 等会被视为弱断言，避免低质量断言绕过检查。
- 修改 `graph/experience_memory.py` 和 `batch_generate_tests.py`：批量运行结束后自动写入机器可读的批量经验到 `agent_experience.json`，包括失败类别、低质量拒绝任务、低变异可信度任务和下一批生成建议。
- 修改 `visualization/graph_visualizer.py`：批量 HTML 报告新增“原因”列。成功任务会提示变异样本是否偏少；失败任务会说明是“低质量测试被拒绝：只有异常测试/缺少正常行为/断言过弱”，或展示具体错误摘要。
- 新增回归测试：覆盖 docstring 示例驱动生成、批量经验写入与复用、低质量异常-only 测试拒绝等场景。
- 验证结果：`task_043.py` 从原先失败变为可生成 8 个正常行为测试，pytest 通过，有效变异体 7 个，质量达到 `quality_high`；`task_154.py` 可生成 9 个正常行为测试，pytest 通过，有效变异体 11 个；完整自检 `python -m pytest -q` 通过，28 项全部通过。

## 操作历史

- 2026-07-13：创建多智能体单元测试生成系统项目结构。
- 2026-07-13：实现 State Flow Graph、五个智能体、pytest/coverage/mutation 执行器、SFC/AE/Mutation/SFQ 指标、命令行入口、示例和自检测试。
- 2026-07-13：增强测试生成逻辑，优先使用类型注解，并通过试跑候选输入生成具体返回值断言。
- 2026-07-13：增强边界值输入，覆盖负数、阈值、空值、字符串、对象等路径。
- 2026-07-13：升级为 Program-Test Evidence Graph（PTEG）框架，在原图基础上新增 Execution、TestCase、Assertion、Mutation、RuntimeState、RuntimeType 证据节点。
- 2026-07-13：新增 Execute、Observe、Kill、Survive、Produce、Depend、Trigger、TypeFlow、RepairTarget 等证据关系。
- 2026-07-13：新增 `graph/graph_query.py`，提供统一查询接口：根因、最高缺陷、未覆盖路径、存活变异、运行时类型、相关断言、执行轨迹、候选修复。
- 2026-07-13：新增 `graph/graph_defect_propagation.py`，实现图缺陷传播，把覆盖缺失、断言不足、变异存活、异常缺失、类型未知传播到更可能的根因节点。
- 2026-07-13：新增 `graph/adaptive_scheduler.py`，实现图驱动动态调度，根据 Coverage、Mutation、Assertion、Exception、RuntimeType 缺口选择下一步动作。
- 2026-07-13：新增 `graph/repair_planner.py`，实现根因约束的最小测试修复，不重写整个测试文件，只围绕根因节点追加小补丁。
- 2026-07-13：改造 Execution Agent，使每轮执行后把 pytest、coverage、mutation 结果写入 PTEG，并自动执行缺陷传播。
- 2026-07-13：改造 Evaluation Agent 和 Repair Agent，使其通过图查询和根因修复计划工作。
- 2026-07-13：改造 `main.py`，加入 Graph-driven Adaptive Scheduling，不再只依赖固定的生成、执行、评估、修复顺序。
- 2026-07-13：修正运行时类型证据，避免没有静态注解的返回值被误判成类型缺陷。
- 2026-07-13：项目自检通过：`python -m pytest -q`，结果为 4 个测试全部通过。
- 2026-07-13：完整示例通过：`python main.py --source examples/sample_target.py --out generated_tests_run --max-iterations 2`，最终 SFC=1.0、AE=1.0、Mutation=1.0、SFQ=1.0。
- 2026-07-14：新增 `batch_generate_tests.py` 批量生成入口，可直接处理 `humaneval_unittest_ai_agent_runs/sources` 下的 164 个 Python 文件；默认只生成测试，也可通过 `--execute` 执行完整质量检查。
- 2026-07-14：曾确认旧版本测试生成主要是本地规则和执行反馈，尚未真正调用大模型；随后按 LLM 多 Agent 目标进行了接入改造。
- 2026-07-14：根据“LLM 多 Agent 协作”目标修正实现方向，新增 `llm_client.py` OpenAI 兼容客户端，并在 Test Generation Agent 和 Repair Optimization Agent 中接入 LLM。命令行支持 `--llm` 和 `--llm-required`，启用后测试生成与修复由大模型完成，pytest/coverage/mutation 作为本地质量评估闭环。
- 2026-07-14：进一步规范为“所有正式 Agent 均可使用大模型”：新增 `llm_config.py` 作为统一配置中心；Source Analysis、Test Generation、Execution、Evaluation、Repair Optimization 五个 Agent 共用同一个 OpenAI 兼容 LLM 客户端。`--llm-required` 模式下任一 Agent 的模型调用失败都会中止流程，避免伪装成 LLM 多 Agent。
- 2026-07-14：新增 `run_agent.py` 正式短启动入口，只需输入目标 Python 文件或源码目录。该入口默认启用并强制使用 LLM，输出默认写入 `My_Mutil_Agent/generated_tests_llm`，模型配置集中读取 `llm_config.py`。
- 2026-07-14：修正主循环逻辑：每轮执行后先根据 SFC、AE、Mutation Score 判断是否达标；若达标或已到最大迭代轮数则停止，只有确实还有下一轮时才进行调度和修复。新增测试覆盖 Execution Agent 的 LLM 执行分析和指标驱动迭代。
- 2026-07-14：补强图驱动自适应调度和根因最小修复：新增 `agents/specialized_agents.py`，调度结果会真实分派到 Coverage、Assertion、Mutation、Boundary、RuntimeType 五类专门智能体；LLM 修复优先执行 Root Cause Guided Minimal Test Repair，只追加根因约束的最小补丁，不再默认整文件重写。
- 2026-07-15：新增 `graph/experience_memory.py`，记录每次专门 Agent 执行前后的 SFC、AE、Mutation、SFQ 变化，形成持久化历史经验，并在 `GraphDrivenAdaptiveScheduler` 中结合当前 gap 和历史收益预测下一轮最可能提升质量的 Agent。
- 2026-07-15：新增 `graph/patch_search.py`，对 LLM 最小补丁和根因规划补丁进行临时试运行，验证 pytest、coverage 和 mutation；只有不降低 SFC、AE、Mutation、SFQ 且产生可测收益的补丁才写回测试文件。
- 2026-07-15：将 LLM 默认调用方式改为 Ollama 原生客户端：`Client(host="http://127.0.0.1:11434").chat(model="deepseek-r1:14b", messages=...)`，统一配置仍在 `llm_config.py`。
- 2026-07-15：增强 Graph Defect Propagation，不再只传播分数，而是传播 `defect_reasons` 原因链，并在根因结果中输出 `root_cause_reasons` 和 `root_cause_explanation`。
- 2026-07-15：增强 Graph-driven Adaptive Scheduling，调度器输出 `stop/verify/repair` 动作决策、置信度、预期收益和策略来源；主循环服从调度器动作，而不是只按固定流程选择 Agent。
- 2026-07-15：按最新审查要求补齐真实执行链路：停止条件必须包含 pytest 通过，并记录 `quality_reached`、`max_iterations_reached`、`no_improvement`、`repair_failed`、`execution_invalid`、`budget_exhausted` 等停止原因；连续两轮综合质量提升不足会停止。
- 2026-07-15：重构变异测试分类，新增 `killed`、`survived`、`timeout`、`invalid`、`infra_error`、`equivalent` 状态；每轮变异前先跑原始源码基线测试，基线失败则本轮 invalid；无变异体时 Mutation Score 记为 N/A。
- 2026-07-15：增强 PTEG 证据层：Mutation 证据节点携带 `execution_id`、`iteration`、`mutant_id`、`status`；调度状态只读取最近一次 Execution 的变异结果，历史变异证据只用于趋势分析。
- 2026-07-15：新增 `executor/runtime_observer.py`，从测试 AST 中提取目标调用并在隔离子进程中采集真实参数类型、返回类型、异常类型和有限值摘要；GraphUpdater 将这些运行观察写入 RuntimeState 和 RuntimeType。
- 2026-07-15：增强断言依赖映射，不再把断言平均分配给所有覆盖节点，而是根据赋值变量、直接调用和 `pytest.raises` 建立 Assertion 到 RuntimeState 的 Observe 关系；无效断言不计为有效断言。
- 2026-07-15：补强 CFG 构建，新增显式函数出口、if join、loop exit、match join、break/continue、return/raise 到出口等控制流节点和边；coverage 使用 branch/arc 数据映射真实执行边。
- 2026-07-15：补丁验证改为真实指标验证：候选补丁先写入临时测试副本，运行 pytest、coverage、mutation，克隆 PTEG 执行完整 GraphUpdater，再重新计算 SFC、AE、Mutation、SFQ；完整文件重写也必须经过同一验证流程。
- 2026-07-15：调度器进一步结合 gap、历史收益、执行诊断置信度、成本、失败率、重复执行惩罚和低样本探索率；经验记忆按模型隔离，避免不同模型效果混用。测试失败时优先走修复/诊断，而不是继续追覆盖率。
- 2026-07-15：项目自检通过：`python -m pytest -q`，结果为 12 个测试全部通过，耗时约 147.46 秒。
- 2026-07-15：生成项目迁移包和依赖文件：新增 `依赖.txt`，并将项目代码与 `humaneval_unittest_ai_agent_runs/sources` 数据集 164 个 Python 文件打包为 `My_Mutil_Agent_with_dataset_20260715_112019.zip`；压缩包包含 `My_Mutil_Agent/`、`dataset/sources/` 和 `启动说明.txt`，已核验无 `__pycache__` 缓存文件。
- 2026-07-15：修复 LLM 生成测试文件的 Path loader 常见错误：新增 `agents/test_code_normalizer.py`，在生成测试和完整文件修复时自动补 `from pathlib import Path`，并把 `target_module = TARGET_PATH.resolve()` 修正为 `target_module = Path(TARGET_PATH).resolve()`；已验证 task_000/task_001 生成测试可完成 pytest 收集，项目自检为 14 个测试全部通过。
- 2026-07-15：继续增强 `agents/test_code_normalizer.py`，修复 LLM 把目标函数名误当模块导入的问题，例如 `import has_close_elements`。规范化器现在会移除这类错误导入，插入稳定的 `importlib.util.spec_from_file_location` loader，并绑定 `has_close_elements = target_module.has_close_elements` 这类函数别名；已新增测试覆盖该场景，项目自检为 15 个测试全部通过。
- 2026-07-15：按“LLM 不直接生成完整 pytest 文件”的新约束重构测试生成链路：新增 `agents/test_plan.py`，LLM 只输出 JSON 测试计划，确定性渲染器负责固定 loader、fixture、import、`STATEFLOW_TARGET` 和 `target.<function>()` 调用；新增 `agents/candidate_validator.py`，候选测试必须通过 AST/compile、pytest collect-only、collected_count>0 和候选 pytest 后才原子写入正式文件，失败候选会保存阶段、类别和 pytest 摘要。
- 2026-07-15：新增 `executor/failure_classifier.py` 和 pytest collect-only 支持，确定性分类 `syntax_error`、`import_error`、`collection_error`、`fixture_error`、`no_tests_collected`、`oracle_error`、`runtime_error`、`timeout`、`valid_baseline`；Execution Agent 改为 pytest 失败时不运行 coverage/mutation，质量状态标为 invalid，mutation_score=N/A，并只允许 TestValidityAgent 或 OracleRepairAgent。
- 2026-07-15：新增结构化修复操作 `agents/test_repair_operations.py`，并实现 TestValidityAgent 与 OracleRepairAgent。修复动作支持 replace_test、delete_test、add_test，不再只能追加测试；修复候选同样必须通过候选验证门。
- 2026-07-15：增强经验库 `graph/experience_memory.py`，新增候选级经验记录、任务签名、相似经验检索、成功策略复用、失败模式规避、时间衰减、版本权重和奖励函数；主流程在分析后加载相似经验并传给测试生成，候选验证后立即写入经验。
- 2026-07-15：分离当前指标和历史证据：GraphUpdater 新增 current-run reset，每次有效执行前清空程序层 visited、assert_count、mutation_status 和边访问状态，再用本轮 coverage/assertion/mutation 重新计算 SFC、AE、Mutation、SFQ，避免历史累计造成虚假增长。
- 2026-07-15：批处理结果状态改为 generated、valid、quality_reached、failed；pytest_passed=false 时不再计入成功。项目自检通过：`python -m pytest -q`，15 个测试全部通过，耗时约 179.73 秒。
- 2026-07-18：根据164任务批处理结果完成局部修复：默认仅发现并数值排序 `task_*.py`，跳过 `__init__.py`；Coverage 拆分为行、分支和综合覆盖率；Mutation 无有效变异体时按 N/A 重新归一化 SFQ；AE 不再重复计入 Mutation；引入 `valid_usable`、`quality_acceptable`、`quality_high`、`plateau_reached`、`budget_limited` 等分层状态；未产生文件变化的修复不再重复执行；记录每轮指标增量、LLM 调用、Token、时间和正/零/负经验；观察候选超时被局部隔离；新增完全离线的 SVG、交互 HTML 和源码覆盖热力图。项目自检 22 项全部通过。
- 2026-07-17：继续补强失败规避和指标可信度：Analysis、Execution、Evaluation 在 JSON/schema 错误时非致命回退，模型连接错误仍明确失败；Repair Agent 不再接收 LLM Python，改为结构化 RepairOperation JSON；AE 改为基于 Assertion->Observe->RuntimeState 的 oracle 覆盖和 specificity 计算，并新增 AE>=0.9 校准测试；无 coverage arc 时边覆盖标记为 unknown，不再端点推断；MutationResult 报告 effective_mutants 和 score_reliability；经验记录补齐 target_evidence_ids、input_partition、boundary_operator、oracle_kind、candidate_status、before/after metrics、quality_delta、strategy_fingerprint；RuntimeObservation/TestCase 增加 pytest nodeid；强制路由 TestValidity/OracleRepair 时也参考历史失败率。项目自检通过：`python -m pytest -q`，16 个测试全部通过，耗时约 123.66 秒。

## 功能文档

本项目实现一个面向 Python 动态程序分析和自动单元测试生成的多智能体系统。

当前核心是 Program-Test Evidence Graph（PTEG）。它不仅保存程序结构，还把测试、断言、执行、运行时状态、运行时类型和变异结果都建模为图中的节点和关系。

主要节点类型：

- 程序结构：Function、Class、Branch、Loop、Return、Call、Exception、Raise、End。
- 测试证据：TestCase、Assertion、Mutation、RuntimeState、RuntimeType、Execution。

主要关系类型：

- 控制和调用关系：flow、true、false、loop_body、loop_back、call、return、raise、except。
- 证据关系：Execute、Observe、Kill、Survive、Produce、Depend、Trigger、TypeFlow、RepairTarget。

核心流程：

1. Source Analysis Agent 读取源码并构建 PTEG 的程序结构层。
2. Test Generation Agent 根据图生成 pytest 测试。
3. Execution Agent 运行 pytest、coverage 和 mutation，并把结果写成证据节点和证据边。
4. Graph Defect Propagation 在图中传播覆盖、断言、变异、异常和类型缺陷，定位根因节点。
5. Graph-driven Adaptive Scheduler 根据全局缺口动态选择下一步动作。
6. Evaluation Agent 通过统一 Graph Query API 获取候选修复点。
7. Repair Optimization Agent 使用 Root Cause Repair Planner 做最小测试补丁。
8. 系统循环执行，直到达到质量阈值或达到最大迭代次数。

质量指标：

- SFC：状态流覆盖率。
- AE：断言有效性。
- Mutation Score：变异得分。
- SFQ：综合质量分。

输出文件：

- `state_flow_graph.json`：完整 PTEG。
- `stateflow_summary.json`：每轮执行、评分和调度摘要。
- `test_*_stateflow.py`：生成和修复后的 pytest 测试。

## 论文 Method 算法抽象

本节不按代码逐行说明，而是从项目中抽象出可写入 ASE/FSE 论文 Method 部分的方法模块。每个算法都能映射到当前代码位置。

### 真正的方法创新模块

#### Algorithm 1：Program-Test Evidence Graph Construction

① 算法名称  
Program-Test Evidence Graph Construction，简称 PTEG Construction。

② 算法目标  
把 Python 程序结构、控制流、异常流、调用关系和可测试状态统一转换为一个多层异构程序测试证据图，为后续测试生成、执行反馈、缺陷传播和修复提供统一知识中心。

③ 输入  
- Python 源程序 P。
- 程序语法结构 A。

④ 输出  
- Program-Test Evidence Graph：G = (V, E, X)。

⑤ 数据结构  
- V：异构节点集合。
  - 程序节点：Function、Class、Branch、Loop、Return、Call、Exception、Raise、End。
  - 证据节点预留类型：TestCase、Assertion、Mutation、RuntimeState、RuntimeType、Execution。
- E：异构边集合。
  - 控制和结构边：flow、true、false、loop_body、loop_back、return、raise、except、call、defines。
  - 证据边预留类型：Execute、Observe、Kill、Survive、Produce、Depend、Trigger、TypeFlow、RepairTarget。
- X：节点属性和边属性。
  - 节点属性包括位置、访问状态、优先级、缺陷分数、版本信息。
  - 边属性包括边类型、访问状态、权重。

⑥ 核心流程  
1. 解析源程序，得到程序语法结构。
2. 为函数、类、分支、循环、异常、返回、调用等程序元素建立程序节点。
3. 根据结构关系建立控制流、调用流、异常流和返回流边。
4. 为无显式出口的程序状态连接 End 节点。
5. 初始化节点优先级和缺陷字段。
6. 保存图版本和构建历史。

⑦ 数学定义  
定义 PTEG 为：

```text
G = (V_p union V_e, E_c union E_e, X)
```

其中：

```text
V_p = program structure nodes
V_e = test evidence nodes
E_c = program control / call / exception relations
E_e = runtime / test / assertion / mutation evidence relations
```

初始构图阶段主要生成：

```text
G_0 = (V_p, E_c, X_0)
```

执行反馈后逐步扩展为：

```text
G_t = (V_p union V_e^t, E_c union E_e^t, X_t)
```

⑧ 伪代码  

```text
Algorithm 1: PTEG Construction
Input: Python program P
Output: Initial evidence graph G_0

1. Parse P into structural representation A
2. Initialize graph G_0
3. For each structural element s in A:
      create a program node v_s
      assign node type, source location, priority, and initial defect fields
4. For each relation r between structural elements:
      create an edge e_r with relation type and weight
5. Connect terminal program states to End nodes
6. Record graph version and construction history
7. Return G_0
```

⑨ 对应代码位置  
- `graph/graph_builder.py`
- `graph/state_graph.py`
- `graph/node.py`
- `graph/edge.py`

---

#### Algorithm 2：Evidence Graph Update

① 算法名称  
Program-Test Evidence Graph Update，简称 Evidence Update。

② 算法目标  
测试执行后，不把 coverage、assertion、mutation 仅作为节点属性覆盖写入，而是增量创建 Execution、TestCase、Assertion、RuntimeState、RuntimeType、Mutation 等证据节点，并用图关系表达它们与程序结构之间的依赖。

③ 输入  
- 当前证据图 G_t。
- 测试套件 T。
- 测试执行结果 R_test。
- 覆盖率结果 R_cov。
- 变异测试结果 R_mut。

④ 输出  
- 更新后的证据图 G_{t+1}。

⑤ 数据结构  
- Execution 节点：表示一次测试执行。
- TestCase 节点：表示测试函数。
- Assertion 节点：表示断言或异常断言。
- RuntimeState 节点：表示一次函数运行产生的状态。
- RuntimeType 节点：表示参数或返回值的运行时类型证据。
- Mutation 节点：表示一次变异体。
- 证据边：
  - TestCase Execute Function
  - Execution Produce RuntimeState
  - Assertion Observe RuntimeState
  - RuntimeState TypeFlow RuntimeType
  - Mutation Kill TestCase
  - Mutation Survive Execution
  - Mutation Trigger ProgramNode
  - Mutation RepairTarget ProgramNode

⑥ 核心流程  
1. 为本轮执行创建 Execution 节点。
2. 根据覆盖行号标记被访问的程序节点。
3. 对每个被覆盖函数创建 RuntimeState 节点。
4. 为函数输入和返回创建 RuntimeType 节点。
5. 从测试文件中抽取 TestCase 和 Assertion 节点。
6. 建立 TestCase、Assertion 与 RuntimeState、Function 的关系。
7. 根据变异结果创建 Mutation 节点。
8. 对被杀死的变异建立 Kill 关系；对存活变异建立 Survive 和 RepairTarget 关系。
9. 保留历史，不覆盖旧证据。

⑦ 数学定义  
第 t 轮执行产生证据：

```text
Delta V_t = V_exec union V_test union V_assert union V_state union V_type union V_mut
Delta E_t = E_execute union E_observe union E_kill union E_survive union E_produce union E_typeflow
```

图增量更新为：

```text
G_{t+1} = G_t union Delta V_t union Delta E_t
```

证据不覆盖历史，而是追加：

```text
History(G_{t+1}) = History(G_t) append Record(t)
```

⑧ 伪代码  

```text
Algorithm 2: Evidence Graph Update
Input: Graph G_t, tests T, execution result R_test, coverage R_cov, mutation R_mut
Output: Updated graph G_{t+1}

1. Create an Execution node v_exec for the current run
2. Mark program nodes covered by R_cov as visited
3. For each covered function f:
      create RuntimeState node s_f
      add edge v_exec Produce s_f
      add edge s_f Depend f
      create RuntimeType nodes for observed interface types
      add TypeFlow edges
4. For each test case tc in T:
      create TestCase node v_tc
      add edge v_tc Execute target function
5. For each assertion a in tc:
      create Assertion node v_a
      add edge v_tc Depend v_a
      add edge v_a Observe related RuntimeState
6. For each mutation m in R_mut:
      create Mutation node v_m
      connect v_m to affected program nodes through Trigger
      if m is killed:
          add edge v_m Kill related TestCase
      else:
          add edge v_m Survive v_exec
          add edge v_m RepairTarget affected program node
7. Append update record to graph history
8. Return G_{t+1}
```

⑨ 对应代码位置  
- `graph/graph_updater.py`
- `agents/execution_agent.py`
- `executor/pytest_runner.py`
- `executor/coverage_runner.py`
- `executor/mutation_runner.py`

说明：当前代码实现了函数级 RuntimeState 和函数接口级 RuntimeType。论文完整定义可以扩展到局部变量类型，但当前实现映射到“函数接口运行时类型证据”。

---

#### Algorithm 3：Graph Defect Propagation

① 算法名称  
Graph Defect Propagation，简称 GDP。

② 算法目标  
不直接把未覆盖、弱断言或存活变异所在节点当成修复目标，而是在 PTEG 中沿结构边和证据边传播缺陷分数，定位更可能导致测试质量下降的根因节点。

③ 输入  
- 当前证据图 G = (V, E, X)。
- 每类边的传播权重 W。
- 最大传播轮数 K。
- 衰减系数 gamma。

④ 输出  
- 每个节点的缺陷向量。
- 每个节点的综合缺陷分数。
- 根因节点 v_root。

⑤ 数据结构  
每个节点维护缺陷向量：

```text
D(v) = [d_cov(v), d_assert(v), d_mut(v), d_exc(v), d_type(v)]
```

其中每个分量范围为：

```text
0 <= d_i(v) <= 1
```

综合缺陷分数：

```text
S(v) = max D(v)
```

边权重：

```text
w(e) in [0, 1]
```

⑥ 核心流程  
1. 初始化缺陷：
   - 未覆盖程序节点：coverage defect = 1。
   - 已覆盖但缺少断言映射：assertion defect = 1。
   - 存活变异节点：mutation defect = 1。
   - 未触发异常节点：exception defect = 1。
   - 未知运行时类型节点：type defect = 1。
2. 为每个节点计算初始综合缺陷分数。
3. 在图中按边权重反向传播缺陷。
4. 对 Trigger 和 RepairTarget 这类证据到程序的边，缺陷从证据节点传播到程序节点。
5. 多轮迭代后选取得分最高的程序节点作为根因。

⑦ 数学定义  
初始化：

```text
S_0(v) = max(d_cov(v), d_assert(v), d_mut(v), d_exc(v), d_type(v))
```

一般边的反向传播：

```text
S_{k+1}(u) = max(S_k(u), gamma * w(u, v) * S_k(v))
```

证据触发边的定向传播：

```text
S_{k+1}(v) = max(S_k(v), gamma * w(u, v) * S_k(u))
```

根因选择：

```text
v_root = argmax_{v in V_program} S_K(v)
```

⑧ 伪代码  

```text
Algorithm 3: Graph Defect Propagation
Input: Graph G, edge weights W, iteration limit K, decay gamma
Output: Root-cause node v_root and defect scores S

1. For each node v in G:
      initialize defect vector D(v)
      set S(v) = max D(v)
2. Repeat K times:
      initialize propagated score A(v) = 0 for every node
      For each edge e = (u, v):
          get edge weight w(e)
          if e is Trigger or RepairTarget:
              A(v) = max(A(v), gamma * w(e) * S(u))
          else:
              A(u) = max(A(u), gamma * w(e) * S(v))
      For each node v:
          S(v) = max(S(v), A(v))
3. Select v_root with maximum S(v) among program nodes
4. Return v_root and all S(v)
```

⑨ 对应代码位置  
- `graph/graph_defect_propagation.py`
- `graph/graph_query.py`
- `agents/execution_agent.py`

---

#### Algorithm 4：Graph Query Interface for Root-Cause Reasoning

① 算法名称  
Graph Query Interface for Evidence-Based Repair，简称 GQ-R。

② 算法目标  
为所有智能体提供统一的图查询接口，避免各智能体分别解析图结构。该接口把 PTEG 中的证据关系转换成根因、执行轨迹、候选修复、存活变异和覆盖缺口等决策对象。

③ 输入  
- 当前证据图 G。
- 可选目标节点 v。

④ 输出  
- 根因路径。
- 最高缺陷节点。
- 未覆盖路径。
- 存活变异集合。
- 运行时类型集合。
- 相关断言集合。
- 执行轨迹。
- 候选修复集合。

⑤ 数据结构  
- Program node set：V_program。
- Evidence node set：V_evidence。
- Reverse evidence path：从目标节点反向搜索得到的程序结构路径。
- Candidate repair record：由根因节点、相关测试、建议动作组成。

⑥ 核心流程  
1. 将节点划分为程序节点和证据节点。
2. 在程序结构子图中反向搜索目标节点的祖先路径。
3. 在路径中选择缺陷分数最高的函数、类、分支或循环作为根因。
4. 根据证据边查找相关测试和断言。
5. 根据节点缺陷向量生成候选修复动作。

⑦ 数学定义  
给定目标节点 v，反向可达路径集合为：

```text
R(v) = {u | u can reach v through reversed program edges}
```

根因查询定义为：

```text
Root(v) = argmax_{u in R(v) and u in V_root_candidate} S(u)
```

候选修复集合：

```text
C = {(u, T_u, A_u) | S(u) > 0}
```

其中 T_u 是与 u 相关的测试集合，A_u 是由缺陷类型映射出的动作集合。

⑧ 伪代码  

```text
Algorithm 4: Graph Query for Root-Cause Reasoning
Input: Graph G, optional target node v
Output: Root cause, related tests, related assertions, repair candidates

1. Partition nodes into program nodes and evidence nodes
2. If target v is not provided:
      choose the node with the highest defect score
3. Search reversed program edges from v to obtain path R(v)
4. Select root node with the highest defect score on R(v)
5. Retrieve TestCase nodes linked to the root through Execute edges
6. Retrieve Assertion nodes linked through Observe and Depend edges
7. Convert defect vector of each candidate node into repair actions
8. Return root-cause record and candidate repairs
```

⑨ 对应代码位置  
- `graph/graph_query.py`
- `agents/evaluation_agent.py`
- `graph/repair_planner.py`

---

#### Algorithm 5：Graph-Driven Adaptive Multi-Agent Scheduling

① 算法名称  
Graph-Driven Adaptive Scheduling，简称 GDAS。

② 算法目标  
打破固定的 Generation -> Execution -> Evaluation -> Repair 顺序。每轮根据 PTEG 的全局缺口动态选择下一步智能体动作，使系统优先处理当前最主要的质量瓶颈。

③ 输入  
- 当前证据图 G。
- 当前质量状态 Q。

④ 输出  
- 下一步智能体决策 a_t。
- 决策理由和各类 gap。

⑤ 数据结构  
全局缺口向量：

```text
Gap(G) = [g_cov, g_mut, g_assert, g_exc, g_type]
```

智能体动作集合：

```text
A = {CoverageAgent, MutationAgent, AssertionAgent, BoundaryAgent, TypeAnalysisAgent, ExecutionAgent}
```

⑥ 核心流程  
1. 从图查询接口计算 coverage、mutation、assertion、exception、runtime type 五类缺口。
2. 选择最大缺口。
3. 将最大缺口映射到对应智能体动作。
4. 如果所有缺口低于极小阈值，则选择执行验证。
5. 记录调度决策到图历史。

⑦ 数学定义  
各类 gap 定义：

```text
g_cov = uncovered program nodes / all program nodes
g_mut = survived mutations / all mutations
g_assert = visited nodes without assertion / visited nodes
g_exc = uncovered exception nodes / exception nodes
g_type = 1 - typed functions / all functions
```

调度策略：

```text
a_t = policy(argmax_i Gap_i(G_t))
```

若：

```text
max_i Gap_i(G_t) <= epsilon
```

则：

```text
a_t = ExecutionAgent
```

⑧ 伪代码  

```text
Algorithm 5: Graph-Driven Adaptive Scheduling
Input: Graph G_t
Output: Next agent action a_t

1. Compute gap vector Gap(G_t)
2. Let i be the index of the largest gap
3. If Gap_i is smaller than epsilon:
      choose ExecutionAgent
4. Otherwise:
      map i to the corresponding specialized agent
5. Append the decision to graph history
6. Return selected agent action
```

⑨ 对应代码位置  
- `graph/adaptive_scheduler.py`
- `graph/graph_query.py`
- `main.py`

---

#### Algorithm 6：Root-Cause Guided Minimal Test Repair

① 算法名称  
Root-Cause Guided Minimal Test Repair，简称 RCMR。

② 算法目标  
修复测试质量问题时，不重新生成整个测试套件，而是依据 PTEG 中传播得到的根因节点和依赖关系，只修改或追加影响该根因的最小测试片段，从而尽量保持已有覆盖、断言和变异检测能力不被破坏。

③ 输入  
- 当前证据图 G。
- 根因节点 v_root。
- 当前测试套件 T。
- 候选修复动作 A。

④ 输出  
- 最小修复计划 p。
- 修复后的测试套件 T'。

⑤ 数据结构  
MinimalRepairPlan：

```text
p = (root_node, target_test, action, patch, reason)
```

候选动作包括：

```text
add_boundary_or_path_case
strengthen_assertion
add_mutation_killing_assertion
add_exception_case
add_runtime_type_case
```

⑥ 核心流程  
1. 查询当前最高缺陷根因节点。
2. 如果根因不是可直接测试的函数节点，则沿根因路径回退到最近的函数或异步函数。
3. 查找与该根因相关的测试用例。
4. 根据根因缺陷向量选择修复动作。
5. 生成只针对根因节点的最小补丁。
6. 若补丁已经存在，则不重复应用。
7. 应用补丁后进入下一轮执行验证。

⑦ 数学定义  
最小修复目标：

```text
T' = T union delta
```

其中 delta 是满足下式的最小测试变更：

```text
delta* = argmin_delta Cost(delta)
subject to S_{t+1}(v_root) < S_t(v_root)
and Q(T') >= Q(T) on preserved evidence
```

当前实现采用启发式近似：

```text
delta = one targeted patch for Root(v)
```

⑧ 伪代码  

```text
Algorithm 6: Root-Cause Guided Minimal Test Repair
Input: Graph G, test suite T
Output: Repaired test suite T'

1. Query root-cause record from G
2. Select a repairable root function related to the root-cause path
3. Find related test cases through Execute edges
4. Select repair action according to the dominant defect type
5. Generate a minimal test patch for the root function
6. If the same patch already exists:
      keep T unchanged
   Else:
      apply the patch to T
7. Return T'
```

⑨ 对应代码位置  
- `graph/repair_planner.py`
- `agents/repair_agent.py`
- `agents/evaluation_agent.py`

---

#### Algorithm 7：Evidence-Aware Test Quality Scoring

① 算法名称  
Evidence-Aware Test Quality Scoring，简称 ETQS。

② 算法目标  
用统一质量函数衡量测试套件在结构覆盖、断言有效性和变异检测方面的整体质量，并作为系统停止条件和调度反馈。

③ 输入  
- 当前证据图 G。
- 断言数量。
- 变异得分。

④ 输出  
- State Flow Coverage。
- Assertion Effectiveness。
- Mutation Score。
- State Flow Quality。

⑤ 数据结构  
- 程序节点集合 V_program。
- 程序边集合 E_program。
- 分支类节点集合 V_branch。
- 已访问节点、已访问边、断言映射和变异结果。

⑥ 核心流程  
1. 只在程序结构子图上计算覆盖质量，避免证据节点膨胀影响覆盖率。
2. 分别计算节点覆盖、边覆盖和路径覆盖。
3. 根据断言分布、断言密度和变异得分计算断言有效性。
4. 组合 SFC、AE 和 Mutation Score 得到综合质量。

⑦ 数学定义  
节点覆盖：

```text
C_node = visited program nodes / all program nodes
```

边覆盖：

```text
C_edge = visited program edges / all program edges
```

路径覆盖近似：

```text
C_path = visited branch-like nodes / all branch-like nodes
```

状态流覆盖率：

```text
SFC = 0.45 * C_node + 0.35 * C_edge + 0.20 * C_path
```

断言有效性：

```text
AE = 0.45 * assertion_distribution
   + 0.30 * assertion_density
   + 0.25 * mutation_score
```

综合质量：

```text
SFQ = 0.40 * SFC + 0.30 * AE + 0.30 * MutationScore
```

⑧ 伪代码  

```text
Algorithm 7: Evidence-Aware Test Quality Scoring
Input: Graph G, assertion count A, mutation score M
Output: SFC, AE, SFQ

1. Extract program subgraph from G
2. Compute node coverage over program nodes
3. Compute edge coverage over program edges
4. Compute path coverage over branch-like nodes
5. Combine them into SFC
6. Compute assertion distribution and assertion density
7. Combine assertion evidence and mutation score into AE
8. Combine SFC, AE, and M into SFQ
9. Return all quality scores
```

⑨ 对应代码位置  
- `metrics/coverage_metric.py`
- `metrics/assertion_metric.py`
- `metrics/mutation_metric.py`
- `metrics/state_quality.py`
- `agents/execution_agent.py`

---

### 工程支撑模块，不建议作为算法创新转换

以下模块主要是工程实现或系统胶水，不应在论文 Method 中写成核心算法：

- `executor/pytest_runner.py`：封装测试命令执行，是运行器工程实现。
- `executor/coverage_runner.py`：封装 coverage 运行和结果读取，是测量工具适配。
- `executor/mutation_runner.py`：轻量变异执行器，可作为实验基础设施说明，但不是核心方法创新；除非论文强调自定义变异算子，否则不应作为主要算法。
- `memory/shared_graph.py`：共享图文件读写，是状态持久化机制。
- `agents/analysis_agent.py`、`agents/execution_agent.py`、`agents/evaluation_agent.py`、`agents/repair_agent.py`：智能体封装层，方法创新在其调用的图算法中，不在封装类本身。
- `agents/generation_agent.py`：当前主要是启发式测试生成器，可以作为系统组件描述；如果要作为论文算法，需要进一步定义输入生成搜索空间、目标函数和去重准则。
- `main.py`：系统协调入口。其创新点是调用 GDAS，文件本身不是算法。
- `examples/`、`tests/`、`README.md`、`pyproject.toml`：实验样例、验证和项目配置，不属于方法算法。

### 当前算法完整性说明

- PTEG、Evidence Update、GDP、GDAS、RCMR 和 ETQS 已能一一映射到代码实现。
- RuntimeType 当前实现为函数接口级类型证据；若论文声明“局部变量级动态类型追踪”，需要后续增加插桩或跟踪器实现。
- RCMR 当前采用启发式最小补丁策略；论文中可以将其定义为受约束优化目标，并说明当前实现是该目标的轻量近似求解。

## 2026-07-20 报告可信度与可读性修复

- 修复质量报告容易误导的问题：页面现在明确区分“真实执行数据”“旧格式执行数据”和“未执行，仅生成”。只有存在 `metadata.last_report` 的任务才会展示为执行后的质量结论。
- 源码覆盖热力图优先使用 `coverage.py` 写入的真实 `covered_lines`，不再只依赖图节点 visited 状态做粗粒度染色；如果没有行级覆盖数据，会在页面中标明“图节点估算”。
- 报告新增行覆盖、分支覆盖、综合覆盖、SFC、AE、变异检出、SFQ 的分开展示，并补充 SFC 和 SFQ 的计算说明。
- 解释“高风险”的含义：它来自证据图缺陷传播分数，表示测试证据仍有覆盖、断言、异常、类型或变异方面的缺口，不等同于源码一定有缺陷。
- 批量任务现在除 `batch_summary.json` 外，还会生成 `batch_quality_report.html`，用于同一文件夹下多个任务的总览展示。
- 自检结果：`python -m pytest -q` 通过，26 项测试全部通过。

## 2026-07-20 函数风险全部 100% 的修复

- 根因：`GraphDefectPropagation` 把所有已访问但没有直接断言计数的程序节点都标记为断言缺口。由于断言当前主要映射到函数级 RuntimeState，分支、返回、End 等内部节点通常没有 `assert_count`，导致它们全部变成 100% 风险，并传播回函数。
- 修复：断言缺口初始化收窄到 `Function`/`Async` 节点，只有函数本身已执行但没有有效断言时才标记断言风险。
- 验证：重新执行 `examples/sample_target.py` 后，函数级风险不再全部为 100%；完整自检 `python -m pytest -q` 通过，26 项测试全部通过。

## 2026-07-20 风险显示与热力图未执行误读修复

- 修复函数风险显示从“全 100%”变成“全 0%”后仍不合理的问题：报告层新增展示用函数关注度，综合函数内部结构边证据缺口、缺失源码行、存活变异、图节点缺口和断言缺口计算，不再直接照搬单一 `defect_score`。
- 修复源码覆盖热力图和 coverage.py 指标矛盾的问题：`CoverageResult` 新增 `missing_lines`，Execution Agent 写入 `metadata.last_coverage.missing_lines`；热力图只把 coverage.py 明确报告的缺失语句标为“未执行”，其余空行、注释或非执行行标为“非执行行”。
- 修复专业证据明细误读：`Execution`、`RuntimeState`、`RuntimeType`、`TestCase`、`Assertion`、`Mutation` 等证据节点不再显示为“未覆盖”，而显示为“执行记录”“运行证据”“类型证据”等。
- 验证：重新生成 `examples/sample_target.py` 报告后，函数关注度出现 65%、53%、46%、36%、28%、22% 等区分值；热力图无真实缺失行时不再批量显示“未执行”；完整自检 `python -m pytest -q` 通过，26 项测试全部通过。

## 2026-07-20 源码证据热图升级

- 根据报告方法一致性要求，将原“源码覆盖热力图”升级为“源码证据热图”。热图不再只表达行覆盖，而是在源码行旁同时展示行覆盖、分支证据、断言证据、运行时证据、变异结果和综合关注度。
- 热图顶部新增行覆盖、分支覆盖、SFC、AE、变异检出、SFQ 指标概览，避免读者只看绿色行就误以为所有路径和测试质量都完整达标。
- 分支行会根据 PTEG 中的结构边访问情况显示“分支已全”或“分支未全 x/y”；函数入口行显示断言数量和运行证据；变异行显示“变异已杀”“存活变异”或“变异超时”。
- 保留“源码覆盖热力图”字样作为兼容说明，但主标题改为“源码证据热图”。
- 自检结果：`python -m pytest -q` 通过，26 项测试全部通过。

## 2026-07-20 源码证据热图指标筛选与变异说明

- 修正“变异超时”的显示颜色：超时现在使用独立的蓝紫色标签，不再和高关注/风险红色混在一起，避免被误读成同一种问题。
- 热图读取变异结果时，除了 `metadata.last_mutation` 的行号汇总，也会兜底读取图中的 Mutation 节点；只要图里有“存活变异”节点，即使汇总字段缺失也能显示出来。
- 新增“变异结果说明”：页面会明确说明本次是没有变异记录、没有生成可报告变异样本、所有有效变异都被杀死，还是存在存活变异。
- 新增指标筛选按钮：支持“全部指标、只看行覆盖、只看分支、只看断言、只看运行时、只看变异、只看关注度”，便于从复杂热图中单独查看某类证据。
- 验证：重新渲染 `_report_check_final/state_flow_graph.json` 后，页面显示已杀 4、存活 0、超时 1，第 43 行显示“变异超时”；`python -m pytest -q` 通过，26 项测试全部通过。
## 2026-07-20 generated_tests_llm 抽样复核

- 对 `generated_tests_llm.zip` 解压后的产物进行了独立抽样评估：164 个源码任务中实际存在 143 个生成测试文件，21 个任务没有生成可执行测试。
- 使用固定随机种子 `20260720` 从 143 个实际测试文件中抽取 64 个，逐个复跑 pytest、coverage.py 行/分支覆盖率和项目自带轻量变异测试；64 个样本 pytest 全部通过，行覆盖、分支覆盖、综合覆盖与报告全部一致，变异分数与报告全部一致。
- 抽样结果显示平均行覆盖 96.43%，平均综合覆盖 95.47%，可用变异样本平均检出率 88.45%；但 64 个样本中有 6 个没有可用变异分数，24 个有效变异体少于 3 个，说明部分“满分变异”可信度偏低。
- 评估脚本和结果保存在工作区根目录：`evaluate_generated_tests_sample.py` 与 `_eval_generated_tests_llm/sample_evaluation/`。

## 2026-07-22 长期经验库与批量总报告修复

- 修复经验只跟随生成目录的问题：`AgentExperienceMemory` 现在在项目内运行时会同时读写当前输出目录的 `agent_experience.json` 和项目根目录的长期知识库 `agent_knowledge_base.json`。删除某次生成结果目录后，后续运行仍能读取长期经验。
- 批量经验新增超时归因：记录 `timeout_tasks` 和 `mutation_timeout_tasks`，并把“使用小输入、短序列、规避长循环/深递归/外部等待”等策略写入下一轮 `batch_guidance`。
- 批量 summary 新增 `duration_seconds`，总报告页面展示开始时间、结束时间和运行时长。
- 批量 HTML 总报告将“已执行任务明细”和“未执行或未通过任务”拆成两张表，避免失败任务和成功任务混在一起；表头增加不换行样式，改善长短不一导致的排版问题。
- 验证：相关测试 `python -m pytest tests/test_batch_generate_tests.py tests/test_stateflow_system.py::test_batch_experience_guides_next_generation -q` 通过 5 项；AST 语法检查覆盖 `graph/experience_memory.py`、`batch_generate_tests.py`、`visualization/graph_visualizer.py`、`run_agent.py`、`main.py`。

## 2026-07-22 失败任务经验学习后自动重跑

- 批量执行流程新增自主失败重试：第一轮结束后，系统会先把失败、超时、低质量等批量经验写入 `agent_experience.json` 和长期知识库 `agent_knowledge_base.json`，再自动挑选失败或超时任务重跑一轮。
- 新增参数 `retry_failed_rounds`，命令行对应 `--retry-failed-rounds`，默认值为 1；传 0 可关闭自动重跑。
- 自动重跑只针对 `failed`、`generation_failed`、`execution_invalid`，以及最终报告中标记 `timed_out` 或 `failure_category=timeout` 的任务，不会重复跑已经成功的任务。
- 批量 summary 新增 `retry_policy`、`retry_history`、`retry_summary`，总报告顶部展示自动重试轮数、重试任务数和重试恢复数。
- `run_agent.py` 简短输出新增 `retry_summary` 和 `retry_history`，方便在控制台直接确认自主重试是否发生。
- 新增测试 `test_batch_retries_failed_tasks_after_writing_experience`，验证第一次失败超时后会写入经验并自动重跑，且成功后最终失败数归零。
- 验证：`python -m pytest tests/test_batch_generate_tests.py tests/test_stateflow_system.py::test_batch_experience_guides_next_generation -q` 通过 6 项；AST 语法检查通过。
## 2026-07-31 PDF 论文读取与 Java 化方案准备

- 根据用户授权，在工作区本地目录 `.codex_deps/pypdf_vendor` 安装 `pypdf`，用于读取 `An Automated Unit Test Generation Method_0706.pdf`。
- 将原 PDF 复制为工作区内的 `paper_java_method_0706.pdf`，并提取全文到 `paper_java_method_0706_extracted.txt`，便于后续基于论文原文制定 Java 单元测试生成方案。
- 已确认论文核心方法为 ValiFixTest：结构化代码分析与重写、LLM 生成 JUnit 5 测试、动态验证、最小目标修复、PreserveMaxCorrect；实验对象为 Defects4J 中 Commons-Cli、Commons-Csv、Commons-Lang、Gson。
- 本次未修改项目业务代码，未运行项目测试。
# 2026-07-31 Java/Python 统一结构化状态流融合改造

- 新增 `core/structured_context.py`，定义 Java/Python 共用的结构化上下文，统一表示类、函数、方法、依赖、分支、循环、返回、异常和执行片段。
- 新增 `analysis/` 模块，提供 Python AST 抽取、Java 结构化兜底抽取和上下文重写；重写只作用于建图和提示上下文，不修改真实源码。
- 新增 `graph/structured_graph_builder.py`，把构图入口升级为 `Source Code -> StructuredContextExtractor -> ContextRewriter -> StructuredContext -> StructuredGraphBuilder -> InitialStateGraph`。
- 新增 `core/state_flow_event.py`，将测试目标、执行证据、覆盖结果、变异结果、动态验证结果、修复范围和评估反馈写入状态流。
- 将动态验证并入 `EvaluationAgent`，新增 `validation/dynamic_validator.py` 和 `validation/validation_error.py`，覆盖规则、逻辑、可行性、覆盖和修复闭环五类验证。
- 新增 `graph/completeness_metric.py`，实现完整状态图指标 CSG，综合 SFC、DVS、AE、MS、TIC 和 RCS 判断状态图是否演化完全。
- 新增 `repair/` 模块，执行智能体修复前会根据状态流定位 assertion/line/test-method/test-class/file 五级最小修复范围，并记录收益/成本比。
- 新增 Java/Python 工具链适配文件：`executor/python_toolchain.py`、`executor/java_toolchain.py`、`executor/gradle_runner.py`、`executor/junit_runner.py`、`executor/jacoco_runner.py`、`executor/pit_runner.py`，并新增 `agents/java_test_renderer.py` 生成 JUnit 5 测试文件。
- 数据集已复制到项目内 `datasets/`：TestEval、HumanEval、Defects4J/cli、Defects4J/csv、Defects4J/lang3、Defects4J/gson，并新增 `datasets/DATASET_MANIFEST.md`。
- 新增确认文档 `项目核心功能_方法与创新点确认.md`，用于核对核心功能、方法融合、三智能体边界、完整状态图指标、实验数据集和创新点。
- 验证：新增 `tests/test_structured_fusion.py` 6 项通过；关键回归组合 16 项通过。修复了结构化构图切换后异步函数和生成器函数元数据丢失造成的测试生成回归。

## 2026-08-20 Java 真实执行链路修复

- Java 生成器新增有明确预期结果的 JUnit 断言，并处理重载方法的 `null` 类型歧义。
- JaCoCo 按目标类解析 CSV/XML，覆盖行和状态流证据回写状态图。
- PIT 增加 JUnit 5 插件，解析 XML 中的击杀、存活、源码行和测试关联，并生成 Mutation 节点。
- Java 执行报告补齐有效变异体数量、可靠性和统一的 `last_coverage`/`last_mutation` 元数据。
- Maven 使用项目内依赖仓库和 HTTPS Central 配置；状态图仅保留最近 3 份回滚快照。
- 使用 `datasets/Defects4J/cli/Util.java` 完成一次真实端到端验证：16 个测试全部通过，行覆盖率 100%，分支覆盖率 91.67%，PIT 100%，质量状态 `quality_high`。
- 全项目测试 78 项全部通过。

## 2026-08-20 LangGraph测试状态引导三智能体重构

- 新增`orchestration/`，使用LangGraph StateGraph实现状态自修复、知识检索、测试生成/修复、状态评估和结束条件之间的非线性路由。
- 使用项目输出目录内`.langgraph/checkpoints.sqlite`持久化节点级检查点；LangGraph状态只保存共享TSG引用和决策摘要。
- 新增`TestStateAgent`与`TestKnowledgeAgent`，现有生成、执行和专项修复模块归入测试生成智能体内部能力。
- 测试状态智能体新增共享TSG健康检查和自修复，可在状态缺失时重新分析源码并重新规划。
- 状态调度先确定主导缺口，再只比较匹配该缺口的生成能力，并把距离高质量阈值的差值注入缺口。
- 知识检索实现语义、结构、历史质量和时间衰减四维评分，并新增`KnowledgeRetrieved`与`KnowledgeUpdated`事件。
- Java解析由正则主路径升级为Tree-sitter Java AST；189个Java数据文件全部解析成功，识别144个类、30个接口、7个枚举和7个注解类型。
- Gradle工具链补齐JaCoCo与PIT XML解析；Maven与Gradle返回统一证据结构。
- TSQ统一为`0.30*SFC + 0.20*AE + 0.20*MS + 0.15*PV + 0.15*NRR`；只有`quality_high`计入批量成功。
- Java的`assertDoesNotThrow`和`assertNotNull`不再计为强有效断言；没有正常行为、有效断言或覆盖证据的测试不再视为可用成功。
- 修复Python参数类型被函数体线索覆盖的问题，增加双数值参数大小关系和双列表参数左右不对称分区。
- 真实验证：HumanEval `task_000.py` TSQ 98%，TestEval `TestEvatask_000.py` TSQ 94.54%，Java `cli/Util.java` TSQ 96.88%，三者均为`quality_high`。
- 回归：最终全量82项全部通过，用时1005.11秒；结构化、批量和跨数据集编排42项单独复跑也全部通过。
- 文档已重构为当前架构；论文明确区分LangGraph编排图和共享TSG领域模型，并将全量实验描述调整为待完成。

## 2026-08-21 换机数据集依赖完善

- 确认项目要求运行HumanEval、TestEval以及Java的cli、csv、lang3、gson六组数据集。
- 静态依赖审计发现TestEval的210个文件全部无条件导入`sortedcontainers`，原`pyproject.toml`和换机说明遗漏该依赖。
- 在`pyproject.toml`和`依赖.txt`中加入`sortedcontainers>=2.4,<3.0`，并重新生成`uv.lock`。
- README、数据集清单和另一台电脑运行说明同步补充TestEval依赖、Python依赖自检、Maven环境变量、首次联网下载和离线`.m2`缓存要求。
- 新增回归测试，保证后续修改不会再次漏掉TestEval依赖声明。
- 验证：结构化、批量和跨数据集编排43项全部通过，`uv.lock`已包含sortedcontainers 2.4.0。

## 2026-08-21 另一台电脑数据集路径适配

- 根据实际目录截图，另一台电脑的数据集根目录为`D:\wn\multi_evo_py\datasets`，并包含额外的`HumanEvalJava`目录。
- 新增`core/project_paths.py`统一解析项目根、数据集根和输出根，支持命令行、`TSG_DATASETS_ROOT`、兼容变量`STATEFLOW_DATASETS_ROOT`、项目内目录和固定换机路径兜底。
- 删除`batch_generate_tests.py`中旧电脑HumanEval源码的绝对路径，默认改为解析后数据集根目录下的`HumanEval`。
- `run_all_datasets.py`把`HumanEvalJava`作为正式Java数据集；其他自动发现的Java目录也按`execute_java`真实执行，不再只生成测试。
- README、换机说明、数据集清单、核心方法说明和论文同步加入实际目录、环境变量与命令示例。
- 验证：路径、数据集发现、结构化和批量回归45项全部通过。

## 2026-08-24 实时实验台与测试知识智能体问答

- 在当前项目新增`frontend/`，替换项目外旧MutAgent演示页面；移除旧Planning/Execution/Evaluation、PTEG、随机工具消息和静态报告依赖。
- 初始新增`monitoring/`独立展示服务，直接读取共享TSG、状态流、单任务摘要、批量摘要、失败报告和测试知识引导库，通过REST和SSE提供实时数据，不进入LangGraph工作流；后续已扩展网页启动、产物读取和多会话控制能力。
- `main.py`、`run_agent.py`、`batch_generate_tests.py`和`run_all_datasets.py`启动实验时统一自动打开`http://127.0.0.1:8765`；新增`--no-ui`，前端失败不影响实验。
- 测试知识智能体新增问答入口。启用LLM时沿用实验模型配置；未启用时仍可回答报告事实。对话记录独立持久化，问答不会未经验证写入长期经验；大模型密钥只保存在监控进程内存，不写入实验登记文件。
- 页面明确区分单任务实际值和批量平均值，并以批量结果中的正式实验轮次为准，避免把内部修复次数错误显示为R2/R3。
- 前端构建与3项接口测试通过；监控新增4项针对性回归测试，覆盖“最终摘要尚未生成时直接读取共享TSG”和“文件更新后下一次实时快照同步指标”。
- 使用`examples/sample_java/SimpleCalculator.java`重新真实运行：运行中页面读取到共享TSG版本6和5条事件，结束后自动更新为版本11、16条事件及`B·R1`；行/分支覆盖100%、AE 92.75%、Mutation 71.43%、TSQ 92.84%。

## 2026-08-24 交互式实验台完善

- 重构前端信息架构与配色，新增实验启动、测试生成过程和系统能力页面；实验总览、智能体内部任务、共享TSG节点关系、任务指标、失败分析、知识记录与报告文件均可点击查看真实详情。
- 共享TSG由杂乱的全量连线图改为程序状态、测试状态、运行证据和决策状态四层下钻视图，并同时识别访问、覆盖、断言和变异证据，避免把已有质量证据的测试节点错误显示为待覆盖。
- 新增网页实验启动接口，可调用原有`main.py`、`batch_generate_tests.py`和`run_all_datasets.py`；原终端入口、自动打开网页和`--no-ui`行为全部保留。
- 新增测试知识智能体多会话存储，支持新建、重命名、删除和历史读取。对话标题独立于实验，问题内容命中实验或任务后才自动绑定上下文。
- 新增系统能力目录，明确Python/pytest/coverage与Java/JUnit/JaCoCo/PIT工具链，以及文件、类、项目和全数据集四种运行范围。
- 验证：前端正式构建通过，4项前端接口测试和4项监控测试通过；网页启动接口真实调用Java样例并完成，结果为`B·R1`、测试通过、行覆盖率100%、Mutation 71.43%。

## 2026-08-24 详情与智能体协作可视化

- 删除详情抽屉中的原始JSON和结构化数组展示，统一转换为指标卡、比例条、分类分布图、标签组和记录流；D/E详情只展示D/E数量、失败任务和失败分类。
- 将智能体协作页从三列文字卡改为共享TSG中心图、三智能体环绕节点、当前交互链、实时事件流和内部能力证据图。
- 将原生实验下拉框改为“正在查看实验”面板，明确区分“切换展示已有实验”和“启动新实验”，并展示当前实验的语言、范围、状态和输出目录。
- 补齐执行、覆盖、变异、验证、评估等状态事件的中文名称；修复整数计数被误格式化为百分比的问题。
- 验证：前端正式构建和4项接口测试通过；真实浏览器检查了D/E详情、智能体协作图和实验切换面板，未发现重叠、JSON泄露或控制台错误。

## 2026-08-24 多实验融合与目录串行队列

- 默认打开“全部实验”，新增全部、正在运行、已跑完、排队中四种融合视图；目录卡同步显示Python/Java、文件/类/项目/数据集范围、完成进度和队列位置。
- 新增持久目录实验队列。一个目录命令完全结束后才启动下一个目录，排队任务可在监控服务重启后恢复；实验注册表不再只保留最近30项。
- `main.py`、`run_agent.py`、`batch_generate_tests.py`和`run_all_datasets.py`新增结束回执，正常结束和异常退出都会同步最终状态。执行完成与A-E质量等级继续分开统计。
- 聚合快照融合各实验任务、失败信息、阶段、三智能体内部能力和状态事件；共享TSG聚焦当前运行实验，已完成时聚焦最新实验。SSE每秒按内容变化推送，不增加智能体推理负担。
- 测试知识智能体对话继续在后端执行并按问题内容识别任务；浏览器不持有密钥。实测询问`sample_target.py`可返回单任务实际覆盖率、AE和变异得分，不使用平均值。
- 验证：后端5项定向检查、前端4项接口检查和生产构建通过；真实网页队列先运行Python、再自动接续Java。Python结果A（行覆盖100%、AE 93.66%、Mutation 100%），Java结果B（行覆盖100%、AE 92.75%、Mutation 71.43%）。桌面和390px移动端无重叠，浏览器控制台无错误。

## 2026-08-24 智能体动作归属与命名校正

- 修复一条状态事件同时被发送方和接收方显示为相同当前动作的问题；发送方显示实际动作，接收方显示“接收：动作名称”。
- 内部能力和智能体事件数改为只统计该智能体实际发出的事件，接收其他智能体的结果不再算作自己的工作证据。
- 多实验聚合时优先使用当前焦点实验的最新智能体动作，避免较旧实验覆盖当前卡片状态。
- 当前交互、事件流和详情统一使用测试状态智能体、测试生成智能体、测试知识智能体三个正式名称，不再使用缩写。
- 删除旧的临时Java实验登记及重复临时输出，只保留用途明确的Python/Java实时同步验证。
- 将协作事件中的内部`iteration`从`R8`一类误导性写法改为“状态迭代8”；正式实验轮次仍只在任务等级中使用`R1/R2/R3`。

## 2026-08-24 状态模型专用可视化与详情完整性检查

- 智能体协作图增加网格状态空间、双层轨道、实时信号点、扫描核心和三个差异化智能体图标，保留白底可读性及实时数据绑定。
- 后端为超大`state_flow_graph.json`提供专用压缩响应；任务报告和共享TSG均直接展示真实节点、关系、证据覆盖、四层状态空间及类型分布，不再触发2MB文本预览限制。
- 普通超大文本改为返回前200,000字符并明确标记截断，不再只显示读取失败。
- 没有共享TSG元数据的详情入口改为禁用状态；已有证据节点补充真实节点数据。
- 修复移动端启动表单被占位文字撑宽的问题，并修正移动端共享TSG扫描环定位。
- 验证：后端5项、前端4项检查和生产构建通过；真实浏览器打开并检查111个详情入口，未发现空详情、读取失败、对象泄露或控制台错误。桌面与390px移动端无横向溢出。

## 2026-08-24 分发前实验数据清理

- 再次定向验证单目录多任务、六个数据集目录编排和多目录串行队列，3项检查通过。
- 删除`_frontend_live_java`与`_frontend_live_python`验证结果，清空`.monitoring/experiments.json`中的实验登记。
- README不再展示某次验证实验的具体指标，初始实验台不附带任务或报告结果。
- 完整保留`knowledge_store`三份测试知识引导库文件和`.monitoring/chats.json`测试知识智能体对话；同时保留运行所需的`datasets`兼容样本、`examples`、测试代码及Java离线依赖缓存。

## 2026-08-24 测试知识学习闭环与前端命名统一

- 测试知识智能体在启用LLM时与模型共同回答单元测试问题；概念回答归纳进独立测试知识引导库，报告查询和无关问题不写入概念知识。
- 新概念初始标记为“正在学习”，后续真实任务检索采用并成功验证两次后转为“已掌握”；失败证据不足时保持学习或回到“待验证”，不会直接伪装为可靠经验。
- 对话概念进入Python生成提示、Java知识应用信息及后续修复/优化上下文，知识不再只出现在结果页面。
- 监控数据层补读`experiment_outcome`和任务失败报告，D/E详情展示具体失败原因、原始错误、失败测试、各轮措施、历史解决措施和下一步建议，并允许直接打开失败报告。
- 前端统一使用“共享TSG”和“测试状态中心”，删除旧式智能体编号；三个智能体卡片按真实事件展示具体评估、规划、生成、验证、修复、优化、检索和学习任务。

## 2026-08-24 旧实验问题吸收与跨层一致性修复

- 直接读取旧归档而未解压，将445条原始经验去重归纳为16条有来源证据的历史模式，并合并到独立测试知识引导库。
- 修复续跑时重复执行已完成R1、批量摘要晚于单任务摘要、R2/R3标记丢失，以及进程中断后轮次统计无法恢复的问题。
- 收紧A-E判定：A/B/C必须存在真实执行报告；无报告的生成失败为E，已有报告但编译、导入、收集或执行失败为D。
- R2/R3增加上一轮最佳产物备份与恢复。C继续定向优化已有测试，D按失败知识重新生成，E补齐证据后重启，候选无增益时不覆盖旧结果。
- Java执行改为临时落盘并恢复项目原测试；评估前清理旧Maven/Gradle/JaCoCo/PIT产物，Gradle筛选目标测试，PIT采用目标类XML分数。
- 评估缓存增加运行环境与本地依赖闭包指纹，避免换机、源码依赖或工具链变化后错误复用证据。
- 前端任务范围、实时轮次、D/E原因与措施、历史知识支持次数均改由后端真实快照提供；JSON和HTML产物改为可读数据视图和报告视图。
- 八个页面逐一打开检查，全部入口可用且浏览器无脚本错误；未运行数据集或耗时测试套件。
- 清空实验登记与历史对话，删除通用Java工作区残留示例和编译清单；按当前八页导航重新截取论文中的五张系统界面图。
- 测试知识引导库新增知识形成闭环图和四类状态含义；对话消息会显示本次概念是否已归纳以及当前学习状态。
- 验证：监控与知识链7项定向测试全部通过，前端TypeScript检查与生产构建通过；未重复运行样例或全量实验。

## 2026-08-24 R2实时轮次与分级策略修复

- 修复批量R2只在整轮结束后更新`batch_summary.json`、单任务摘要又固定写R1，导致终端已进入R2但页面仍显示`C·R1`的问题。
- 新增逐任务`experiment_round_progress.json`；任务开始R2即显示R2，结束后同步最终等级、策略和是否提升，不再等待同轮全部任务完成。
- C级R2改为保留现有通过测试，按最大质量缺口检索知识并选择覆盖、断言、边界或变异增强；启用LLM时优化现有代码，无质量增益自动恢复R1最佳文件。
- D级R2/R3改为按失败类别检索历史原因与解决措施，并把检索结果实际注入重新生成上下文；E级补齐证据后重启。
- 批量报告增加轮次策略统计，前端任务列表、任务头部和轮次详情同步展示本轮采用的处理方式。
- 验证：实时R2显示、C定向优化、D失败后重生成和中断续跑四项针对性检查通过；未运行全量数据集。

## 2026-08-26 真实LLM链路、三次内部治理与动态可视化

- Java生成接入与Python一致的LLM、共享TSG测试意图和测试知识上下文；测试生成后的真实编译、JUnit、JaCoCo与PIT证据会回写测试状态中心。修复Java Method/Constructor节点未参与状态健康、规划和缺口查询的问题。
- 测试生成智能体内部统一使用状态定位修复策略与状态目标优化策略，每个正式轮次最多3次并支持提前停止；R1/R2/R3仍是唯一正式实验轮次。
- A-E判定改为先确认可行性，再依据适用的覆盖、断言与可靠变异证据评价质量；缺失或不适用的Mutation不再构成虚假高质量结论。
- 长期知识改为主文件加进程分片的多JSON合并读取，避免并行写覆盖；知识状态统一为已采纳、正在学习、待复核和应避免。知识页面、对话学习和系统助手按所选实验绑定知识路径。
- 实验总览合并动态执行过程和实时控制台，使用全任务综合质量替代批量平均值；协作台改为滚动交互控制台和动态共享TSG核心；测试生成页新增逐任务耗时折线图。
- 启动实验页新增本次模型、服务地址和密钥配置，并真实传入现有后端LLM入口。前端生产构建和关键Python文件语法检查通过，未运行耗时实验或回归套件。
- 实验总览删除综合质量区中的当前任务重复项，将“当前任务实际指标”简化为“当前任务指标”，右侧仅保留全任务综合质量、有效结果数和A-E构成。
- 共享TSG视觉重构为透明轨道容器中的三层六边形能量核心，拼图矩阵扩展为16个分波闪动单元，粒子扩展为18条不同半径、方向、速度、尺寸和颜色的轨道；前端构建与浏览器实测通过且无控制台错误。

## 2026-08-26 实时状态监控与论文方法图

- 多智能体协作台新增实时状态监控，直接消费现有运行快照，动态展示当前质量指标、最近任务质量信号、三个智能体状态和系统运行信号；所有模块均可点击查看对应真实数据。
- 监控没有另起采集进程，也不调用LLM、JaCoCo或PIT，只复用后端已经产生的状态和评价结果，因此不干扰实验执行。
- 新绘制三张本地化方法图：方法总体框架、测试状态引导机制、三智能体协作结构；均提供可编辑SVG和高分辨率PNG，内容严格对应共享TSG、R1/R2/R3和三个正式智能体职责。
- 论文已插入三张方法图，并把系统界面图统一调整为图4至图8；新增协作与实时监控截图，修正当前七个主页面及“系统在线”入口的文字说明。
- 前端生产产物已更新；浏览器确认实时监控唯一渲染、各项真实数据可打开、页面控制台无警告或错误，论文8个图片链接全部有效。未运行数据集或耗时测试套件。

## 2026-08-26 全局真实执行复核与关键修复

- 用真实Python单文件、Python包相对导入和Java Maven任务执行生成、验证、覆盖、变异、状态评价与报告链路；Java最终获得14个真实PIT变异体，Python包相对导入任务完整通过。
- 修复通用Java工作区PIT目标包硬编码及正式评估前误删编译产物的问题；PIT现在由当前任务动态传入目标类和测试类。
- Python生成、修复、规范化、运行时观察和变异执行统一使用包感知加载器。
- A级改为全部适用核心指标均达到高标准且Mutation有效；不可靠Mutation不再进入TSQ。终止质量已为A/B时跳过无必要的LLM评价，C/D/E仍保留知识与LLM引导。
- 数据集完成标记加入源码树签名，避免源码变化后错误跳过；`run_agent.py`目录任务默认轮次统一为R1/R2/R3；删除未调用的Python/Java空验证器。
- 统一运行说明、论文、系统目录、项目记忆与批量报告中的三轮和内部最多3次规则。核心Python语法编译与前端生产构建通过。

## 2026-08-31 参考既有论文的方法图科学化重构

- 逐页检查用户提供的《开题终稿1130-王娜》和《论文2》，重点归纳多智能体协作图的控制/执行分层、结构化工件、监督反馈与经验回流，以及ValiFixTest图的阶段分区、状态变化、决策节点和闭环表达方式；仅借鉴组织原理，未复制原图内容。
- 图1改为“任务与环境建模—单任务轮内协作—数据集级正式轮次—持久化证据层”四层结构，补入统一任务包、三智能体内部模块、语义筛查/screening/full分层验证、R1/R2/R3路由和可续跑证据。
- 图2改为“结构建图S₀—证据融合Sₜ—状态引导Sₜ₊₁”三阶段状态演化图，明确五类证据、GraphUpdater、缺口向量、8步0.85衰减传播、能力选择器和真实质量门之间的因果关系。
- 图3改为“控制平面—共享状态平面—知识平面”协作结构，明确任务目标、测试工件、决策工件、证据工件及task_signature/state_version/evidence_ref约束，并保留三个智能体的修改权限边界和知识生命周期。
- 三图均保留可编辑SVG并重新生成1600×960真PNG；已逐图检查字体、箭头、分区、信息密度和缩放可读性。论文第3章三处图前说明同步更新，与新图中的层次和数据流一致。

## 2026-09-01 研究问题、贡献、公式与算法全局重构

- 保留“测试目标与执行结果统一为可演化共享TSG”的问题1；问题2调整为多智能体增强大语言模型的理解、统筹、生成验证和经验记忆能力，并面向跨语言、跨层级任务协调覆盖、断言、变异、类型和异常目标；问题3调整为方法指标、实验效果与正式轮次贡献评价。
- 三项贡献与研究问题重新一一对应：共享TSG状态引导、多智能体断言—变异多目标自主协作、七数据集711项任务的多维实验效果。状态定位与候选修复降为保证可执行性的基础环节，不再作为贡献或独立算法强调。
- 第5章RQ同步重写：RQ.1评价跨语言跨层级总体效果，RQ.2评价断言与变异多目标增强，RQ.3评价TSQ指标体系；结论同步报告R2有效率增量、R3的BC/AE/MS/TSQ增量及TSQ消融边界。
- 全文18个公式删除HTML布局标签，改为可直接阅读的Unicode公式与顺序编号；补回根因节点选择和状态增量更新公式，消除正文中冒号后公式缺失的问题。
- 算法由表格改为代码行式伪代码，并收敛为3个：数据集三轮与轮内状态引导、断言—变异多目标增强、状态感知记忆检索。方法图1和图3同步突出断言增强、变异增强与多目标增强。

## 2026-09-01 研究问题去重与三张方法图差异化重绘

- 问题2从“状态驱动多目标能力”改为独立的多智能体协作问题，重点研究三个顶层智能体之间的职责分离、结构化任务交接、条件路由、冲突避免和经验回流；问题1继续负责共享TSG建模与状态引导，二者不再重复。
- 第二项贡献同步改为共享TSG多智能体协作协议：任务目标、测试工件、决策工件和证据工件均绑定task_signature、state_version与evidence_ref，测试代码只由测试生成智能体修改。
- 图1保留方法总体框架；图2完全重绘为断言—变异证据关系建模与联合增强机制，使用Observe/Kill正关系、Weak/Survive/Unobserved负关系、R⁺/R⁻矩阵、缺口传播和增强前后关系变化说明AE与MS如何提升。
- 图3完全重绘为多智能体任务交接与记忆回流流程，按感知分解、记忆检索、生成执行、评估回流四个阶段展示Q_t、K_t、(T_{t+1},O_t)和评估工件的编号交接轨迹。
- 新图借鉴用户示例中的分区、正负关系和编号轨迹表达，但未引入代码中不存在的自演化智能体拓扑、教师—学生蒸馏或视觉特征网络。两张新图均保留可编辑SVG和1600×960 PNG，并已完成XML、格式、尺寸与视觉检查。
