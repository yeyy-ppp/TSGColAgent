# 另一台电脑运行说明

## 2026-09-13：论文指标记录

新实验自动记录LC、BC、AE、MS、PassRate、AvgExec、Time和TIR，无需新增启动参数。AvgExec按工具级测试执行会话计数，联合测试与覆盖计一次，PIT一次评估计一次；TIR按执行前登记的评价目标及实际保留结果统计。前端任务详情和实验统计区展示这些指标，同时显示TIR分子/分母及AvgExec记录完整性。AE、MS沿用原计算，论文定义已作对应修订。

任务目录新增`paper_metrics/events.jsonl`与`paper_metrics/summary.json`；任务摘要、批量摘要、总摘要中的数据集摘要和终端汇总同步保存新增统计。记录跨R1/R2/R3累计，候选回退不删除已消耗成本。旧结果没有采集记录时仍显示“—”；在旧目录续跑也不能补回旧阶段成本。需要完整新指标时，请为新实验指定一个空输出目录，不删除原始结果。

可在项目目录运行只读导出命令：

```powershell
python scripts/export_paper_metrics.py "D:\实验结果\新实验目录"
```

论文逻辑、实验来源及待确认的方法差异见`论文实验数据/论文逻辑与实现审查_20260913.md`。本次增加统计，不改变内部调度、三轮策略、候选选择及知识读取边界，也没有新增消融实验执行器。

> 2026-09-04验收更新：当前行为与已知边界以《功能验收报告_2026-09-04.md》及“记忆.md”顶部规则为准。对话优先模型+知识，模型不可用时知识自主回答，未知问题持久排队并在后台服务运行时自动补学；不是只在实验启用--llm时才允许对话。HumanEvalJava自动识别与显式java模式一致；本地163项不足164项参考规模，不冒充完整数据集。节点检查点有保存，但现有续跑主要为任务/轮次级。历史实验日志不改写。

另一台电脑的项目与数据集位置按以下结构使用：

```text
D:\wn\multi_evo_py
  datasets
    Defects4J\cli
    Defects4J\csv
    Defects4J\gson
    Defects4J\lang3
    HumanEval
    HumanEvalJava
    TestEval
```

建议先进入项目目录：

```powershell
cd D:\wn\multi_evo_py
$env:TSG_DATASETS_ROOT="D:\wn\multi_evo_py\datasets"
```

## 环境

- Python 3.11或更高；
- Java测试需要JDK 11或更高、Maven 3.8+；
- Gradle项目优先使用项目自带wrapper；
- 沿用实验电脑上已经部署并配置正确的OpenAI兼容本地模型服务。

安装项目与锁定依赖：

```powershell
python -m pip install -e .
```

项目包中应保留`frontend/src`、`frontend/dist`和`frontend/package.json`。正常运行直接使用已构建的`frontend/dist`，因此实验电脑不要求安装Node.js；只有修改前端源码后重新构建才需要Node.js和`npm install`。不要打包`frontend/node_modules`。

当前分发包的`.monitoring/experiments.json`为空，不附带本机验证实验及报告。首次打开实验台会显示等待状态；从网页或终端启动新实验后，目录、任务、状态图和质量结果会实时出现。`knowledge_store/`与`.monitoring/chats.json`属于测试知识智能体的长期知识和对话资产，迁移时必须保留。

依赖包括pytest、coverage、sortedcontainers、LangGraph、LangGraph SQLite检查点、Tree-sitter和Tree-sitter Java。TestEval的210个源码文件全部会导入sortedcontainers，该依赖已由`pip install -e .`自动安装。

安装后先确认Python数据集依赖：

```powershell
python -c "import pytest, coverage, sortedcontainers, langgraph, tree_sitter, tree_sitter_java; print('Python dependencies OK')"
```

如果另一台电脑不能联网，应提前下载项目依赖wheel并离线安装；仅复制源码但不复制Python依赖缓存，无法在干净环境完成安装。

## 快速自检

```powershell
python -B -m pytest tests\test_structured_fusion.py tests\test_batch_generate_tests.py tests\test_run_all_datasets.py -q
```

当前这三组应为45项通过。

## 单文件运行

通过`main.py`、`run_agent.py`、`batch_generate_tests.py`或`run_all_datasets.py`启动正式实验时都会自动打开实时实验台，固定地址为`http://127.0.0.1:8765`，默认显示“全部实验”。批量实验只打开一个页面，不会按任务重复弹窗。顶部可筛选全部、正在运行、已跑完、排队中或单个实验。也可以先打开该地址，在“启动实验”页面中选择单文件/类、项目/目录或数据集根目录；网页会调用同一套终端入口，原命令仍可独立使用。服务器环境或不需要页面时追加`--no-ui`；前端故障不会影响测试生成和评估。

网页启动时，源路径和输出路径必须填写为实验电脑上的绝对路径。多个网页实验按目录串行排队：一个目录中的全部项目运行和报告收尾结束后，下一目录才会自动启动。大模型地址、模型名和密钥继续沿用原后端环境变量或项目配置，不需要改动；密钥不在浏览器中填写、保存或传输。

Python：

```powershell
python -B main.py --source datasets\HumanEval\task_000.py --out runs\humaneval_000 --max-iterations 3
```

Java：

```powershell
python -B main.py --source datasets\Defects4J\cli\Util.java --out runs\java_util --max-iterations 3
```

启用本地模型：

```powershell
$env:STATEFLOW_LLM_ENABLED="1"
$env:STATEFLOW_LLM_BASE_URL="http://127.0.0.1:11434"
$env:STATEFLOW_LLM_MODEL="deepseek-r1:14b"
python -B main.py --source examples\sample_target.py --out runs\sample --llm
```

`STATEFLOW_*`环境变量名称为兼容接口，不代表系统仍采用旧状态流架构。

## 数据集运行

先抽样：

```powershell
python -B run_all_datasets.py --datasets-root "D:\wn\multi_evo_py\datasets" --limit-per-dataset 1
```

完整运行：

```powershell
python -B run_all_datasets.py --datasets-root "D:\wn\multi_evo_py\datasets" --out-root "D:\wn\multi_evo_py\runs" --llm
```

系统会处理TestEval、HumanEval、HumanEvalJava、cli、csv、lang3和gson。`HumanEvalJava`和其他自动发现的Java目录会真实执行JUnit、JaCoCo和PIT。HumanEvalJava每个类使用自己结果目录内的`.java_workspace`，默认2个隔离工作槽，只共享项目内Maven依赖缓存；其他Java目录和项目继续使用原串行工作区。Gradle工程会解析`build/reports`中的JaCoCo和PIT XML。

路径解析优先级为：显式`--datasets-root`、`TSG_DATASETS_ROOT`、兼容变量`STATEFLOW_DATASETS_ROOT`、项目内`datasets`、固定兜底`D:\wn\multi_evo_py\datasets`。因此从其他工作目录启动时也不会依赖当前目录碰运气。

## 续跑与检查点

每个任务的`.langgraph/checkpoints.sqlite`保存LangGraph节点级检查点；`state_flow_graph.json`保存共享TSG状态数据；`stateflow_summary.json`保存任务结果。A/B/C均为有效完整结果；R1中的D/E会进入R2恢复。D/E不再等到全部实验结束才补报告：R1或R2出现D/E时立即生成对应的`task_failure_report_R1.json`或`task_failure_report_R2.json`及HTML，摘要同步保存失败阶段、具体原因、原始错误、已采取措施、建议措施及知识记录编号。R3仅优化C，不会把D/E继续带入第三轮。

批量实验固定最多三轮：R1处理全部任务并形成基线；R2只恢复D/E，仍为D/E的任务在R2后直接形成最终失败报告；R3统一优化全部C，包括R1原本的C和R2恢复得到的C。三轮都会先读取“测试知识引导库”，再由测试状态智能体规划、测试生成智能体生成执行并验证、测试状态智能体评估和决定后续。系统不会开启R4。批量完成后生成`dataset_completion.json`；只有数据集目录内全部应处理任务均为A/B/C，下一次启动才会在源码遍历前直接跳过整个目录。

Python任务默认4并行；HumanEvalJava类级任务默认2并行，可用`--humanevaljava-workers 1`退回严格串行，或在资源充足时设置2至4。每个HumanEvalJava正式轮次开始时冻结知识视图，任务新经验在轮次结束后统一合并，避免并行顺序改变生成结果。R1、R2、R3的测试生成智能体内部仍允许最多3次状态定位修复或状态目标优化；JUnit、JaCoCo、PIT、A-E分级和三轮规则均未改变。Defects4J、普通Java目录和Java项目继续保持串行。

默认完整命令已经启用同一Python数据集内部4任务并行，并把长期知识固定在代码目录之外：

```powershell
python -B run_all_datasets.py --datasets-root "D:\wn\multi_evo_py\datasets" --out-root "D:\wn\multi_evo_py\runs" --llm --knowledge-store "D:\wn\evo\test_knowledge_guidance.json"
```

该路径指另一台正式实验电脑。首次运行时，如果外部文件不存在，系统会把代码包内现有测试知识初始化到该位置；如果已经存在，系统只读取和追加，绝不会用新代码包中的默认知识覆盖它。因此替换代码、删除实验结果或重新解压项目都不会重置长期经验。需要迁移旧实验知识时，可在打包电脑上提前整理`D:\wn\evo`并整体复制到目标电脑。Python并行使用`--python-workers 2`或`--python-workers 4`；HumanEvalJava使用`--humanevaljava-workers 1`至`4`，默认`2`。

4个Python任务与2个Ollama通道并不冲突：4个任务可以同时处于源码分析、生成、pytest、覆盖率、变异或等待阶段，但同时进入模型推理的最多2个；其余模型请求短暂等待通道，不会阻止已经进入非模型阶段的任务继续工作。

续跑不会重复执行已经完成且失败证据完整的R1任务。系统从单任务摘要恢复实际等级和轮次，携带原始错误、既往措施和知识记录直接进入R2；只有旧摘要缺少失败原因、错误和诊断时才重跑R1补证据。R2/R3每完成一个任务都会立即更新总报告。轮次开始前的最佳结果备份可在无增益、损坏或中断时恢复。评估缓存还包含当前操作系统、Python/Java工具版本、本地Python导入依赖以及Java主源码和测试源码指纹，因此复制到另一台电脑或依赖变化后会自动失效，不会把旧电脑证据当作新实验结果。

强制重跑：

```powershell
python -B main.py --source examples\sample_target.py --out runs\sample --rerun-existing
```

批量强制重跑：

```powershell
python -B run_all_datasets.py --rerun-existing
```

## 输出内容

- pytest或JUnit测试文件；
- `stateflow_summary.json`：TSG任务摘要；
- `state_flow_graph.json`：共享TSG状态数据的兼容文件名；
- `state_flow_graph.html`与SVG；
- `source_coverage.html`；
- `.langgraph/checkpoints.sqlite`；
- 批量目录中的`batch_summary.json`、`batch_quality_report.html`和`dataset_completion.json`；
- D/E任务各自的JSON/HTML失败报告及总报告中的失败原因和既往解决措施；
- `knowledge_store/test_knowledge_guidance.json`长期经验；
- `knowledge_store/test_knowledge_guidance_library.html`“测试知识引导库”可视化。
- `frontend/`实时实验台源码和构建结果；
- `.monitoring/experiments.json`保存已登记实验目录、执行状态和安全的启动参数，用于全部/运行/完成/排队视图及服务重启后的排队恢复；不保存大模型密钥。

实时实验台不会修改智能体结果文件。运行中直接读取测试状态中心持续更新的共享TSG和状态事件，结束后读取任务摘要、覆盖率、AE、变异结果和失败报告。终端启动的四个入口也会回写最终的“已完成”或“失败”状态，避免完成任务长期显示为运行中。

并行运行时，实验总览和测试生成过程会显示任务工作槽、当前活动任务、等待任务和模型通道。任务与报告页同时列出各个运行任务；多智能体协作台合并显示活动任务的实时事件，并在每条事件上标注任务名。测试状态中心仍聚焦最近更新任务的一份共享TSG，不会错误地把多个任务的状态图拼接在一起。运行中的任务不提前计为E级。

## 成功判定

A高质量、B良好、C可用均计入有效完整结果；D无效和E未完成不计入。默认A级门槛为行覆盖90%、分支覆盖80%、SFC 75%、AE 75%、可靠Mutation 75%、TSQ 82%，并要求测试全部通过、有效断言、正常行为、PV=1和NRR=1。B级基础门槛为行覆盖80%、分支覆盖70%、SFC 65%、AE 75%、可靠Mutation 60%、TSQ 70%，允许一个非关键指标偏低；C要求测试真实可运行且具备正常行为和有效断言。

工具缺失、依赖失败、低质量、停滞和预算不足会分别报告。D/E会展示具体失败原因、此前尝试的解决措施、等级轨迹及失败报告链接。

迁移到另一台电脑时必须保留`knowledge_store`，否则长期学习经验会丢失。也可以用`TSG_KNOWLEDGE_STORE`指定独立知识文件位置，使实验输出目录被删除后经验仍然保留。

当前知识库已经吸收旧实验中可复用的失败和优化模式，原始重复记录经过归纳后仅保留有证据的模式。打包时可以清空`.monitoring/experiments.json`、`.monitoring/chats.json`和实验输出目录，但不要删除`knowledge_store`。

## Java依赖异常

Maven使用`java_workspace/.m2`项目内仓库和HTTPS Central配置，不写系统Maven仓库。第一次在线运行会从Maven Central下载JUnit、JaCoCo、PIT和数据集所需依赖。

先检查：

```powershell
java -version
mvn -version
```

如果`mvn`不在PATH，可以设置其中一种方式：

```powershell
$env:MAVEN_HOME="D:\tools\apache-maven-3.9.1"
$env:PATH="$env:MAVEN_HOME\bin;$env:PATH"
```

或者直接指定命令：

```powershell
$env:MAVEN_CMD="D:\tools\apache-maven-3.9.1\bin\mvn.cmd"
```

代码还兼容`MVN_CMD`、`M2_HOME`、项目`mvnw.cmd`和项目内`apache-maven*/bin/mvn.cmd`。

如果另一台电脑不能访问Maven Central，打包时必须保留当前`java_workspace/.m2/repository`。当前缓存约43MB；运行时会自动重写`.m2/settings.xml`中的本机绝对路径。不要只复制`pom.xml`而漏掉离线依赖缓存。

若显示`java_execution_unavailable`，检查JDK、Maven与`java_workspace/pom.xml`是否存在。若显示依赖解析失败，检查网络或`java_workspace/.m2/repository`是否完整。
