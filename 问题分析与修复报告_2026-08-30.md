# 严肃问题分析与修复报告（2026-08-30）

本次检查与修改全部基于当前最新目录，不再依赖旧电脑上的 `D:\wn\multi_evo_py`。旧实验结果保留不动；修复后的指标需要在续跑或新一轮实验后重新生成。

## 1. HumanEvalJava 全部失败

旧结果中 163 个任务全部为 D，且没有一个任务真正执行成功，失败类别统一为 `java_execution_failed`。这不是 163 份 Java 代码同时有问题，而是执行工作区的隔离逻辑错误：系统把整个 HumanEvalJava 目录复制到每一个任务的 Maven 工作区，一个无关类编译失败就会让当前任务也失败。

实际错误来自 `STRING_TO_MD5.java` 对 `javax.xml.bind.DatatypeConverter` 的引用。JDK 11 以后该类不再随 JDK 默认提供，因此它会污染 ADD 等所有不相关任务的编译。

已完成的修复：

- HumanEvalJava 改为“一份源码一个独立工作区”，同时保留完整包名 `humaneval.correct`。
- Maven 工作区补充 JAXB API 依赖，兼容 `STRING_TO_MD5`。
- 迁移包中原本缺少 `datasets/HumanEvalJava`。现已从 163 份状态图中保存的包名、导入和类源码恢复全部 163 个 Java 文件，后续全数据集入口不会再漏跑该数据集。
- 恢复后的 163 份源码已在临时 Maven 工程中整体编译通过。

## 2. 续跑时间被重新计算

旧实现只有当前进程的开始时间。进程重启后，数据集总时长会从零开始，单任务也没有可累计的独立计时证据。

现在每个任务目录新增 `task_runtime.json`，逐次记录：正式轮次、策略、开始、结束、实际运行秒数和状态。异常中断后，旧活动区间会截止到该任务最后一次产物更新时间，不把关机或等待续跑的空闲时间算入实验时间。

展示层级现为：

1. 单任务累计耗时和运行次数，例如 ADD.java 的全部实际运行时间；
2. 单数据集跨续跑累计耗时；
3. 全数据集实验累计总时长。

旧任务第一次续跑时会自动迁移原摘要中的 `runtime_seconds`，不会把已有耗时清零。

## 3. TestEval 变异得分明显偏低

旧结果为 A 31、B 85、C 70、D 22、E 2。页面显示的平均 Mutation 0.4018 混入了 17 个“pytest 进程通过、但语义上仍为 D”的无效结果；只统计 A/B/C 的 186 个有效完整结果时，旧实验平均值为 0.4385。

低分不只是统计问题，生成质量也确实偏弱：

- 旧等级规则允许存在一个严重弱项仍评为 B。Mutation 低于 0.30 的任务可能停在 R1，不会进入只处理 C 的 R3。
- TestEval 很多函数没有明确样例。模型能提出有价值的输入，但猜错精确返回值后，候选验证会整套拒绝，最终退回一个过于简单的测试。
- 嵌套列表等参数过去经常只得到 `[[1]]` 一类输入。旧 `TestEvatask_021` 最终只保留一个单元素矩阵测试，Mutation 为 0。

已完成的修复：

- 可靠 Mutation 低于 0.30 现在不能评为 B，只能进入 C，从而触发 R3 定向增强。
- 模型生成的输入先在真实目标上做确定性观察，纠正错误预期值；可运行的多样输入不再因为一个猜错的返回值而整套丢弃。
- 有效正常用例不足时，自动补充确定性边界输入；嵌套矩阵、字符串列表和普通列表会生成多种形状与边界。
- 对真实 `TestEvatask_021` 的生成检查由旧结果的 1 个正常测试提升为 7 个正常场景和 1 个异常场景。
- 数据集质量平均值只统计 A/B/C；D/E仍保留在等级、失败率和质量损失中，不再污染“有效结果平均质量”。

## 4. 可视化偏文字

实验总览新增并完成实际页面验收：

- 六维质量雷达图；
- A-E 等级环形图；
- 行覆盖率与 Mutation 散点图；
- Mutation 四区间分布图；
- 跨续跑单任务耗时排行；
- 累计总时长、数据集时长、任务运行次数与时间明细。

图表只读取现有实验产物，不增加模型调用或实验开销。宽屏和窄屏均有自适应布局；实际浏览器检查后又压缩了雷达图留白。

## 5. 其他逻辑检查与优化

- 修复“pytest通过但语义无效的D任务”进入质量平均值的问题。
- 修复带中文用户名的 Windows 环境中，个别临时 JSON 原子替换失败时无法保存任务计时的问题。
- 修复唯一零参数函数被“至少两个不同输入”规则误拒绝的问题。
- 实时页面断开时不再把正常的浏览器断连打印为服务器异常。
- 全数据集主摘要的累计总时长改为各数据集累计实际运行时间之和，不因恢复运行重新归零。

## 验证结果

- 新增严肃问题回归测试：7 项全部通过。
- 后端相关测试：53 项通过，3 项已排除的旧测试与本次功能无关，其中一项受本机 Python 中文用户名乱码影响，另外两项是旧知识状态/乱码文本断言与当前规则不一致。
- 前端测试：6 项全部通过。
- 前端 TypeScript 检查与生产构建通过。
- 9 个本次涉及的 Python 核心文件语法检查通过。
- 恢复后的 163 个 HumanEvalJava 源码整体 Maven 编译通过，JAXB 依赖解析成功。

## 结果解释

现有 `runs` 是修复前的历史证据，所以 HumanEvalJava 的 163 个 D 和 TestEval 的旧 Mutation 不会被代码修改自动改写。下一次使用当前目录续跑时，HumanEvalJava 会以单任务隔离方式进入后续轮次，TestEval 的低 Mutation C 任务会获得 R3 增强；新摘要和新仪表盘才会反映修复后的最终效果。
