
# 项目记忆

## 2026-09-17 用户可见名称与结果报告指标

- 用户明确更正前端系统方法名称为“ValiFixTest & TSGColAgent”。本次只调整用户可见名称：前端品牌、实验启动主按钮、浏览器标题、系统目录、测试知识智能体自述、指标导出标识及新生成报告标题；论文正文未随本次请求修改。
- 后端SFC、TSQ/SFQ计算、阈值、分级、调度和历史JSON字段保持不变。前端与新生成的state_flow_graph.html、state_flow_graph.svg、source_coverage.html、batch_quality_report.html不再展示SFC和TSQ。
- 实验总览新增“论文评估指标”区域，实际挂载此前已存在但未使用的汇总组件，显示LC、BC、AE、MS、PassRate、AvgExec、Time和TIR，以及TIR改善/可比较/缺证数和AvgExec计数完整性。任务详情继续显示单任务指标。
- 单任务报告从paper_metrics/summary.json读取TIR、AvgExec、Time和PassRate；源码热图展示LC、BC、AE、MS、TIR、AvgExec；批量报告从batch_summary.paper_metrics和任务paper_metrics读取汇总及分项指标。旧实验缺少paper_metrics时显示“未执行”，不伪造0。
- 前端生产构建通过；三项报告定向测试通过，覆盖单任务图报告、源码热图和批量报告，断言用户可见HTML不含SFC/TSQ且能显示TIR/AvgExec。历史已生成HTML不会仅因模板变化自动改写，重新生成报告或运行新实验后使用新模板。

## 2026-09-16 Word交叉引用修复

- 用户反馈公式/图/文献不能点击跳转。检查发现原59处参考文献超链接及目标确实存在，原生Word导航测试ref_4/33/34/35均可到达，普通编辑模式通常需要Ctrl+单击；公式/图表/算法及章节当时未完整建立引用。
- 在同名0904.docx增加100个精确编号书签，并统一建立105处Word REF带超链接域：文献59、公式15、图5、表10、算法6、章节10。连续[4][33][34][35]独立设域，公式书签覆盖编号，图/表/算法书签覆盖各自数字。
- 使用本机Word更新全部引用域，验证每个REF结果等于书签文字，并逐个调用Word内部导航验证目标起点，105处全部通过。式1、图1、表1、算法1、文献4/33/34/35均专门记录测试结果。
- 可见正文逐字保持，源MD未改；96处OMML、11编号公式、6实验表、5图、44文献检查通过，仍33页。重新渲染并检查变化页；正文和数据未变。最终文件直接覆盖原同名Word，未新建论文版本。
- 临时修复脚本fix_crossrefs.py及测试verify_crossrefs.ps1在C:/Temp/paper-word-84250c0262054e4693ca376c46ca5a10。以后从MD重新生成Word后须重新建立此交叉引用层，不能只跑旧build_word.py而丢失公式图表等跳转。Word编辑模式告知用户Ctrl+单击编号；没有修改用户全局Word设置。

## 2026-09-16 0904论文Word导出

- 用户明确要求输出Word且公式、算法、字符上下标直接可读。已生成与唯一MD同目录同名的“基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.docx”，未改正文、数据或MD。
- 正文96处数学表达式转为Word原生可编辑OMML，其中11个编号公式；算法中的变量及T_0等数字下标转为Word上下标文字，保留3段完整算法。6张实验表为原生Word表格，5幅方法图和44条参考文献保留，引用设内部书签链接。
- 使用临时目录C:/Temp/paper-word-84250c0262054e4693ca376c46ca5a10下脚本转换及QA，不在论文目录新增辅助版本。默认渲染器因无LibreOffice失败后，用本机Microsoft Word后台导出预览PDF，再调用文档技能renderer.rasterize生成PNG检查。最终33页，宽表2/3/5采用横向页，正文纵向，重复表头。
- 已逐页检查数学、上下标、算法和表格显示，修正列宽、单元格格式跳过、图题重复、代码数字下标及公式边框。内容验证：96 OMML/11编号公式/6数据表/5图/44书签，全部表格文字与MD对比通过；无TABLEMARK、ALGOMARK、EQNUMBER、LaTeX美元符、反斜杠命令、T_0等占位/原始标记。输出约2.25MB。
- 源MD SHA256保持965d927e5468ce06d25c7065eee42bdf99bfafc5a25ecd99629652e4a8793aa2。此次是格式导出，不是新一轮论文修订或实验复算；以后若改MD需重新同步Word。

## 2026-09-16 全文学术语言审查（最新）

- 按用户12项语言意见审查唯一0904论文，不修改方法结构、算法步骤、参数或实验数据。摘要与引言拆分长句，状态变量定义分为结构/要求属性与映射/轮次关系，知识参数分段表达，实验配置和对照控制条件拆句。
- “消费任务包/职责流程/准备知识/目标定向/落实到测试代码/执行完成/承担保留结果”等改为接收任务及上下文、协作过程、知识检索、目标导向生成、生成相应测试代码、执行通过情况、保留当前测试结果。分支与返回行为示例按用户完整文字替换。
- 2.5重写为研究方向梳理—信息协同不足—共享状态方法承接，5.3按用户给定应用边界/实验解释边界收束。结论完整并列测试状态智能体、测试生成智能体和测试知识智能体，正文统一无共享测试状态的多智能体对照流程。
- 算法后说明压缩为阶段作用与必要边界，保留Unknown中断保留、每版本一次确认、知识适用条件、目标类型及变化/测试结果/执行次数耗时、可比较冲突验证等技术条件。算法代码块逐字保持原样。
- 清理二者均表明/此类混合研究说明/它们共同关注等机械总结，改为具体文献技术差异。实验分析段不再逐段回答RQ，仅4.5本章小结集中对应RQ1—RQ4。正文首次给英文配置名后使用中文；表格、算法、公式与参考文献中的英文标识保留。
- 验证6张表逐字未变、11个行间公式逐字未变、3个算法块逐字未变、44条参考文献全文未变且首次引用顺序连续；96数学表达式解析通过。最终再补回知识记录的目标类型及可比较冲突限定，避免语言精简弱化技术条件。只改论文与本记忆，未修改代码、Excel、图片或生成新论文版本。

## 2026-09-15 元信息阈值术语与摘要精简（最新）

- 用户本轮要求1—9项以及暗中随机改变Use Count。已明确拒绝把随机数作为实测记录，不修改表6或原Excel；所有六张实验表逐字保持原值。可按原始使用记录纠正统计，但不得通过随机扰动掩饰规律。
- 已完成第1—3、5—9项：Mt首次定义完整包含任务/目标/版本/历史引用摘要/要求集合及mu映射，去掉后续追加定义；两套检索使用tau_exp和tau_know独立参数，当前各0.20，不编造开发集校准来源；算法3接收双阈值并明确Success/Failure/Pending，正文知识状态只用待验证（Pending），消除学习中/待复核额外状态。
- baseline补同候选轮次、知识检索及更新规则、测试工具和模型调用预算控制。摘要删除模型会话数/耗时细节，结尾改断言质量和复杂分支覆盖受任务及模型影响；英文改six primary evaluation settings。AE权重说明人工先验设计，而非声称经开发集调参或敏感性分析确定。
- 第4项未编造：已通过异步问题向用户确认实际任务顺序、是否随机及种子、5次重复顺序，以及KB在重复/模型/数据集的重置边界。截至本轮保存未获回复，4.1暂未写未经确认的协议。只读看到代码发现任务按_task_sort_key排序、按轮次快照，但不足证明本次5次实验实际执行设置，不可直接写成既成实验事实。
- 验证六张表含Use Count逐字未变；101数学表达式解析、11公式编号、44引用顺序通过；仅更新唯一0904论文和本记忆，无代码修改、无新论文版本。

## 2026-09-15 状态存储对照定义与知识排序一致性修订（最新）

- 仅修改唯一0904论文和本记忆。摘要问题改为程序结构/测试要求/执行结果缺少统一组织，平衡报告LC17/BC13/AE8/MS18及Pass/TIR/AvgExec/Time变化，中英文同步。
- 用户要求对称实验名称，Static Feedback改Fixed Target Organization、w/o Feedback改w/o State Guidance，数值不变。RQ2比较状态驱动目标选择/固定目标组织/无状态引导；baseline定义为无共享测试状态多智能体对照，写明任务、初始测试、模型/采样、重复次数、工具、初始知识副本、角色/阶段等控制条件以及共享状态与局部阶段消息的区别；RQ4与结论同步。
- 测试要求集合及映射mu保存在M_t，节点/关系关联由mu给出，X_t中的X_req按稳定要求ID保存状态与局部结果，Status读取该属性表，形式化闭合。不新增第五状态分量。生成过程句移至3.3.2；生成智能体只返回候选与检查结果，下一修复/优化/增强任务全部由状态智能体确定。
- Unknown可比性按参与比较候选的共同目标集合处理：任一候选未知或条件不可比则全候选统一跳过该目标，再计算各自改善向量。
- 两套检索评分只做组内排序：按签名完整/缺省及成功/失败/学习中分组，组内分数降序+ID并列，按固定组序round-robin选取至k，过滤使用各组阈值。算法3增至17行，不以同值域证明跨组分数可比。
- 知识转换严格连续两个相邻已完成且任务ID不同的任务，都实际采用同一条目版本并给同类可判定结果；未采用/未知/异类打断序列，累计ns/nc与连续转换计数分离。
- 表1拆除三行共用Task Level，HumanEval与HumanEval-X均Function，TestEval原File/Function/Class。AE明确为固定启发式实验配置而非通用质量标准；不编造权重研究或敏感性数据。TIR仅表4完整方法过程分析，RQ2由表5四项质量指标回答。
- 收紧覆盖因果推断，修正“断言检出变异体”为测试杀死变异体但断言各维度得分不同。2.4改为状态引导/知识复用/整体协作流程的实验分析。
- 按本次用户要求修改实验设置为重复5次均值，JUnit5.12.0。没有实际重跑或重算现有表格，也没有修改源Excel或系统代码，后续不得将文字调整称为已新完成五次实验。
- 验证表2—6除授权名称替换外逐字不变，11个行间公式逐字不变，93数学表达式解析、44引用连续通过，分组选择与共同可比目标示例通过。

## 2026-09-15 第三章框架建模算法与协作重排（最新）

- 按用户指定结构重排：3方法概述，3.1TSGColAgent总体框架，3.2测试状态引导机制（3.2.1建模、3.2.2算法），3.3多智能体协作测试生成（状态、生成、知识三个小节），3.4小结。3.1只保留输入目标、职责、总体流程、图1及过渡；式1—3移到建模，式4—7保留于同一算法小节，不设更低层标题。
- 算法标题统一“测试状态引导算法”“多智能体协作测试生成算法”“测试知识混合检索算法”。算法1确认前后都生成包含Confirmable Unknown的I_t并写入状态/历史，资源中断时未确认项保留；算法2选择返回(T_keep,S_keep)，UpdateWithHistoryReferences只接收S_keep和H_next，拒绝候选O_t仅存历史，不直接覆盖正式状态。
- 状态智能体小节压为3段职责与状态交接；生成智能体接收完整任务包并返回结果，知识检索始终由状态智能体发起。知识小节按表示/检索/验证提交组织，式8/9和算法3保留。图1改总体框架图题，图4为生成、图5为知识，复用原图片路径并只调整正文位置/图题，不改图片内容。
- 全文主语回到测试状态引导，RQ2和4.3集中于状态引导目标选择及其在流程中的效果，不把TSG等同动态反馈；删除“自主选择”。摘要病句修正，中英文均用生成过程；六个主要数据集统一六个主要评价对象。4.2.3改“测试生成过程分析”。
- pytLMtester从2.2移入2.3的混合生成讨论，参考文献按首次出现重排（原18/19/17依次为新17/18/19），44条原文未改。5.2补外部有效性段落，5.1末句改为跨任务经验在当前任务中的使用。
- 验证6张实验表逐字未变、11个行间公式逐字未变、83个数学表达式解析通过、44引用连续、3算法行号连续、全部图片路径有效。检查分支：资源中断保留未确认Unknown；拒绝候选状态不覆盖保留测试状态。仅改唯一0904论文和本记忆，未改Excel/图片/程序代码或生成Word。

## 2026-09-15 删除系统章节并按最新实验重构全文（最高优先）

- 用户取消Word导出，要求删第4章系统实现。已删除整章，必要平台、语言与工具配置压入连续的实验设置；全文连续编号为1引言/2相关工作/3测试状态引导方法/4实验研究/5讨论/6结论。
- 已完整读取D:/wn/小论文/论文撰写材料/提示词六文件，最新用户要求优先于旧RQ5、旧综合指标和旧贡献模板。阅读ValiFixTest方法与实验相关页，采用目的—机制/对照—结果—适用范围的论述方式，不复制其实验数值或配置。
- 实验4.1连续写，不分三级标题，按目的、对象、方法、环境、指标、消融设置展开。4.2有覆盖效果/测试质量/生成效果三个小节；4.3消融和4.4知识复用不分三级；4.5本章小结逐项回答RQ1—RQ4，不再堆数字。
- RQ2改为动态状态反馈相对固定反馈和关闭反馈对覆盖/质量的作用，符合表5；RQ4改为与常规生成修复优化多智能体方法比较，不是直接LLM或Single-Agent。baseline按本次用户定义为常规多智能体流程；Static Feedback固定顺序、固定内容，不随测试进展调整。TSG正文突出完整动态反馈、自主目标选择与任务历史/知识学习。
- 全文自有实验模型GPT-4:20b改gpt-oss-20b，temperature0.5、每组重复3次。保留共同平台、模型与评价工具的可比性，删除总token、调用次数、重试预算等无关声明，不宣称各方法内部设计完全相同。
- 当前数据.xlsx已更新！不能使用此前缓存数值。源表配置现为w/o Feedback而非w/o TSG。表4最新TIR：TestEval64.95/66.54/72.53；HumanEval91.40/92.58/95.07；HumanEval-X16.44/20.63/27.14；Cli5.38/7.54/11.06；Csv3.96/6.57/9.53；Lang14.85/17.62/22.17；Gson4.64/7.51/10.52。AvgExec同步最新文件：38.60/40.06/45.17，15.63/16.07/18.23，4.83/5.32/6.84，1.55/2.03/2.81，1.31/1.71/2.24，3.89/4.31/5.83，1.32/1.82/2.32。其他源数值核验未变。
- 新TIR/AvgExec分析重新撰写：gpt-oss-20b每数据集TIR与AvgExec均更高，Time均更低；删除所有旧TIR和执行次数分析。表2/3/5以及6知识计数保持最新源数值，表内无—，不恢复旧A-E体系。
- 删除正文“尚需明细核验”“统计口径”等作者校验语言，保留正常的研究局限与构造有效性讨论。常用MS/Pass不再逐变量列公式，保留AE与TIR正式定义并紧接研究解释；现公式连续1—11，参考文献44条全部引用。
- 最终核验606个源单元格通过，6表无空占位，11公式/72数学表达式解析/44引用顺序通过；旧模型、5次/五次、旧系统章、旧小节、w/o TSG、直接LLM baseline表述均清除。源Excel哈希未改，程序代码未改，无Word输出；只改唯一0904论文与既有记忆。
- 本轮为论文修订及源数据回填，不是重新执行实验或重算原始指标；此前已知方法/实现差异未通过改文自动修复，若后续涉及代码应独立核查，不得声称实际实现已同步。

## 2026-09-15 严格按数据.xlsx重写第五章（最新）

- 用户要求按数据文件重新撰写实验章节，有数据不要有—。已重写第五章，以源文件六表块对应论文六表：1数据集、2LC/BC、3AE/MS、4本方法TIR/AvgExec/Pass Rate/Time、5机制配置、6知识记录。
- 删除源文件没有的外部方法Pass Rate独立表，移除N、Improved、Evaluable/Total、Excluded空列及所有“值/—”展示，六张表没有—占位，仅保留真实方法不适用N/A。源表已删除的A-E体系不重新引入。表1移除源文件未给出的Focal Methods计数列。
- 5.2按覆盖、断言与变异、目标改善/执行通过/开销展开；5.3分别解释动态反馈、知识积累和直接生成baseline，5.4解释知识计数，5.5小结。所有分析采用源数值，主要六数据集18条件LC17、BC13、AE8、MS18的描述性比较保留，TestEval按开发性结果解释。
- 摘要/英文摘要/结论删除不再有本章数据来源支持的“Pass Rate优于基线12组”声明，改为方法自身执行表现；表号与上下文同步。AE/TIR保留运行记录值并保留定义核验边界，不声称重新计算，原子目标方法定义不改。
- 606个对应数据单元格逐项核验，六表无空破折号，13公式、44引用、数学解析通过，数据.xlsx哈希未变化。仅编辑唯一0904论文和本记忆，未改源Excel或代码，未另建论文文件。

## 2026-09-14 按用户原表恢复实验结果及上下文（最高优先）

- 用户明确反对因缺少重算明细而把已有结果全部置—，要求根据数据.xlsx补全并同步上下文。本轮已恢复唯一0904论文表2/3的LC/BC/AE/MS、表5TIR及表6所有配置结果，均原样采用D:/wn/小论文/论文撰写材料/数据.xlsx的汇总数值，不因缺N或原子轨迹再次删除已有数值。
- 新增的N、Improved、Evaluable/Total、Excluded在源表没有，仍保留—。表2/3/6采用“数值 / —”分别显示已有均值和未知N；表5已恢复95.64等TIR数值。AE/TIR标明recorded并在统计口径中说明与任务条件化/原子目标定义尚需核验，回填不是新定义重算或重新运行。
- 第五章覆盖、断言/变异、TIR、机制对比、知识复用恢复具体数值分析；摘要、5.5小结和结论同步。保持上一轮原子目标、Unknown确认、知识验证链、无总预算声明、TestEval开发性划分等方法逻辑，不回滚方法。
- 主要6数据集18条件LC17组/BC13组/AE记录8组/MS18组/Pass12组高于对应最优比较方法。按6数据集等权，Full减Static feedback为LC25.07/BC18.67/AE39.21/MS67.19个百分点；减w/o TSG为45.07/41.93/64.36/74.57；减Static Knowledge为25.61/20.55/35.75/67.75；减w/o Knowledge为42.89/35.31/61.81/80.56；减直接baseline为34.44/23.56/51.34/74.71。TestEval原值完整保留但不混入主要比较计数。
- 验证606个源表对应单元格（510数值、96不适用）通过；表2/3/5/6所有已有结果已填，Pass/成本/知识计数与源表一致；106数学表达式、13式、7表、44引用核验通过。原Excel SHA256前后相同。仅改论文及本记忆，代码不改、不新建论文文件。

## 2026-09-14 原子目标、评价样本与行文逻辑复审（后续轮次）

- 用户指出运行原子目标与实验metric_increase不一致，要求原子TIR、可评价数量、独立指标分母、任务条件化AE、Unknown确认、固定规划对照及知识验证计数链。本轮仅修改唯一0904论文和本记忆，不改程序代码、原始Excel或PDF，不另建论文版本。
- 用户确认ASTER/ValiFixTest/pytLMtester均为本次重新运行、统一评价AE；要求删除实际四维总预算内容。论文统一重跑表述，删除B四维形式化及相同总预算声明，保留已有单次请求配置和候选轮次。算法使用执行配置终止判定。
- 用户确认TestEval前64个任务用于系统测试并形成初始知识。论文按开发性对象呈现TestEval，不纳入主要泛化与知识效果结论；其余6数据集18条件中Pass Rate胜12组。没有声称已经完成种子/评价近重复隔离，也未虚构146任务重跑结果。
- TIR式13与3.2.3共用Improved(q)，明确指定行/分支、Observe行为、Mutant ID、阻塞、异常/类型契约；不读全局LC/BC/AE/MS增量。已开始且已知失败计0并保留分母，真正不可判定才排除，增加Improved、Evaluable/Total、Excluded字段及缺失样本上下界；纯信息确认独立记录成本，不混入生成目标TIR。
- Unknown增加可工具确认集合；算法1先逐项确认再选择生成目标，每相同测试/工具状态只尝试一次并更新H，避免无目标死区与无界重试。算法2显式接收H，任务结束追加UpdateKnowledgeValidation再提交KB，按实际使用的知识与结果匹配更新ns/nc，每任务最多计一次，成功/失败经验均按自身预期判断支持。
- 给输入相容、位置关联的持续不足、可选有效目标、修改AST语句范围及适用知识增加判定条件；候选并列时不再用AE等汇总指标决定，按修改范围及同类工具成本比较。AE仅实验分析，公式10按契约具体性、必要行为观察、Observe关联与适用类别计算，去异常独有30%上限，assertDoesNotThrow弱断言0且排除有效集合。
- LC/BC/AE/MS分别使用独立可统计任务N，表2/3/6改Mean/N。现有数据.xlsx只有汇总百分比，缺原子目标轨迹、各指标有效任务明细及新AE契约分类，无法由旧均值复算。因此论文相应数值暂置—/—，表5原子TIR和计数暂—，撤下依赖旧值的定量结论；原Excel完整保留。用户问此前是否给过TIR，已明确百分比已读取，缺的是证明新原子定义与分母的明细，不是未读取表。
- Pass Rate、AvgExec、Time、知识计数保持原值。AvgExec仅同语言/工具口径比较，N/A只用于方法不适用、—用于结果缺失；表1Task Level为数据集层次描述而非分层实验因子。
- 阅读ValiFixTest引言/方法/评价与局限、ASTER方法/结果分析、CoverUp目标分段与统一重测说明、中文静态分析综述；借鉴按研究对象—操作—可观察结果展开、按具体技术维度比较文献的写法，不抄写内容和结论。减少相关工作中间段落反复回扣本文，方法动词细化为筛选/排序/合并/封装。
- 验证通过：13公式、106数学表达式解析、3算法行号、44引用顺序；Pass/成本/知识表数值与修改前一致；分支B增加不使分支A成功、MutantB检出不使MutantA成功、指定目标变化才成功，已知失败计分母/未知排除计数。验证是论文形式化边界与文本检查，不是新增机制已经在系统代码运行。

## 2026-09-14 新数据与论文结构同步

- 仅编辑唯一0904论文和本记忆，源Excel和程序代码未改。正文命名统一TSGColAgent，首次于引言以Test State Guided Collaborative Agents全称定义，TSG在RQ前定义。摘要按附加提示词不增加中文括号释义。2.4顺序改为已有方法含Procedural Graph、综合比较、本文定位；第3章节点按四类归纳。
- 用户确认baseline为相同提示直接调用LLM，不是Single-Agent；Static feedback为静态顺序反馈；初始KB来自TestEval功能测试。RQ4对应完整流程与直接生成，RQ3限定新表能支持的测试结果，不再声称三角色纯消融或知识降成本已获证明。初始TestEval同源接触和顺序复用写明，源码按记录去重未排除同项目/同函数近重复。
- 读取D:/wn/小论文/论文撰写材料六份提示词；旧RQ5/旧综合指标等示例不覆盖最新指令。保留9月13日现行加权AE、分语言MS、工具会话AvgExec、模块目标TIR口径。本轮补AE常数的工程启发式来源，未虚构文献、专家或敏感性实验。
- 数据.xlsx为1页6表块，21组主实验已齐。论文表1数据集、2LC/BC、3AE/MS、4Pass比较、5TIR/AvgExec/Time、6配置对照、7知识记录；保留原外部方法Pass并按新表更新TSG数值。旧等级列不引入。Gson DeepSeek BC50.93/Qwen BC41.91；Lang Qwen Pass70.91；Gson Qwen Pass62.53。
- LC20组、BC16组、AE11组、MS21组、Pass15组高于对应最优基线。Full对直接baseline的七数据集等权百分点差为LC33.91/BC25.84/AE49.49/MS68.63。知识计数成功6700/4868、待验证564/410、失败3716/2700，合计10980/7978；场景缩短，去掉旧reward/evidence表述。
- 补检索默认权重0.35/0.35/0.20/0.10及0.55/0.15/0.30，权重和1，exp(-age_days/90)、k5、阈值0.20。尚有既知方法实现冲突：本地retrieve_hybrid的quality仍是旧reward归一化，论文Q(e)为验证可靠度；不擅改算法代码或此公式，需作者确认实验实际版本。
- 依赖.txt给的是版本范围，正文据此写Python3.11+/pytest8+/coverage7+及其他包约束，删除待补括号，未伪造精确锁定版本。567个新表数据单元格核验通过，93个数学表达式解析通过，7表13式44文献，PIT引用42补齐。

## 2026-09-13 指标采集、前端与论文口径

- 用户要求补齐论文指标且尽量不改算法；随后明确“口径不同则更新论文，与代码保持一致”，并要求遵守D:/wn/小论文/论文撰写材料下六份写作提示词。六份已完整读取；旧模板里的TSGColAgent、SFC/TSQ实验指标、RQ5及预设正向结论未套用。
- 新增metrics/paper_metrics.py、measurement.py和pytest_counter.py，通过执行入口轻量接入独立记录。AE/MS不变更原评分，LC/BC按有效覆盖显示，PassRate按全部选定任务；AvgExec现在明确为工具级测试会话，联合覆盖计一次、PIT一次评估计一次，不宣称逐PIT内部测试计数。TIR按行动前固定的评价目标计算，覆盖/断言/变异使用指标提升，异常/类型使用可比较运行观测扩展，不等同于第3章逐原子行为要求的候选排序。统计不进入运行决策。
- 任务paper_metrics目录保存事件及统计摘要，跨轮次累计，外层回退会更新TIR保留结果但保留成本。新增统计写入任务、批量、总摘要的数据集条目及终端汇总。前端详情/统计区显示全部保留指标、TIR分子分母/排除数、执行计数完整性及经验记录/上下文使用次数。旧数据缺记录不填零；需要完整指标须新空输出目录。
- 新增scripts/export_paper_metrics.py，按实际模型标签只读导出各数据集结果，缺证保持null；不改论文数值或旧runs。论文修订AE/MS/AvgExec/TIR及质量均值范围，清理摘要缩写括号和正文工程核对说明。189行表格内容及3个算法块逐行保护一致，13个公式编号连续。
- 理论框架条件性可行，但实质差异仍在：三轮不等于三个候选；逐要求保护/目标词典序、三目标容量、任务级固定只读知识、Q(e)可靠度及执行预算单位与当前代码不同。依用户写作提示词将方法冲突列入论文实验数据/论文逻辑与实现审查_20260913.md，未擅改算法或图中文字。不可对用户宣称全文已与代码完全一致或全部消融已实现。
- 已验证：8项已有执行加速相关检查通过；新增统计定向检查通过；一次真实pytest/LangGraph上下文计数通过；一次无模型小型完整工作流及前端数据适配通过；前端构建和3项格式测试通过；21个修改Python文件语法检查通过。另验证旧GPT目录HumanEval导出时AvgExec/TIR仍缺失。未重跑完整数据集或模型服务。

## 2026-09-12 TSQColAgent界面与历史指标核对

- 按本次要求，方法名统一为TSQColAgent（界面、系统目录和当前主论文）；展示指标移除SFC/TSQ，趋势与矩阵使用真实BC，状态证据卡使用BC/MS，雷达图改为四轴。任务详情新增Time/s，取任务跨续跑累计秒数，缺证显示“—”。旧评分字段和内部调度算法保留兼容，不篡改历史结果。
- ValiFixTest按钮不再显示“未接入”及对应悬浮文字，仍保持禁用；此次没有实现独立执行器。
- 只读核对两组历史结果的14份批量摘要、1422份任务摘要，各批次任务/结果/摘要数量一致。runs-GPT-4全部摘要记录gpt-oss:20b，不可仅凭目录名认定GPT-4。部分现填PassRate与批次结果按论文定义计算不一致，核对值单列，不覆盖论文已有数字。
- 旧数据缺完整测试调用账本及逐目标预注册、独立前后判定，不能准确补填AvgExec/TIR。主论文增加缺证说明，空缺保持“—”；所有已有表格数值（包括LC、BC、AE、MS、Time）保持不变。未新增未来运行的全链路指标采集，不能宣称这两项已具备自动统计能力。
- 新增只读脚本scripts/audit_experiment_metrics.py及论文实验数据/历史结果指标核对_20260912.md。前端生产构建通过，3项格式定向测试通过；Python/Java历史任务读取适配与系统目录检查通过。未重跑数据集、未调用模型、未修改实验登记与知识库。

## 2026-09-12 删除指标后的全文逻辑复审

- 仅编辑唯一0904论文和本记忆，未改源Excel、实验数据、程序代码、参考文献或另建论文版本。最新附件要求继续适用；原提示词目录当前不存在。
- 审查研究问题—相关工作—状态建模—不足识别—有序目标—候选保护与保留—历史/知识—实验—讨论结论的衔接。增加第3章方法总述、目标组合示例、第4章要求标识映射及第5章RQ评价对应关系。
- Improved按六类离散目标状态变化在3.2.3正式解释，实验TIR引用该判定并只对最终保留状态统计；Unknown目标统一从该次候选排序中略过以保持所有候选可比，保护要求Unknown仍阻止候选接受。
- 算法1返回补全后的S_t，算法2显式接收，停止时保存状态；修正QualitySatisfied参数一致性、先处理已登记前置要求、保持优先层次后容量截断。
- 状态更新明确先构建候选状态、再选择测试、最后更新保留状态及历史引用；拒绝候选的反馈不会覆盖当前测试要求的满足状态。无可行候选时保留原版本并可引用历史局部修复继续生成。
- 保护替换范围限任务预先许可的非必要要求；区分任务停止、执行通过与必要要求完成；TIR按任务/轮次/步/目标标识去重，纳入有可比较预检查结果的恢复行动。
- AvgExec与4.4统一：计实际测试调用，解析/编译/收集仅计耗时，同一执行兼采覆盖不重复计。讨论明确TIR受目标、生成及接受共同影响，多目标记录存在同一任务/行动内关联，现有数据仍不能替代RQ2—RQ4专项验证。

## 2026-09-12 删除嵌套评价体系并按主表回填实验（最新最高优先级）

- 用户79cac96d附件明确仅保留TIR为专项指标。唯一0904论文已删除SFC/NC/EC/PC、全部NRR、TSQ、RetrySR、Valid Rate及A-E等级体系，RQ缩为附件给定4个，贡献调整为状态表示、TSG、三智能体/历史知识机制与实验评价4项。所有旧指标相关公式、阈值、结果表及论述删除。
- 共享状态及Branch/Loop/Return/Exception、Observe、Kill/Survive、RuntimeType等事实完整保留。要求由基本要求和任务要求初始化并区分必要/可选；不足取Unsatisfied并合并实际执行阻塞，Unknown独立记录。TSG依据阻塞、就绪依赖、近期持续不足及稳定位置组织目标。
- 候选Executable仅Collected且未超时且Pass。可执行但无有效断言的测试保留为状态输入，观察要求继续未满足。原质量保护改为已满足要求保护条件Protect；选择先执行与保护，再按有序目标布尔向量词典序，目标相关直接指标作为并列判定，再比较修改范围和执行成本，最后保留当前测试，无加权总分。
- 算法1/2/3同步，运行算法中无TIR；TIR独立从前后实际保留状态统计，加入执行恢复目标，原子目标各一记录。H_t任务内维护、KB只读快照、任务结束提交的生命周期保持。
- AE按附件简化为有效断言数/全部断言数×100%，无断言0；MS直接杀死/(杀死+存活)，无有效变异体N/A。表3数值按用户明确指定原样采用，未做旧加权AE到新比例的推算。若该源表AE实际仍来自旧公式，需原始断言计数核实，此数据来源口径仍不能由汇总值独立证明。
- 数据来源D:/wn/小论文/论文撰写材料/多智能体论文方法数据.xlsx：LC/BC以源表2、AE/MS以源表3、Pass Rate以源表4、Time以源表5为准。论文表6Full已改CSV LC89.64/BC56.00/MS97.36及Lang BC56.48，与主表一致；相同Full值同步状态专项与知识专项。原Excel保持原始记录未覆盖。
- 用户同时要求随机降低Valid Rate及分配等级缺失2%，但附件最终删除这两个体系，故论文直接移除旧Valid列和等级表，未生成随机实测值、未重分配旧等级，也未修改源Excel这些字段。
- 新表共9张：1数据集，2LC/BC，3AE/MS，4Pass Rate，5AvgExec/Time，6模块/角色直接结果及开销，7状态专项TIR/BC/AE/AvgExec，8知识专项Pass Rate/AE/AvgExec/Time，9经验计数。旧系统总览截图含旧指标，正文引用移除，原文件保留；正文保留图1—5方法图。
- 表2/3/4逐单元格匹配源表，Full行数据再次独立校验。20组完整项目—模型条件中LC19组、BC16组、AE11组、MS20组、Pass Rate14组高于已报告基线最高值；结论只据此说明RQ1直接结果，不虚构RQ2—RQ4缺失的消融/目标轨迹结论。Time全部按源表20个已给值显示。
- 知识出现10980、使用7978，成功类6700/4868、失败类4280/3110。按记录标识计数，使用频次不作任务成功率。TIR、AvgExec及无结果消融继续空值。
- 修正用户列出的4.1“组织过程/经整理和验证”、2.3确定性规则处理病句、2.4角色承担、候选词典序并列规则、知识条目签名分类及DeepSeek-R1:14b命名等。第4章不另设执行/生成组件，直接使用三智能体与工具链。
- 公式连续1—13，参考文献44条条目原文保留并按新首次出现重排；数学解析通过，表格结构、来源数值、图片路径通过检查。已验证高优先目标(1,0,0)胜过(0,1,1)，运行算法无TIR依赖。仅修改唯一论文与本记忆，程序代码与源Excel未改动。

## 2026-09-12 新实验汇总表可复算性核对（只读分析）

- 用户提供新路径D:/wn/小论文/论文撰写材料/多智能体论文方法数据.xlsx。工作簿只有Sheet1，11个表块，968个非空单元格，无Excel公式，无逐任务/逐候选/5次运行原始明细。本轮只读源表和现行0904论文，没有覆盖源表或回填论文，PV按用户要求忽略。
- 可直接算出的是记录计数合计、使用/出现比例和表间一致性。不能将旧汇总SFC/AE/NRR/TSQ自动当作现行定义结果；NRR列未区分proc/base，I_NRR、I_MS和逐任务U缺失。不能从旧TSQ直接扣除PV重建新版TSQ，也不能在适用权重变化时以汇总均值代替逐任务计算后求均值。
- 表11总出现10980、总使用7978，与给定Total一致，使用/出现比72.6593806922%。成功类6700/4868，比72.6567164179%；失败类4280/3110，比72.6635514019%。源表声明这些为记录标识口径，不是独立任务数或已确认物理执行数，故比例不称为任务成功率。
- 表11第153行 Test Generation & Compilation Repair：1781/2451=72.664219%。
- 表11第154行 Coverage Identification & Targeted Optimization：1601/2203=72.673627%。
- 表11第155行 Assertion Design & Mutation Management：882/1214=72.652389%。
- 表11第156行 Successful Execution Recovery：215/296=72.635135%。
- 表11第157行 Measured Quality Gains & Evidence Confirmation：389/536=72.574627%。
- 表11第158行 Failure Diagnosis & Pending Concepts：410/564=72.695035%。
- 表11第159行 Compilation & Execution Failures：1597/2198=72.656961%。
- 表11第160行 Model/Planning Failures & Invalid：135/186=72.580645%。
- 表11第161行 Negative Outcomes & Pending Items：968/1332=72.672673%。
- 表8为百分比而非任务计数，直接相加A+B+C仅作为既有等级表核对，不能替代新版等级复算：
  - TestEval：ABC=95.71%，表4DS Valid=95.71%，Pass=99.52%，等级总和=100.00%。
  - HumanEval：ABC=99.39%，表4DS Valid=99.39%，Pass=100.00%，等级总和=100.00%。
  - HumanEval-X：ABC=97.56%，表4DS Valid=69.94%，Pass=69.94%，等级总和=100.00%。
  - Commons-Cli：ABC=98.00%，表4DS Valid=76.47%，Pass=66.47%，等级总和=98.00%。
  - Commons-Csv：ABC=100.00%，表4DS Valid=72.50%，Pass=71.25%，等级总和=100.00%。
  - Commons-Lang：ABC=95.06%，表4DS Valid=64.55%，Pass=64.55%，等级总和=100.00%。
  - Gson：ABC=95.65%，表4DS Valid=64.49%，Pass=64.49%，等级总和=100.00%。
- 表4有35组Valid Rate>Pass Rate（TSGColAgent 8组、ASTER 21组、pytLMtester 6组）。按当前相同总体且ABC需Pass的定义存在冲突。源单元格对：AR89/AV89、AS89/AW89、AR90/AV90、AS90/AW90、AP91/AT91、AR91/AV91、AS91/AW91、AR92/AV92、AS92/AW92、AR93/AV93、AS93/AW93、AP94/AT94、AR94/AV94、AS94/AW94、AR95/AV95、AR96/AV96、AP97/AT97、AR97/AV97、AP98/AT98、AR98/AV98、AR99/AV99、AP100/AT100、AR100/AV100、AP101/AT101、AR101/AV101、AR102/AV102、AR103/AV103、AR104/AV104、AP105/AT105、AR105/AV105、AP106/AT106、AR106/AV106、AR107/AV107、AR108/AV108、AR109/AV109。
- 表2/3与表6的Full/DeepSeek-R1有4处冲突：
  - Commons-Csv LC：AE48=89.64%，AI114=100.00%。
  - Commons-Csv BC：AI48=56.00%，AI115=50.00%。
  - Commons-Csv MS：AI75=97.36%，AI117=100.00%。
  - Commons-Lang BC：AI51=56.48%，AJ115=36.48%。
- 新版SFC缺各任务/每次运行的节点、可追踪关系、控制节点的访问数与总数及适用性；AE缺按新弱断言规则筛选后的A_e/A_n/A_m、q(a)、检查类型或原始测试断言；NRR缺统一初始/候选产生前/候选后各保护维度及适用性；TSQ还缺逐任务U、最终NRR_base和MS正式适用标识。
- TIR缺预先目标及执行后保留状态的对应改善记录；AvgExec缺每任务实际测试执行次数；RetrySR缺恢复对象去重清单与成功恢复计数；新A-E/Valid Rate缺逐任务完整质量及U条件，不能从数据集均值重新评级。旧汇总RetrySR已有值仅保留为源值，当前无法独立复算。
- 源表Gson/Qwen3.5的TSGColAgent LC/BC/AE/MS/TSQ/Time/Valid/Pass均缺值；消融各变体多数为空，不能由Full推算。未继续纠结版本/API/预算等用户本轮明确暂缓的参数。
- 用独立Python读取核对了知识合计、35组比例约束冲突、Commons-Cli等级总和98%；artifact-tool只读导入并渲染表4、8、11核对了百分比和合并表头。无新版核心指标缺乏数据时伪造点值。


## 2026-09-12 正式表达与第五章新旧定义同步

- 根据用户62424894附件进行全文正向学术表达清理：删除提醒作者/AI不要如何理解的句式，压缩3.2伪代码复述；第3章保留语言无关AE定义，Python/Java具体断言规则移入4.2；SFC异常记录方式移入实现说明。方法结构、算法逻辑及TSQ/SFC/AE既定权重不重构。
- 第5章解除上轮冻结并同步：全文删除PV，表5移除PV列，表7移除PV行、NRR明确改为NRR_proc。可比较集合A_c仅含NRR_i为0/1的尝试，过程均值排除N/A，无可比较尝试为N/A；最终及候选综合评分均使用相对统一S_base的NRR_base。所有既定实验数值保留，缺失结果为“—”。
- Single-Agent保留与Full相同共享状态、TSG、H_t、KB、动态检索及范围、目标、目标容量、工具与预算，仅使用单一LLM上下文顺序承担三个职责。5.3解释改为角色分离效应，旧静态模板及整体架构混杂说明已清理。
- Full任务内使用H_t与只读KB快照，结束后提交供后续任务；Static Knowledge全部任务使用初始KB，任务内H_t使用方式与Full相同；w/o Knowledge保留任务内H_t但无跨任务知识输入。5.5旧轮内知识积累口径已修正。
- 读取用户ValiFixTest正式PDF第12页并目视核对：JUnit5.13.4、PIT1.15.0、Maven Surefire3.0.0-M7、Defects4J2.0.0、Ubuntu22.04/JDK11/Maven3.8.6/JaCoCo0.8.8。这些软件版本写入4.5；参考文献里的模型GPT-4o-mini、4096tokens、6次运行不迁入本论文。此前明确的Mockito/Gson/Lombok等版本继续保留。
- 模型ID按用户确认为DeepSeek-R1:14b、Qwen3.5:27b、GPT-4:20b；temperature0.2和2048tokens保留。按用户授权设定API180秒、单轮3个原子目标、知识转换连续2个已结束任务的同向验证。所有实验条件5次独立运行，先逐次计算指标，再报告5次算术平均，未知结果仍为空；这些是论文配置，未修改或运行代码。
- 贡献3按最新授权改为实验评价设计，去除未获实测结果支撑的“实验结果表明”等结论；RQ1—RQ5保持当前原文。相关工作仅局部清理，11张表数量、6张图引用、44条参考文献保留。
- 剩余问题：4.5模型服务提供方及API地址；4.5Python/pytest/coverage.py/AST变异执行器依赖精确版本，所给PDF为Java研究，未含这些信息；5.1.3各资源预算的具体总上限；5.2—5.5各条件5次运行原始报告及外部行为评价数据。前三项为参数缺失，后者为实验数据缺失，等待作者实际材料，不编造。
- 已检查全文无PV与旧空历史NRR=1、旧Single-Agent模板、即时KB更新等定义；18个行间公式含17个连续编号及166个行内表达解析通过，表格行列匹配。仅编辑唯一0904论文与本记忆，没有修改程序代码或生成新论文文件。

## 2026-09-10 TSG核心机制与指标边界修订（最新定义）

- 用户附件de168bd1要求本轮严格不改第5章、贡献、最终结论、摘要数字。上述章节/段落已逐字核验保留；所有实验表、结果、任务顺序、显著性和TIR难度控制均未调整。旧第5章因此有意保留待同步口径，不得声称全文实验已一致。
- 第3章与第4章删除PV方法定义及TSQ分量，仅用U判断可行性。TSQ改为(0.30SFC+0.20AE+0.15I_NRR NRR_base+0.20I_MS MS)/(0.50+0.15I_NRR+0.20I_MS)，缺失NRR_base/MS移除对应权重，不将N/A当0。局部NRR_i只用于过程保护，空可比较集合为N/A且不进均值；最终及候选综合评分统一相对S_base计算NRR_base，不代入局部NRR或均值。
- SFC改为纯NC/EC/PC适用性加权覆盖，无LC/BC下限；结构合法不存在、提取失败和执行失败分别记录，N/A分量移除权重，全部N/A不做正式TSQ。AE统一先排除弱断言，q(a)按具体目标行为检查分类，A_e/A_n/A_m及EffectiveAssertion定义一致；AE高阈值0.85，可接受0.75。U真但无法形成正式SFC/TSQ时暂不形成依赖其的质量评级。
- RQ5按作者新原文替换为传统指标之外的质量区分与解释信息，相关引言、方法、讨论避免指标自证；第5章旧RQ5论述保留待后续修改。
- 算法1“测试状态引导”移到3.2评价指标前，展开不足逐项登记、位置映射、相容目标合并、容量拆分、阻塞/前置条件/最近持续不足/稳定位置的确定性顺序，任务包中直接提供H_t摘要，按缺少输入/断言/失败处理知识字段触发检索。没有新的加权重要性评分。
- H_t仅当前任务历史，KB为已结束其他任务的知识。算法2任务开始固定KB只读快照，循环内仅归档候选、局部知识使用和状态变化；循环结束关闭任务历史、提取经验并CommitTaskKnowledge。新记录从下一任务开始可检索。相应3.3和4.1/4.3/4.6说明已同步。
- 系统配置写入作者确认的Ubuntu22.04、Xeon2.40GHz/32GB、JDK11/Maven3.8.6/JUnit5.12.x/Vintage/4.13.2/Mockito5.12/JaCoCo0.8.8/Gson2.10.1/Lombok1.18.28；模型实验标签DeepSeek-R1(14B)、Qwen3.5(27B)、GPT-4(20B)，temperature0.2/max tokens2048。标签不是推断出的官方model ID。
- 后续Single-Agent硬约束：与Full相同共享状态、TSG、更新、H_t、KB、动态检索及检索范围、工具、模型、目标、轮次、token/执行/时间预算，仅改变三个独立职责上下文为一个统一智能体；本轮只记录此约束，未改第5章旧静态模板条件。
- 后续实验同步位置：5.1.3及表5/表7统一删除PV；5.1.3/5.2/5.3更新NRR_i、NRR_base、N/A均值与最终TSQ；5.3状态质量及评级按纯SFC/弱断言排除/AE0.85重算；5.1.2/5.3更新Single-Agent；5.5及表10/11改为任务结束后提交KB；5.3.2按新RQ5检验补充信息，独立外部评价留待实验阶段设计，不虚构Defects4J结果。
- 需要作者确认：Python、pytest、coverage.py、PIT及自研变异执行器依赖的精确版本；实际API服务、服务端model ID、API超时和各条件重复运行次数；目标容量与知识验证次数等既有配置具体值。未补造缺失参数。
- 验证：17个原编号保持，增加无编号局部NRR分段定义以避免影响冻结实验章；所有行间及行内数学表达解析通过。完成N/A/零覆盖/缺失分量/局部NRR边界检查，第5章、贡献、摘要和结论原文均未变。仅修改论文和本记忆，未修改代码或图片，旧指标代码与KB更新代码仍需后续同步。

## 2026-09-10 A1—A15/B1—B10及系统章节修订

- 唯一0904论文按最新审查意见统一：H_t仅为任务历史，KB为跨任务持久知识库，M_t只留引用/摘要；三个算法的查询、归档和知识更新均采用此分工。测试版本按r编号，式（3）考虑本轮、当前保留及历史可用候选，先限定可执行和保护条件，再比较TSQ，无可行结果时保留旧测试。
- QualitySatisfied定义为式（8）可执行性成立且任务预定必要要求全部满足，不以A/B停止。B为调用、token、执行和耗时上限配置，各项逐项比较。算法2明确Screen→条件满足且预算允许→FullEvaluate，非完整候选不能作为已完整评价结果保留。
- 代码核实SFC使用max(结构SFC,0.55LC+0.45BC)下限，无BC用LC，无有效工具覆盖用结构值；AE存在NR=0封顶0.30。已写入正式公式，并补齐q(a)、有效/正常断言与四布尔条件的操作定义。PV全称Program Validity，MS可靠条件定义为完整评估且有效变异体≥3。
- 原AvgIter全部改为AvgExec（平均测试执行次数）。所有比较方法最终输出相对统一初始状态计算NRR并给TSQ/A-E，内部逐次候选NRR与该最终评价分别保存；表7NRR仍为全部尝试均值。
- Full和w/o TSG单轮目标容量相同、均可多目标组合，固定调度类别起点平衡。w/o History Comparison仅关闭规划历史信息，保留NRR/保护/归档/知识更新。TIR明确为最终保留状态中的目标改善，初始生成的可比较质量目标计入。
- 表6/7/8新增w/o TSG & w/o Knowledge组合条件，保持11张表；质量指标按2×2交互差异分析互补。Single-Agent仅用于整体架构差异，不归因于单一角色分工。RQ1—RQ5原文保留，按用户最新建议收敛贡献第一项“能够解决”措辞，其他锁定贡献未自行改写。
- 删除旧经验收益式，检索Q(e)改为支持/冲突验证次数的平滑可靠度(n_s+1)/(n_s+n_c+2)，正负知识性质与可靠度分开；失败模式独立相关性筛选与容量，不被负收益压低。该新策略尚未在代码实现，不能称为当前程序已同步。
- Python变异工具核实为系统0.1.0内置AST执行器，无独立外部版本；模块SHA256为6a173fa50a5fd19b94723bc02d868dda5b974a8a5c0e7bcbac52142572fd9de6。正式文中说明工具身份、算子、结果类别和系统版本，不编造mutmut等外部工具。
- 第4章压为“系统实现”，仅总体架构、语言适配、角色/存储映射、工具链及小结，保留图6一张综合视图，删除原图7—10正文引用、173项回归/6项前端/生产构建及产品操作说明，原图片文件未删除。四种层级Function/File/Class/Project；Dataset为批量范围。表1改Dataset Scale并标单位、Release / Version，原数值不变。
- 删除写作过程备注、结论压为两段，相关工作只做局部语言清理。图4使用内置图像编辑工具将历史收益reward改为验证可靠度（图内使用Reliability和Q(e)），原布局不变，论文引用图4_知识智能体_可靠度.png；已目视核对最终标注。
- 删除收益公式后连续编号1—17；44条文献原文保持，引用按首次出现重排，摘要/第4章/结论不加引用。已核对数学解析、图片路径、表数量及文献链接。
- 实际执行5项指标检查通过：AE异常封顶、SFC双覆盖下限、保留更高结构值、无BC回退LC、无工具覆盖回退结构值。另复现Java异常断言分类缺陷：单一assertThrows的现有AE返回0.7875而非≤0.30，原因是共享AE代码仅用pytest.raises识别异常；新版论文已定义两语言异常类别，代码未改，后续需同步。

## 2026-09-09 依据最新十五项意见重构论文方法（覆盖旧定义）

- 以用户本次pasted-text.txt为最新方法定义：正式过程只保留r∈{1,2,3}，每轮一次实际生成/修复/优化，轮内状态步t单独计数。删除旧R1/R2/R3分阶段叙述。算法2拒绝候选后仍可重新规划，最多3次尝试，保存全部候选及其评价并优先保留较优结果。
- 删除最大不足、80%候选竞争及旧式（9）调度评分；删除旧式（11）的0.85、8步传播与最大传播值定位。状态评估改为识别不足集合、定位目标、组织单目标/多目标任务、更新状态及比较历史变化。全文“缺口”改为“不足”，“程序状态节点”改为“程序结构节点”。
- 初始状态由源码与测试上下文共同建立，AST是输入之一。操作范围和候选接受/选择由测试状态智能体负责，生成智能体仅在给定范围内执行并消费K_t。统一Python/Java生成描述，Java适配形成JUnit骨架。
- NRR针对所有尝试候选与其产生前的测试状态比较，全部尝试求均值，不只统计接受或修复候选；NRR不依赖TSQ。RetrySR按实际进入恢复的任务统计，不再绑定固定第2阶段。TIR直接依据目标要求及状态比较判断改善，多目标按步骤—目标记录统计，不再使用传播分数，仍不进入TSQ，外部评价隔离保留。
- 式（3）限定为已获得测试与合格候选间选择更优者；B为综合资源预算。原TSQ、SFC、AE、MS及A-E数值标准不变。删除2个公式后全文连续重排为式（1）—式（18），检索为11/12，NRR为15/16，RetrySR为17，TIR为18。
- 混合检索改用Sim_text（Jaccard词项匹配）而非语义相似度，结构、收益、时效继续保留；0.35/0.35/0.20/0.10及0.55/0.15/0.30、90天、0.20阈值已在experience_memory.py中核对，转入配置说明。k按上下文容量设置，未在论文虚构固定值。
- 知识分为已验证经验、失败经验、待验证知识，删除对话概念及理论正文中的两次验证阈值。语言参与匹配但不排他，实验独立初始化仅为实验控制，不是方法禁止跨语言复用。
- Single-Agent无独立知识智能体，使用同源经验预处理的静态模板，不执行独立检索与经验更新；正文说明此对照同时涉及角色和动态知识组织差异。表9的w/o Gap Propagation改为w/o History Comparison，其他10张表及全部数值不变。
- 阅读参考PDF第1—6页方法与框架，借鉴进展条件化引导及失败历史保留，不导入Procedural Graph的离线拓扑演化。增加该文献并重新维护44条交叉引用，原43条条目原文不变；锁定的问题与贡献保持原文。
- 使用内置图像编辑工具同步图4，项目文件为论文图片/方法框架/20260904/图4_知识智能体_机制修订.png。编辑要求：保留四列检索布局，改为适用性检查、语言特征、词项相似度text；结果返回测试状态智能体；生命周期为待验证知识向已验证经验/失败经验分流；执行结果与状态比较回流。已目视检查并更新论文引用，原图片保留。
- 本轮仅完成论文和图4修订，未修改系统代码或运行新机制实验。当前代码中仍有旧检索记录类型等实现，不能将论文修订称为代码实现已同步。旧提示词文件夹当前不存在，按本次明确修改意见及此前读取的格式规则执行。
- 验证：18个公式解析通过，11张表保持结构与数据，44个参考文献锚点及链接有效，10处图片文件存在，未新建论文或Markdown文件。

## 2026-09-09 新版十一张实验表替换（优先于旧表设计）

- 根据用户同名《多智能体论文方法数据.xlsx》最新Sheet1替换唯一0904论文中的全部实验表，保留表1—表11的给定名称与分组。源工作簿只读，不输出新论文或Markdown版本。
- 表1数据集；表2覆盖LC/BC；表3检测AE/MS；表4执行Valid Rate/Pass Rate；表5效能TSQ/PV/AvgIter/Time；表6模块代码质量；表7模块状态质量SFC/NRR/PV/TSQ；表8评级；表9状态引导TIR/SFC/TSQ/AvgIter；表10知识策略；表11经验积累与复用。
- 新版明确ValiFixTest仅参加四个Java Class/Project数据集，pytLMtester仅参加两个Python数据集。该范围统一应用表2—表4，含表4未显式画横线的HumanEval-X组合。N/A为不适用，“—”为待报告值。
- ASTER在Commons-Cli/GPT-4的LC72%、BC61%按新表放入表2。旧独立31任务及Method coverage记录已不在新版源表，不再保留该段旧参考结果。
- 表9采用Full、w/o State Update、w/o Gap Propagation、w/o TSG；后者承担固定调度对照。TIR与外部影子状态定义不变。表10采用Full、Static Knowledge、w/o Knowledge，固定初始知识与任务顺序，说明条件及数据集之间独立初始化。
- 第5章按方法对比、模块消融、状态引导、知识复用组织，保留5.6本章小结；旧协作接口表和独立指标选择表已由新表替换，不应再引用旧Goal Handoff、Independent Pass Rate等列。正文相关引用和摘要、结论实验范围已同步。
- 表11按源表保留两条成功经验和两条失败经验待填行，具体场景和频次不编造。20个公式、3个算法、锁定问题与贡献及43条文献内容未变，已验证表题、合并表格网格和固定值。

## 2026-09-09 论文新增目标改善率TIR

- 仅修改唯一0904论文，在3.3.1式（12）后补充目标标识及执行前后传播分数记录；5.1.3在RetrySR、AvgIter和Time之后新增式（20），同一公式包含I_t判定与TIR汇总。
- TIR比较执行前选定的同一q_t在更新前后的8步缺口传播分数，严格下降记1，否则记0；确认满足后退出缺口集合可按0评分，记录丢失或工具不可用不能视为满足。
- 仅统计实际执行且前后状态可比较的质量优化；失败恢复仍由RetrySR评价，未执行、不可比较及中断等原因单独记录，无有效分母记N/A。不得以执行失败或候选被拒绝为由删除仍可比较的未改善步骤。
- 全部消融含Full统一使用evaluation-only shadow state重建及评分，目标在执行前记录，评价状态与分数不回流到规划、检索、生成、排序或停止判断。评价开销与生成开销分别记录。
- 表5补充各条件TIR及Fixed Scheduling对照；新增5.3.3解释调度评价，表8增加TIR列。保持九张表，已有数据不改，新增结果为占位符。TIR不进入TSQ或A-E评级，原式（1）—式（19）、算法、问题与贡献和文献不变。
- 本次为论文方法记录要求与实验评价设计的修改，未实现或运行新的系统采集与影子评价模块，不得将本记录解释为TIR采集功能已在程序中完成。

## 2026-09-08 相关工作章节深化

- 按用户要求阅读CoverUp、ASTER、CANDOR的相关工作章节，参考其按技术问题分类、解释代表方法、比较输入与反馈机制、明确研究位置的论述方式；不复制原文，不将其对旧版本方法的批评直接套用于新文献。
- 唯一0904论文第2章由约2400汉字扩展至约5500汉字，保留2.1—2.4与2.5本章小结。增加结构覆盖与预言、套件搜索与动态目标选择、模型介入时机、路径与覆盖提示、验证修复、断言变异增强、检索与多角色协作的机制比较。
- 明确已有研究已使用动态目标、结构化上下文与工具反馈，本文差别集中在多类测试质量反馈的状态组织、目标规划与知识准备的联系。不得笼统声称其他方法均无反馈或无结构化上下文。
- 43条参考文献条目内容保持不变，正文引用与文末编号继续按首次出现顺序同步维护。第2章之外正文仅可能调整引用编号，问题与贡献、方法、公式、算法、实验表不改动。

## 2026-09-08 用户参考文献全文替换与交叉引用

- 唯一论文0904版的原17条参考文献已全部替换为用户《D:/wn/Visio_Editable_10_Figures_Full_Package/参考文献列表.docx》中的43条。条目文字逐字保留，仅按正文首次引用顺序重新编号；用户源DOCX未修改。
- 正文使用可跳转的编号引用，43条文献全部被引用，共54处引用。补充相关工作中的测试预言、覆盖与有效性、变异分析、多目标搜索、生成修复、检索增强及仓库级多智能体研究；方法与实验设置补充工具、数据集和比较方法出处。
- 摘要、第四章、结论没有参考文献交叉引用，第四章原LangGraph旧编号已移除。问题和贡献8段锁定文字、19个公式、3个算法和9张实验表保持不变。
- 已检查条目与源文档一致、编号按首次出现连续、每条文献均有正文引用、链接目标正确。后续增删引用时须同步维护正文编号、ref锚点和文末顺序，不保留旧编号。

## 2026-09-08 论文实验表与方法表述更新（优先于旧记录）

- 继续只维护《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》，不新建论文或Markdown文件。
- 第5章的九张实验表已依据用户《多智能体论文方法数据.xlsx》重新组织。表1—表7保留给定名称和明确数据；表8设计为三智能体职责分工与信息协作对比，表9设计为自定义指标必要性与有效性评价，对应RQ4、RQ5。源工作簿保持只读。
- 缺少的实验结果统一使用“—”，不适用条件使用N/A；未编造实测结果。数据集规模、项目版本、目标数量及ASTER独立参考记录中的固定数值均保留。旧711项、413项及旧增益数字不再混入新实验叙述。
- A-E仅为质量评级，R1/R2/R3仅为依评级安排的处理方案，不作为算法内容。算法2改为给定共享测试状态下的测试优化；HumanEval-X为论文统一名称。
- 第2至第6章分别增加2.5、3.4、4.6、5.6、6.3“本章小结”。问题与贡献锁定原文不变，19个公式内容不变；表格行列结构及公式解析已检查，10处图片引用均存在。
- 本记录更新此前关于第5.4节回应问题、表格保持不变以及三轮算法的旧说明；后续按当前论文和最新用户要求执行。

## 论文问题与贡献锁定原文（用户最新明确要求）

- 《论文修改规则与论述说明要求/结构辅助提示词.txt》中“【问题设计】”下新RQ1—RQ5，以及“【本文贡献】”中最后“替换为：”后的三项贡献，均为用户锁定文字。论文已逐字复制，后续不得调整其中任何词语或标点；此要求优先于此前“差异较大以原论文为主”的通用处理原则。
- 方法章节与实验验证范围已按五个RQ和三项贡献补充衔接，第5.4节逐项回应研究问题。锁定第三项贡献含有尚需独立实验验证的表述，旁文及实验部分明确现有结果范围和待验证内容，不编造实验结果。
- 本轮19个公式内容、3个算法步骤、9张表格均保持不变。仍只编辑唯一论文《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》，不新建论文或Markdown文件。

## 论文冲突处理优先级（用户最新要求）

当提示词、辅助说明与原论文存在较大差异时，以原论文论述为主，保持原论文逻辑理论不受影响。提示词用于辅助表达与结构调整，不直接覆盖原有变量含义、公式定义或算法逻辑；用户明确提出的具体修改仍按其授权范围执行。此规则优先于下文较早的提示词应用说明。后续仍仅编辑唯一论文《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》。

## 2026-09-06 论文规则文件夹与当前研究主线

- 后续所有论文修改必须先遵循项目根目录《论文修改规则与论述说明要求》中的五份提示词，重点参考《结构辅助提示词.txt》。继续只编辑《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》，不新建论文或Markdown文件。
- 方法名称为TSGColAgent；TSG只表示测试状态引导机制，S_t称共享测试状态。论文已采用新文件给出的RQ1—RQ5和方法贡献主线，状态引导与协作是重点，轮次和存储属于辅助执行安排。
- 公式、算法步骤、变量含义和实验事实受到格式/措辞提示词的保留约束；新符号表Q_t、O_t存在与原公式的多义冲突，按规则保留现有公式定义并在回复中列为需作者确认。未把结构辅助文件中尚无实验支持的肯定性结论直接写成已证实结果。
- 新增实验设计覆盖指定模型、Java方法比较、具备功能对应实现后才能开展的Defects4J跨语言比较、组件/调度/轮次消融、知识复用和指标评价。实际结果暂缓处理；9张已有表内容未变。轮次移除对照需要共同初始测试；Defects4J现有Java快照不充当Python数据。
- 已按图表要求清理最新图2、图4、图5内部解释性长句，保持模块、公式、节点和流程；局部处理之外像素无变化。图内说明的技术内容由正文对应段落承接。19个行间公式、117个行内数学表达、3个算法和10处图片链接检查通过。

## 2026-09-06 论文当前编辑口径

- 继续仅编辑《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》，不新建论文或Markdown文件。
- 五张方法图已直接替换为用户9月6日提供的裁剪版；顺序为总体概述、状态引导、状态智能体、知识智能体、生成智能体，图题和正文引用已同步。
- TSG仅表示测试状态引导机制，S_t为共享测试状态；论文正文不使用“共享TSG”“证据”“状态图”“门”等旧表达。
- 算法1为“状态引导”，描述当前状态决策与知识准备；算法2为“测试优化”，描述正式轮次及轮内生成、修复和优化；算法3为“知识检索”。生成智能体消费状态智能体组织的K_t，不主动发起检索。
- 第4章补充系统架构、核心模块与方法映射、任务运行、界面及系统验证。19个行间公式与9张实验表均保持原内容，数学语法及10处图片链接检查通过。用户最新要求暂不处理实验数据和结果分析，后续工作遵循此范围。

## 2026-09-04 论文唯一编辑版本（优先于旧记录）

- 唯一论文文件为《基于测试状态引导机制的多智能体协作测试代码智能生成方法_0904.md》，由已完成的修订稿原内容重命名。后续所有论文修改只在此文件进行；用户明确要求不再新建论文文件或Markdown文件。
- 原主论文及修改前备份已按用户要求移入回收站；旧修订稿已重命名，不再保留并行论文版本。其他项目说明不作为论文版本。
- 按四份新提示词，仅规范18个公式、3个算法与8张实验表，并清理不规范措辞；逐式数字及逐单元格数值/缺失值核对一致，保留系统章节和5张界面截图。
- 用户提供的5张新方法图已按授权完成本地裁剪和正文式文字清理，最终图片位于论文图片/方法框架/20260904，原图位于其原图子目录。裁剪保留区中，遮盖区域之外像素完全一致；未AI重绘技术内容。
- 代码与原文、新图的符号、根因候选域、检索过滤、Java并行和断点恢复粒度冲突，集中列在《论文修订核对_20260904.md》，未擅自修改技术定义或历史数据。相关定向测试7项通过。

> 当前有效架构为LangGraph运行底座、TSG、共享TSG以及测试状态、测试生成、测试知识三个顶层智能体。旧术语仅用于保留历史排错上下文；发生冲突时，以日期最新的记录为准。

## 2026-09-04 当前生效规则与验收边界

本次最终验收：完整回归173项、独立HTTP对话生命周期1项、前端6项均通过；生产构建与运行模块编译通过。证据位于.test_tmp/audit_20260904/，服务日志位于.monitoring/server_20260904_130201.*.log。后续不得把已知边界改写为“所有功能全部实现”；论文由另一窗口维护。

以下内容优先于本文中较早的历史条目；旧实验日志与原有成绩不追溯改写，论文由其他窗口维护，本次不修改论文及图片。

- HumanEvalJava：仅扁平独立Java类目录采用默认2槽、每任务独立工作区、每轮冻结知识视图；自动语言识别与显式java一致。有pom.xml/build.gradle的项目目录和直接单类入口不自动进入批量隔离路径。其他Java项目仍串行，Python仍按原规则执行。
- 同轮任务写入独立知识日志，轮末合并新记录及已有记录的验证增量；合并有幂等标识，任务中断后补合并尚未处理的日志，不把同轮其他任务的新知识引入当前生成。
- R1建立基线，R2只处理D/E，R3只处理C，不开启R4；保留最佳产物回退、原screening/full与A-E评价。不能声称PIT每轮绝对只调用一次：R3可能依据正式PIT反馈继续优化。
- 对话优先模型+知识，模型失败时知识自主检索回答，知识不足进入持久待学习队列。后台服务运行期间每15秒检查，失败后按1/2/4/8/16/30分钟退避；关闭服务期间不会自行运行，重启后读取队列。
- 知识自主回答是证据检索、规则判断和已知内容组合，不等同于另一个无需模型的通用推理模型。陌生概念不能因包含Java/测试等词而被误判为已知。
- 对话保留最近历史、已绑定任务和命中的知识；提示不会整体截断而丢掉检索结果。写入失败不得标为已学习。模型提供的新内容先进入正在学习，不直接当成已验证事实。
- 只有正式full测试证据参与掌握验证；同一证据不重复计数。未编译的Java候选不是负面经验。
- Java工具增加阶段心跳、超时进程树清理；暂时性超时不进入正式工具缓存。新增结果记录源码SHA256用于续跑核对。
- 本地HumanEvalJava仍为163份源码，参考规模164；数量缺口会标记完整性不足并阻止“完整数据集跳过”。本次不凭空补写基准源码，不改历史711项实验成绩。
- ValiFixTest独立执行器尚未接入，按钮已禁用并标注未接入。LangGraph保存节点检查点，但当前入口恢复主要为任务/轮次级，不能宣称任意节点无损恢复。
- 最新证据索引见《功能验收报告_2026-09-04.md》；外部电脑实际模型服务、全量基准成绩和加速比例不由本机模拟接口测试替代。

## 2026-08-26 旧TestEval知识迁移与系统助手

- 从`runs.rar`和旧代码压缩包直接读取报告与知识，没有把旧代码或运行目录解压到当前项目。旧TestEval共210项、耗时75807.32秒，A/B/C/D/E为17/75/109/7/2；131个任务进入后续轮次、累计144次尝试，仅19次改善。该结果证明旧版C级宽泛优化和D/E进入R3存在明显时间浪费。
- 旧知识822条经`import_historical_records`压缩为42类历史模式，并叠加批量统计和5类关键失败规避；`D:\wn\evo\test_knowledge_guidance.json`最终有92条底层记录。可视化归纳为36类知识：已掌握8、正在学习7、待验证7、应避免14。
- 五类专项规避为：禁止调用源码中不存在的方法；区分返回值函数与原地修改函数；C级候选失败后保留原通过测试且禁止整文件替换；AST无效时先由测试状态智能体重建状态；变异超时改用更小边界输入和独立预算。
- 用户本次明确要求在打包电脑的`D:\wn`准备可迁移知识，因此此前“当前电脑不创建外置目录”的约束被替代。应整体打包`D:\wn\evo`，目标电脑继续通过`--knowledge-store "D:\wn\evo\test_knowledge_guidance.json"`读取和追加。
- 测试知识智能体升级为系统助手：提示事实包含四层架构、入口命令、数据集族、语言工具链、作用范围、三智能体输入输出与能力、阶段、指标、并行/续跑规则、产物、前端页面和代码职责索引。无LLM时也能回答系统入口、实现位置、产物和实时报告；启用LLM时复用后台模型并继续把可验证测试概念写入长期知识。
- 多智能体协作台的三个节点升级为独立TSG能力终端，显示正式智能体名、专属通道、实时脉冲、能力证据进度、工作内容和状态事件；中等桌面宽度自动改为上下布局。1280宽度检查无节点重叠、无横向溢出、无控制台错误。

## 2026-08-25 三轮优先级、外置知识与实时并行展示

- 固定轮次已经落实到代码和论文算法：R1为全部任务建立基线；R2只对D/E依据失败原因和历史措施进行恢复，仍为D/E则结束并输出最终失败；R3只对所有C保留通过测试并执行最小目标定向优化，不创建R4。每个正式轮次内部最多进行3次状态定位修复或状态目标优化，并支持提前停止。
- C级R3必须把最大质量缺口、目标状态、未覆盖行、存活变异行和检索知识组成优化契约；启用LLM时使用专项提示约束局部修改、具体断言和不退化，未获得精确有效补丁时保留上一轮最佳测试，不进行启发式整文件重写。
- 正式实验电脑使用`--knowledge-store "D:\wn\evo\test_knowledge_guidance.json"`持久保存长期知识。首次运行可由项目内初始知识创建，已有外置文件只能读取和追加，不能被新代码包覆盖；迁移准备时可在打包电脑提前生成并携带该目录。
- Python并行是同一个数据集目录内最多4个独立源码任务并发推进，本地Ollama最多2个请求同时占用模型，其余任务仍可执行解析、测试框架和报告阶段。Java项目、项目内类和Java工具链保持串行。前端从`batch_runtime.json`实时展示工作槽、活动任务、排队任务和模型通道。
- 前端协作图以共享TSG和三个正式命名的智能体展示真实状态事件，不显示智能体编号、共享TSM或测试状态模型。面向用户统一使用“测试状态中心”；测试知识智能体的问答上下文包括系统身份、三智能体职责、轮次策略、语言范围、实验事实和长期知识，并复用后端相同模型配置。
- 论文算法1已改为与真实三轮分支一致的伪代码；D/E失败报告只可能对应R1或R2，R3只记录C级优化结果。

## 2026-08-24 三轮结果机制与单Java真实验证

- Java项目级数据集默认保持串行：不并行运行两个Java项目，也不把同一项目中的类并行送入共享构建目录。项目内依赖、Maven生命周期、JaCoCo数据文件和PIT进程都可能相互影响；5090主要加速大模型推理，并不能替代CPU、内存和磁盘上的Java构建评估。后续缩时优先采用构建合并、依赖与编译缓存、无效候选提前停止和PIT分层执行，同时保留最终完整评估。
- 前端实验总览固定展示开始时间、运行中逐秒增长的耗时和结束时间，实验目录列表同步显示各自耗时。时间必须取自进程真实启动和结束记录；等待任务不能使用登记时间冒充开始时间，中间轮次的结束不能冒充整个实验结束，同目录重跑必须清空旧时间。
- 缩时重构前，Python内置变异执行器对每个任务最多尝试20个变异体并串行执行；当时不存在“全局上限8”或“8个并行”的配置。后续展示必须始终把“每任务候选上限”和“并行任务数”分开命名。
- 2026-08-24起正式采用分层变异策略：Python内部筛查每任务最多8个高价值变异体、正式评估最多20个，全部串行；8个只用于规划，正式Mutation、TSQ、A-E分级和知识学习必须来自`full`报告。LangGraph新增最终完整评估节点，任何可用的`screening`报告在停止前都必须转入`full`。
- D/E在测试基线可运行前不执行覆盖和变异；C级统一在R3保留通过测试，并把上一轮存活变异行、未覆盖行和状态缺口带入定向优化。Java继续全局串行，JUnit与JaCoCo合并为一次构建，PIT延后到稳定候选。
- 任务目录新增`evaluation_cache.json`，缓存键由语言、评估阶段、源码、测试、依赖配置和执行设置共同计算；任一内容变化即失效。缓存属于可删除实验加速数据，不属于测试知识引导库。
- 该阶段曾采用伪代码块和数学源码块排版；2026-08-31参考用户既有ValiFixTest期刊稿后已替换为最新规范：居中公式右编号、算法输入/输出/编号步骤表、表题在上和结果表后论述。
- 论文已新增独立的“单元测试多智能体协作可视化系统”章节，必须使用真实后端数据截图并说明前端不参与智能体决策。论文图片统一保存在`论文图片/系统界面`。
- 当前简单Java样本在不调用LLM时端到端仍耗时63.52秒。主要优化方向是合并JUnit与JaCoCo的重复Maven测试、取消默认`-U`更新检查、按源码/测试/依赖哈希缓存评估结果，以及仅在必要节点和最终候选运行完整PIT；最终报告必须保留一次真实完整质量评估。
- 报告口径补充：批量总览中的覆盖、SFC、AE、变异和TSQ是所有有效任务的平均值；单任务HTML必须展示该任务自身实际值并明确注明“非批量平均值”。`unique_tasks`已改名为`followup_task_count`，中文显示“进入后续轮次的任务数”；累计执行次数另行统计。
- 顶层智能体固定为测试状态智能体、测试生成智能体和测试知识智能体。测试状态智能体负责共享TSG检查、自修复、质量评估、A-E分级和下一任务规划；测试生成智能体负责生成、执行、验证、修复与优化；测试知识智能体负责每轮执行前检索和执行后学习。验证证据可以用于评估，但最终评估属于测试状态智能体，且不存在第四个顶层智能体。
- 结果口径固定为A/B/C/D/E：A高质量、B良好、C可用均为有效完整结果；D无效、E未完成进入失败统计。总任务数=A+B+C+D+E，完整执行数=A+B+C。
- 正式批量实验最多三轮：R1运行全部任务；R2只恢复D/E，未恢复的D/E直接形成最终失败；R3统一优化全部C，包括R1的C和R2恢复得到的C。每轮均先读取知识，不开启R4。
- D/E必须在任务报告和总报告中显示具体失败原因、此前解决措施、知识引用和失败报告链接，不能只给出失败数量。
- D/E失败证据必须在每一轮结束后立即固化，不能等待最终轮次或整批结束。工作流摘要要保留`last_error`和候选验证证据；测试状态智能体完成归因后，测试知识智能体立即记录“应避免”经验，下一轮按同一任务和失败类别检索应用。
- 跨实验经验独立保存在`knowledge_store/test_knowledge_guidance.json`；“测试知识引导库”可视化保存在`knowledge_store/test_knowledge_guidance_library.html`。删除某次运行目录不能删除该知识目录。
- 完整数据集目录使用`dataset_completion.json`判定是否可整体跳过。只有全部应处理任务均为A/B/C才可在遍历源码前跳过；缺少一个任务或存在D/E都只能续跑未解决项。
- 为Java结构化生成补充了具体数值断言、范围边界和异常类型断言。`examples/sample_java/SimpleCalculator.java`真实端到端结果为`B·R1`：JUnit全部通过，行/分支覆盖率100%，AE 92.75%，PIT Mutation 71.43%，TSQ 92.84%。Mutation未达A级但整体质量有效，因此按B保留。
- 本次验证确认Java结构读取、共享TSG、状态流、测试生成、JUnit、JaCoCo、PIT、分级报告、完成标记和知识回写链路连通。只运行了一个Java示例，不能据此声称Python和Java全量数据集已经完成实验。
- 最终职责证据：报告中`validation_owner=TestGenerationAgent`、`result_evaluation_owner=TestStateAgent`；`ValidationPassed`事件方向为测试生成智能体到测试状态智能体，`EvaluationFeedback`方向为测试状态智能体到测试生成智能体。快速相关回归24项通过，随后同一数据集续跑返回`dataset_skipped=true`。

## 2026-08-12 Java 项目扫描与冗余目录清理

- 复查“Java 项目无法生成对应测试”的真实风险：单个 Java 文件和内置 `java_workspace` 链路可运行，但整 Java 项目作为输入时，批量发现会递归扫描所有 `.java`，没有排除 `src/test/java`、`target`、`build`、旧生成输出等非主源码目录，容易把已有测试、构建产物或历史生成文件误当作被测源码，导致智能体目标混乱、生成结果不可用。
- 修复 `batch_generate_tests.py` 的 Java 源码发现规则：Java 批量扫描现在跳过测试源码目录、构建输出目录、缓存目录和本工具历史生成目录，只保留真正主源码作为测试生成目标。
- 新增回归测试 `test_batch_java_project_discovery_skips_tests_and_build_outputs`，确认整 Java 项目扫描时只选择 `src/main/java` 下的业务源码，并记录跳过原因 `java_non_main_source`。
- 清理当前项目目录下历史自检输出、报告检查目录、pytest/Python 缓存目录；清空 `java_workspace/src/main/java`、`java_workspace/src/test/java` 和 `java_workspace/target` 的运行残留，仅保留 `java_workspace/pom.xml` 与空源码/测试目录结构，避免后续运行读取旧文件。
- 验证：`python -m pytest tests\test_batch_generate_tests.py tests\test_structured_fusion.py -q` 通过 30 项；`java_workspace` 清理后只剩 POM 和空 `src/main/java`、`src/test/java` 目录。

## 2026-08-01 断点续跑失败任务重跑修复

- 修复批量续跑策略：现在只复用成功完成的任务结果，`valid_usable`、`quality_acceptable`、`quality_high` 会跳过；`execution_invalid`、`generation_failed`、`failed` 以及不完整状态不会再被当作可复用结果。
- 这样再次运行 `python -B run_all_datasets.py --llm` 时，成功任务不会受影响，旧失败任务会自动重新进入生成、验证和修复流程。
- 新增回归测试确认旧 `execution_invalid` 记录会重跑，旧成功记录仍会跳过。
- 验证：`tests/test_batch_generate_tests.py tests/test_structured_fusion.py tests/test_run_all_datasets.py` 共 25 项通过；pytest 缓存写入警告仍为本机权限噪声。

## 2026-08-01 一键全量实验交付打包

- 用户要求不再手动替换单个文件，改为重新打完整交付包。
- 打包范围应包含源码包、`datasets/`、`java_workspace/`、测试、运行说明、项目记忆、长期经验库和项目配置。
- 打包时应排除旧生成结果、`_codex_*` 自检目录、`.package_*` 暂存目录、`generated_tests_*`、缓存、覆盖率临时文件和旧压缩包。
- 当前推荐运行命令保持唯一主入口：`python -B run_all_datasets.py --llm`。
- 已生成交付包 `My_Mutil_Agent_all_datasets_ready_20260801_002122.zip`；包内检查包含 TestEval、HumanEval、Defects4J、`java_workspace` 和主入口，排除旧输出与缓存；解压后关键测试 24 项通过。

## 2026-07-31 一键运行全部数据集入口与内置 Java 覆盖率工作区

- 新增 `java_workspace/` 内置 Maven 执行模板，包含 JUnit、Surefire、JaCoCo、PIT 配置。Java 数据集目录不需要自己是完整工程，运行时会自动把当前数据集源码同步到 `java_workspace/src/main/java`，把生成的测试同步到 `java_workspace/src/test/java`。
- Java 执行链已改为优先使用真实工程根；若数据集目录没有构建文件，则自动使用项目内置 `java_workspace`。执行前会清理旧的测试、class、JaCoCo 和 PIT 输出，避免误读历史覆盖率。
- `run_all_datasets.py` 默认执行 Java JUnit/JaCoCo/PIT，用户只需运行 `python -B run_all_datasets.py --llm` 即可启动全部 Python 和 Java 数据集实验；如临时只想生成 Java 测试，可用 `--skip-java-execution`。
- `JacocoRunner` 支持按当前测试类运行 `mvn -q -Dtest=... test jacoco:report`，并记录 `target/site/jacoco/jacoco.csv` 路径。
- 更新 `另一台电脑运行说明.md`，删除“Java 数据集必须是完整 Maven/Gradle 工程”的描述，明确项目内置 `java_workspace` 必须随代码一起拷贝。

## 2026-07-31 一键运行全部数据集入口

- 新增 `run_all_datasets.py`，用户只需执行 `python -B run_all_datasets.py --llm`，即可自动依次处理 `datasets/TestEval`、`datasets/HumanEval`、`datasets/Defects4J/cli`、`datasets/Defects4J/csv`、`datasets/Defects4J/lang3`、`datasets/Defects4J/gson`。
- Python 数据集默认执行生成、pytest、coverage 和变异测试；Java 数据集通过内置 `java_workspace` 执行生成、JUnit、JaCoCo 和 PIT。
- 一键入口复用原有断点续跑机制，默认读取每个任务目录中的 `stateflow_summary.json`，再次输入同一条命令会跳过已完成任务并继续未完成任务；需要强制重跑可加 `--rerun-existing`。
- 新增全部数据集总记录和总览页面：`runs/all_datasets_summary.json` 与 `runs/all_datasets_report.html`；每个数据集目录仍保留自己的 `batch_summary.json` 和 `batch_quality_report.html`。
- 更新 `另一台电脑运行说明.md`，把一键运行命令作为推荐方式，并继续明确“本机已部署 DeepSeek，直接使用原有本地模型调用配置”，不再出现下载 Ollama 或下载模型说明。

## 2026-07-31 按本地 DeepSeek 环境修正安装与运行说明

- 根据用户确认，另一台电脑已经部署好本地 DeepSeek，项目应沿用原有本地模型调用配置，不应在说明中引导下载或安装 Ollama/DeepSeek。
- 保留 `pyproject.toml` 的包发现修复：显式安装代码包，排除 `datasets`、`examples`、`tests`，解决 `pip install -e .` 的 Multiple top-level packages 报错。
- 删除 `pyproject.toml` 中的 `ollama` 可选依赖配置，避免安装流程出现无关依赖提示；代码仍按原逻辑在运行时调用本地模型服务。
- 重写 `另一台电脑运行说明.md`，明确“使用已部署好的本地 DeepSeek，只需确认服务地址和模型名一致”，不再出现下载或安装 Ollama/模型的步骤。
- 验证：`pyproject.toml` 可解析；关键词检查确认不再包含下载/安装 Ollama 或模型的说明；关键测试 `tests/test_batch_generate_tests.py tests/test_structured_fusion.py` 共 21 项通过。

## 2026-07-31 安装配置修复

- 用户在另一台电脑执行 `python -m pip install -e .` 时触发 setuptools 报错：平铺目录中存在多个顶层包，自动发现无法判断应安装哪些目录。
- 已修复 `pyproject.toml`：显式声明只安装 `agents`、`analysis`、`core`、`executor`、`graph`、`memory`、`metrics`、`repair`、`validation`、`visualization` 等代码包，排除 `datasets`、`examples`、`tests`。
- 将本地模型相关包从基础依赖中移出，避免离线或内网机器在基础安装时受无关依赖影响；不使用大模型时只需要 `pytest` 和 `coverage`。
- 更新 `另一台电脑运行说明.md`：基础安装用 `python -m pip install -e .`，需要 Ollama 时再执行 `python -m pip install -e ".[llm]"`；离线且依赖已存在时可使用 `--no-build-isolation`。
- 验证：`pyproject.toml` 可解析；editable 包构建阶段成功，原“Multiple top-level packages”错误已消失。本机最终安装写入用户目录时因权限被拒绝，这是本机环境权限问题，不是项目配置问题。关键测试 `tests/test_batch_generate_tests.py tests/test_structured_fusion.py` 共 21 项通过。

## 2026-07-31 项目与数据集交付打包

- 新增 `另一台电脑运行说明.md`，说明 Python/Java 环境准备、大模型配置、单文件运行、Python 数据集批量运行、Java 数据集生成/执行要求、中断续跑、报告位置、终端指标和快速自检命令。
- 生成交付压缩包 `My_Mutil_Agent_with_datasets_clean_20260731_223513.zip`，包内顶层目录为 `My_Mutil_Agent/`。
- 交付包包含源码目录、测试目录、`datasets/` 数据集、核心说明文档、数据集兼容性文档、运行说明、长期经验库和项目配置。
- 交付包不包含历史生成结果、`__pycache__`、pytest 缓存、旧压缩包和临时验证输出；包内检查 `bad_count=0`。
- 包内数据确认：Python 数据集 374 个 `.py` 文件，Java 数据集 189 个 `.java` 文件，测试文件 3 个；包内可导入 `main` 与 `batch_generate_tests`，可收集 59 项测试。
- 说明：在 clean 包目录内运行 21 项快速测试时已输出 21 个通过点，但工具等待完整摘要时超时；随后确认包内导入和测试收集正常，残留 Python 子进程已自动结束。

## 2026-07-31 最终流程复查与断点续跑补强

- 根据用户再次要求，对“终端命令可执行、数据集可用、未知中止后可续跑、每个任务有记录和可视化、终端输出总指标”做最终复查。
- 修复批量入口默认偏 Python 的问题：`generate_tests_for_directory` 现在支持 `language=auto/python/java/all`，普通 Python 文件夹会自动处理 `*.py`，HumanEval/TestEval 任务命名仍优先按 `task_` / `TestEvatask_` 排序，Java 目录会自动使用 `*.java`。
- 修复 Java 项目目录扫描不完整的问题：Java 自动模式现在默认递归扫描，Defects4J 这类带包目录的源码树不需要额外手动加 `--recursive`。
- 新增断点续跑机制：批量命令默认读取每个任务目录的 `stateflow_summary.json`，已完成任务会显示 `resumed_existing` 并跳过；需要重跑时可使用 `--rerun-existing`，不想复用时可用 `--no-resume`。单文件 `main.py` 也默认复用已有完整结果。
- 新增失败任务本地记录：任务抛异常时也会在该任务目录写入 `stateflow_summary.json` 和 `task_failure_report.html`，保证失败任务也有可检查记录和页面。
- 改进终端输出：`batch_generate_tests.py` 现在输出 discovered/selected/resumed/succeeded/failed/incomplete、重试摘要和平均行覆盖、分支覆盖、SFC、AE、变异、SFQ；`main.py` 默认输出简洁指标摘要，完整 JSON 仍写入 `stateflow_summary.json`，需要打印完整内容可加 `--full-json`。
- 真实命令验证：普通 Python 文件夹 `examples` 生成成功，二次运行显示 `resumed_existing=1`；Java 数据集 `datasets/Defects4J/cli` 自动识别为 `*.java`、递归发现 34 个文件，limit 1 生成成功，二次运行显示 `resumed_existing=1`；单文件 `examples/sample_target.py` 执行成功，二次运行显示 `resumed_existing=true`。
- 数据集静态验证：TestEval 210 个 Python 文件 + HumanEval 164 个 Python 文件全部 AST 解析通过；Defects4J cli/csv/lang3/gson 共 189 个 Java 文件全部结构化抽取与状态图构建通过，共 24475 个图节点。
- 测试验证：`pytest --collect-only` 收集 59 项；`tests/test_batch_generate_tests.py` 与 `tests/test_structured_fusion.py` 共 21 项通过。pytest 缓存写入权限警告仍为环境噪声，不影响执行结果。

## 2026-08-02 Java 执行异常、命名规则与未达标优化修复

- 2026-08-03 再次按用户要求做 Maven 可用性与方法可行性复查：确认代码路径为 `ExecutionAgent` 准备 `java_workspace/src/main/java` 与 `src/test/java`，`JavaToolchain` 先 JUnit，再 JaCoCo，再 PIT，三者均通过解析到的真实 Maven 命令执行。补强 `java_workspace/pom.xml` 的 PIT 目标包配置，覆盖 `cli.help`、`lang3.concurrent`、`gson.internal.bind` 等子包；`PitRunner` 新增 `target/pit-reports/**/mutations.xml` 兜底解析，避免只依赖终端输出导致变异分数缺失。
- 修正“能跑但未达标”的硬失败口径：`Thresholds.usable()` 现在只判断测试是否真实可执行、是否收集到测试、是否未超时；覆盖率、断言、变异不足进入 `valid_usable` 弱成功，并由 `weak_quality_flags`/`threshold_failures` 解释弱覆盖、弱断言、弱变异或指标不可用。批量状态不再用 `plateau_reached` 覆盖已通过运行的弱成功结果。
- 验证：针对性测试 `tests/test_batch_generate_tests.py`、`tests/test_structured_fusion.py`、`tests/test_run_all_datasets.py` 共 30 项通过；13 个改动相关 Python 文件 AST 解析通过；`java_workspace/pom.xml` XML 解析通过；PIT XML 兜底解析样例返回 0.5。
- 2026-08-03 追加：根据用户实验机部署方式，Java 工具链解析已内置固定 Maven 路径 `D:\wn\mavenEvo\apache-maven-3.9.1\bin\mvn.cmd`。另一台电脑只要把 Maven 解压到 `D:\wn\mavenEvo\apache-maven-3.9.1`，无需配置系统 PATH 或环境变量，Python 智能体也会优先使用该 Maven。环境变量、MAVEN_HOME、项目本地 Maven 和 PATH 仍作为备用。
- 2026-08-03 复核用户反馈后，进一步修正 Maven 执行语义：代码不再返回一个可能不存在的 `mvn.cmd` 让 subprocess 碰运气，而是先解析真实 Maven 命令；若 `mvnw/MAVEN_CMD/MAVEN_HOME/M2_HOME/项目本地 apache-maven/PATH` 均不可用，则明确记录为 Java 工具链不可执行，不再伪装成已运行 JaCoCo/PIT。
- Java 工具链缺失属于环境阻断失败，主流程会直接标记失败并写入 `failure_location=java_toolchain` 和 `repair_failure_reason`，不会让智能体反复修测试代码来掩盖 Maven 不可用。
- 根据用户最新口径，`valid_usable` 改回成功类别，但属于“弱成功”：终端、批量 JSON、总数据集 JSON 会额外统计 `weak_success`，每个任务报告会写 `weak_quality_flags` 和 `threshold_failures`，用于说明弱覆盖率、弱断言、弱变异或指标不可用等未达标原因。
- 未达标任务在单任务内部仍会先进入 optimize 轮次；若优化/修复轮次结束后测试仍能运行但达不到阈值，最终状态为 `valid_usable`，计为成功但保留弱项原因。若修复后仍不可运行，仍标记失败，并保留失败阶段、失败位置和修复失败原因。
- 验证：针对性测试 `tests/test_structured_fusion.py`、`tests/test_batch_generate_tests.py`、`tests/test_run_all_datasets.py` 共 28 项通过；52 个核心源码文件 AST 解析通过。

- 根据用户提供的 `Util` Java 任务样例定位到两层问题：一是 Java 执行环境中 `mvn.cmd` 未被 Python 进程找到，导致 JUnit 未运行，JaCoCo/PIT 没有机会生成指标；二是 Java 结构化分析把 `public static boolean`、`final Object[]` 等签名解析不准，生成了不可编译的 `new final object[]{}` 和错误的 `new Util().isEmpty(...)`。
- 修复 Java 测试命名规则：`JavaJUnitTestRenderer` 现在统一生成“源文件名 + Test.java”，例如 `Util.java -> UtilTest.java`，并保持测试类名一致，便于 Maven/JUnit/JaCoCo/PIT 识别。
- 修复 Java 结构化分析：方法可见性、`static` 修饰符、返回类型、构造方法、`final` 参数和数组类型会被清洗成稳定结构化上下文，不再把参数修饰符带入默认值生成。
- 增强 Java 工具链命令查找：JUnit、JaCoCo、PIT 统一使用 `executor/build_tools.py`，按 `mvnw/mvnw.cmd`、`MAVEN_CMD/MVN_CMD`、`MAVEN_HOME/M2_HOME`、PATH 的顺序寻找 Maven；找不到时会明确记录为 `java_tool_missing` 或 `java_execution_unavailable`。
- Java 执行失败时，`failed_tests`、`execution_diagnosis` 和状态流现在会携带 Maven/JUnit 命令、错误摘要、测试放置路径和建议焦点，后续规划与执行模块可以据此修复或优化，而不是只看到笼统失败。
- 修复失败/未达标决策：失败任务进入 repair，已可运行但未达到质量阈值的任务进入 optimize；`valid_usable` 不再计为真正成功，也不再作为可复用终态，只有 `quality_acceptable` 和 `quality_high` 会被续跑直接跳过。
- Java 修复/优化现在作为 `ExecutionAgent` 内部模块处理，不新增第四个主智能体；规划智能体通过状态流决定修复或优化方向，执行智能体对 Java 测试做最小清洗或调用本地 DeepSeek 生成完整 JUnit 5 优化版本。
- 经验保留机制确认：每个数据集输出目录会写 `agent_experience.json`，项目根目录会同步写长期 `agent_knowledge_base.json`。如果只删除某次生成的测试文件夹，长期经验仍保留；若删除整个 `runs/某数据集`，只要项目根目录的 `agent_knowledge_base.json` 还在，后续智能体仍可读取历史失败和策略经验。
- 验证：`tests/test_structured_fusion.py`、`tests/test_batch_generate_tests.py`、`tests/test_run_all_datasets.py` 以及状态流调度关键用例共 28 项通过；50 个核心 Python 文件 AST 解析通过。临时 Java 批量生成检查确认 `datasets/Defects4J/cli` 会生成 `AbstractHelpFormatterTest.java`。`compileall` 因本机 `__pycache__` 写权限被拒绝，已改用不写 pyc 的 AST 检查通过。

## 2026-07-31 数据集兼容性复查与 Java 执行分流修复

- 再次复查 Java/Python 统一流程时，发现 Java 测试生成后执行阶段可能仍误走 Python pytest 路径；已在 `ExecutionAgent` 中补齐 Java 分流：Java 图或 `.java` 测试文件会进入 `JavaToolchain`，并自动寻找 Maven/Gradle 项目根目录。
- 补齐 Java 真实执行前的测试放置步骤：生成在输出目录中的 JUnit 测试会按 `package` 自动复制到 Java 项目的 `src/test/java/...` 下，避免 Maven/Gradle 找不到测试类。
- 历史记录：此前 Java 数据集目录只有源码时会记录 `java_execution_unavailable`；现在已由内置 `java_workspace` 替代，Defects4J 源码目录可通过项目内置 Maven 模板执行 JUnit/JaCoCo/PIT。
- 修复批量失败任务自动重跑时漏传 `retry_rounds` 的问题，避免超时任务二次运行时参数不完整。
- 批量发现入口已支持 `*.java`，Java 文件会先做结构化抽取和上下文重写，再判断是否存在可测试类、方法或构造器。
- 数据集静态兼容检查：`datasets/TestEval` 210 个 Python 文件、`datasets/HumanEval` 164 个 Python 文件全部可被 AST 解析；`datasets/Defects4J/cli` 34 个、`csv` 19 个、`lang3` 56 个、`gson` 80 个 Java 文件全部可完成结构化抽取和状态图构建，共生成 24475 个图节点。
- 验证：语法检查通过；`tests/test_structured_fusion.py` 与 `tests/test_batch_generate_tests.py` 共 15 项通过；`test_all_agents_can_use_llm_path`、`test_system_iterates_by_quality_thresholds_with_llm` 和末尾关键规划/执行测试单独通过。完整 `test_stateflow_system.py` 组合运行因耗时累计触发工具超时，但已拆分验证关键长耗时项通过。pytest 缓存写入权限警告仍存在，不影响测试结果。

## 2026-07-31 状态流核心缺陷修复

- 修复覆盖率运行不稳定问题：`CoverageRunner` 现在为每次运行使用独立 coverage 数据文件，并按目标源码真实路径匹配 coverage JSON 条目，避免 pytest 已通过却因覆盖率文件冲突或路径误配被判为 `coverage_valid=false`。
- 补齐类方法证据链：`RuntimeObserver` 和 `GraphUpdater` 已能识别 `target.Solution().method(...)` 形式的调用，运行观察会记录 `class_name`，图更新不再把类构造器误当作被测函数。
- 修复类方法补丁生成：根因最小修复和 Oracle 修复会根据函数节点的类作用域生成 `target.ClassName().method(...)`，并跳过 `self` 参数。
- 收紧测试目标筛选：生成阶段只接受模块顶层公开函数和公开类的一层公开方法，避免把函数内部局部函数误判为可测试 API。
- 改进补丁验收：Patch Search 在判断候选补丁时增加行覆盖、分支覆盖不退步检查。
- 改进经验库写入：经验记录写入失败时不再中断主流程，而是尽力写入可用路径并记录写入错误。
- 修正批量统计口径：`plateau_reached`、`metric_unreliable`、`budget_limited` 不再计入成功数，单独统计为 incomplete。
- 修正旧变异指标：没有有效变异体时返回 `None`，与主流程的 Mutation N/A 语义保持一致。
- 新增回归测试覆盖动态加载源码覆盖率、类方法运行观察、类方法断言归因、跳过内部函数、批量 incomplete 统计和 Mutation N/A。
- 验证：新增/受影响测试 6 项通过；批量模块测试 6 项通过；核心链路测试 4 项通过；12 个受影响源码文件 AST 语法检查通过。示例主流程 `examples/sample_target.py` 无 LLM 运行后 pytest 通过、`coverage_valid=true`、可视化生成成功，最终状态为 `valid_usable`。

## 2026-07-31 缺陷复核与边界加固

- 复核旧缺陷后，补强两个边界：Patch Search 现在会把行覆盖或分支覆盖提升视为有效改进，不再只看状态流质量、变异分数和状态流覆盖；GraphUpdater 对同名类方法按“类名 + 方法名”归因，避免 `A.value()` 和 `B.value()` 这类场景互相混淆。
- 新增回归测试覆盖同名类方法归因和“仅覆盖率提升”的补丁验收。
- 完整验证：`python -m pytest -q` 通过 44 项；示例主流程 `examples/sample_target.py` 无 LLM 运行成功，pytest 通过、覆盖率有效、变异分数有效、可视化文件生成，最终状态为 `valid_usable`。
- 已知环境噪声：pytest 缓存目录 `.pytest_cache` 写入受权限限制，会显示警告，但不影响测试执行结果；`py_compile` 因同样的 `__pycache__` 写权限限制不能写 `.pyc`，已用 AST 语法解析替代检查并通过。

## 2026-07-31 Java/Python 统一状态流融合架构

- 新增 `StructuredContext` 统一中间表示，Python 和 Java 源码先经过结构化抽取与上下文重写，再由 `StructuredGraphBuilder` 构建初始状态图；真实源码不被重写。
- 新增 `StateFlowEvent`，将 ContextExtracted、InitialGraphBuilt、TestIntentPlanned、ExecutionObserved、CoverageObserved、MutationObserved、ValidationPassed/Failed、RepairScopeLocated、RepairApplied、EvaluationFeedback、CompletenessReached 等信号写入状态图。
- 将动态验证机制并入 EvaluationAgent，新增规则、逻辑、可行性、覆盖、修复闭环五类验证，并把结果写入 `last_validation` 和状态流。
- 新增完整状态图指标 CSG：综合 SFC、DVS、AE、MS、TIC、RCS，要求无未解决验证错误、目标状态被覆盖且修复不退步。
- 新增最小目标修复模块，ExecutionAgent 会根据验证错误、失败测试、修复建议定位 assertion/line/test-method/test-class/file 五级修复范围，并用收益/成本比记录修复决策。
- 新增 Java/Python 工具链接口：Python 复用 pytest、coverage.py、mutation runner；Java 提供 JUnit 5 测试生成、Maven/Gradle 自动识别、JaCoCo、PIT 统一执行接口。
- 数据集已复制到项目内 `datasets/`：TestEval 210 个 Python 文件，HumanEval 164 个 Python 文件，Defects4J 的 cli/csv/lang3/gson 四个 Java 项目；清单见 `datasets/DATASET_MANIFEST.md`。
- 生成确认文档 `项目核心功能_方法与创新点确认.md`，集中说明核心功能、方法流程、三智能体边界、三点融合、完整状态图指标和创新点。
- 验证：新增融合测试 `tests/test_structured_fusion.py` 6 项通过；关键回归组合 16 项通过；完整 50 项测试因单次运行超过工具时限，已按分组全部跑完并通过。样例主流程 `examples/sample_target.py` 在 `_codex_final_fusion_run` 中运行成功，生成测试、覆盖、变异、状态图和可视化均完成。已修复结构化构图切换后异步函数和生成器函数元数据丢失导致的测试生成回归。

## 2026-07-21 批量数据集任务名前缀兼容

- 在 `batch_generate_tests.py` 中新增统一变量 `DEFAULT_TASK_PREFIXES`，当前支持 `task_` 和 `TestEvatask_` 两类任务文件名前缀。
- 批量发现源码文件时，不再只认命令行默认的 `task_*.py`，也会自动接受由 `DEFAULT_TASK_PREFIXES` 派生出的任务文件名，例如 `TestEvatask_000.py`。
- 排序逻辑同步改为读取 `DEFAULT_TASK_PREFIXES`，确保新增前缀的任务也能按编号排序。
- 快速自检：`task_000.py` 和 `TestEvatask_000.py` 均能匹配，`other_000.py` 不匹配；完整 pytest 因用户要求停止，未完成。

## 2026-07-21 sourcesTestEval 类方法数据集兼容

- 确认 `sourcesTestEval` 文件名前缀为 `TestEvatask_`，但文件内容是 `class Solution` 下的方法，没有顶层公开函数；原批量筛选只认顶层函数，因此 210 个文件全部跳过。
- `batch_generate_tests.py` 的 `_has_public_testable_function` 已放宽为支持顶层公开函数和公开类中的公开方法。
- `agents/generation_agent.py` 已把一层类作用域的方法纳入测试目标，并记录 `class_name`，观察执行时调用 `Solution().method(...)`。
- `agents/test_plan.py` 渲染测试时支持类方法，生成 `target.Solution().method(...)` 形式的调用。
- `run_agent.py` 简短输出保留 `skipped_reason_counts` 和 `skipped_examples`，便于继续诊断其他数据集。
- 自检：对本机 `sourcesTestEval` 发现 210 个文件、选中 210 个、跳过 0 个；`TestEvatask_000.py` 可生成 `target.Solution().findMedianSortedArrays(...)` 测试；`batch_generate_tests.py --limit 1` 生成成功。

## 2026-07-22 长期经验库与批量总报告修复

- 修复经验只跟随生成目录的问题：`AgentExperienceMemory` 现在在项目内运行时会同时读写当前输出目录的 `agent_experience.json` 和项目根目录的长期知识库 `agent_knowledge_base.json`。删除某次生成结果目录后，后续运行仍能读取长期经验。
- 批量经验新增超时归因：记录 `timeout_tasks` 和 `mutation_timeout_tasks`，并把“使用小输入、短序列、规避长循环/深递归/外部等待”等策略写入下一轮 `batch_guidance`。
- 批量 summary 新增 `duration_seconds`，总报告页面展示开始时间、结束时间和运行时长。
- 批量 HTML 总报告将“已执行任务明细”和“未执行或未通过任务”拆成两张表，避免失败任务和成功任务混在一起；表头增加不换行样式，改善长短不一导致的排版问题。
- 验证：相关测试 `python -m pytest tests/test_batch_generate_tests.py tests/test_stateflow_system.py::test_batch_experience_guides_next_generation -q` 通过 5 项；AST 语法检查覆盖 `graph/experience_memory.py`、`batch_generate_tests.py`、`visualization/graph_visualizer.py`、`run_agent.py`、`main.py`。`py_compile` 因 Windows 拒绝写入 `__pycache__` 未完成，改用 AST 检查确认源码语法正常。

## 2026-07-22 失败任务经验学习后自动重跑

- 批量执行现在支持“先学习、再重跑”：第一轮完成后先写入当前经验和长期知识库，再自动挑选失败或超时任务重跑一轮。
- 默认自动重跑 1 轮，可通过 `--retry-failed-rounds 0` 关闭，也可调高轮数。
- 重跑对象包括 `failed`、`generation_failed`、`execution_invalid`，以及报告中 `timed_out=True` 或 `failure_category=timeout` 的任务；已成功任务不会重复跑。
- 总报告和控制台摘要新增 `retry_summary` / `retry_history`，展示自动重试轮数、重试任务数和重试恢复数。
- 验证：新增测试覆盖第一次超时失败、写入经验后自动重跑并恢复成功；相关 6 项测试通过，AST 语法检查通过。
## 2026-07-31 PDF 论文读取与 Java 迁移要点

- 用户授权安装 `pypdf` 后，已在工作区本地 `.codex_deps/pypdf_vendor` 安装，并将 `D:\新\An Automated Unit Test Generation Method_0706.pdf` 复制为 `paper_java_method_0706.pdf`，提取全文为 `paper_java_method_0706_extracted.txt`。
- 论文方法名为 ValiFixTest，核心包括：结构化 Java 代码分析与提示上下文重写、LLM 生成 JUnit 5 测试、动态验证机制、最小目标修复策略、PreserveMaxCorrect 失败保留机制。
- 论文实验数据对象为 Defects4J 中 Commons-Cli 1.6.0、Commons-Csv 1.10.0、Commons-Lang 3.1.0、Gson 2.10.1；工具链为 JDK 11、Maven 3.8.6、JaCoCo 0.8.8、PIT 1.15.0、JUnit 5.13.4、Maven Surefire 3.0.0-M7。
- 当前项目若迁移到 Java，应保留状态图/状态流/三智能体闭环，新增 Java 语言适配层、Java 状态图构建器、JUnit 渲染器、Maven/JUnit 执行器、JaCoCo 覆盖采集、PIT 变异采集和 Java 最小目标修复模块。

## 2026-08-03 经验记忆机制增强

- 已确认用户另一台实验电脑的 Maven 部署路径为 `D:\wn\mavenEvo\apache-maven-3.9.1\bin`，代码内 Maven 解析逻辑会直接寻找 `D:\wn\mavenEvo\apache-maven-3.9.1\bin\mvn.cmd`，不依赖系统 PATH。
- 增强 `graph/experience_memory.py`：经验记录不再只是保存历史指标，新增 `learning_notes` 和 `prompt_reminders`，把失败、弱成功、优化无收益等情况转成“原因类型、原因说明、解决方案、下次提醒”。
- 失败经验现在会归类并给出修复方向，包括语法错误、导入错误、测试未收集、oracle 错误、运行错误、超时、异常-only、弱断言、Java Maven/JUnit/JaCoCo/PIT 工具链不可用、Java 编译执行失败等。
- 优化经验现在会记录可运行但未达强阈值的原因，例如弱行覆盖、弱分支覆盖、弱状态流覆盖、弱断言有效性、弱变异分数、覆盖/变异不可用，并给出下一轮优化建议。
- 批量经验现在会统计 `weak_quality_flag_counts`、`threshold_gap_counts`、`weak_success_tasks`，使 `valid_usable` 这类“能跑但质量弱”的结果进入经验学习，而不是只当普通成功。
- `generation_agent.py`、`repair_agent.py`、`evaluation_agent.py` 已把 `actionable_lessons`、`failure_reminders`、`optimization_reminders` 注入大模型提示词，下一轮生成、修复和评估都会看到这些历史注意事项。
- 验证：新增经验学习测试 3 项通过；批量弱成功/恢复/Maven 关键回归测试 6 项通过；修改文件 AST 语法检查 5 项通过。一次较大的组合测试运行到 17 项通过后触发工具超时，未显示失败，已用更小的关键集合替代验证。

## 2026-08-03 Maven 3.9.1 与 Java 指标链路复查

- 用户确认实验电脑环境：Maven home 为 `D:\wn\mavenEvo\apache-maven-3.9.1`，JDK 为 `D:\wn\jdk17.0.19`，`mvn -version` 可正常输出 Apache Maven 3.9.1。
- 复查 `executor/build_tools.py` 后确认：代码会直接查找 `D:\wn\mavenEvo\apache-maven-3.9.1\bin\mvn.cmd`，该路径与实验电脑 Maven home 匹配；即使系统 PATH 不参与，也能由 Python 智能体调用 Maven。
- 发现并修复 Java 工作区 POM 的真实风险：Gson 数据集里有源码引用 `com.google.gson.stream.JsonReader/JsonWriter` 和 `com.google.gson.internal.GsonBuildConfig`，原 POM 未声明 `com.google.code.gson:gson:2.10.1`，可能导致 Gson 数据集编译失败、JaCoCo/PIT 无效。现已在 `java_workspace/pom.xml` 补入该依赖。
- 补强 PIT 深层测试匹配：`targetTests` 现在覆盖 `cli.*.*.*Test`、`csv.*.*.*Test`、`lang3.*.*.*.*Test`、`gson.*.*.*.*Test`，避免深层包测试类不被 PIT 识别。
- 验证：`java_workspace/pom.xml` XML 解析通过；Java 关键回归测试 5 项通过；经验学习和批量弱成功相关测试 4 项通过；全项目 Python 源码 AST 语法检查通过 2992 个文件。未跑全量数据集，遵循用户要求避免耗时实验。

## 2026-08-20 Java 真实样本端到端修复与验证

- 仅选择 `datasets/Defects4J/cli/Util.java` 作为真实 Java 数据运行，生成 `_real_java_util_check/UtilTest.java`，包含 16 个 JUnit 5 测试和 16 个有效结果断言，覆盖空值、空数组、空字符串、普通输入、单字符引号、成对引号和连字符边界。
- 修复 Java 测试生成只使用 `assertDoesNotThrow`、重载方法传入 `null` 时编译歧义、批量发现误收集测试/构建产物等问题。
- Maven、JUnit、JaCoCo、PIT 统一使用项目内 `java_workspace/.m2` 和 HTTPS Central 配置，避免依赖系统 Maven 仓库权限及镜像配置。
- JaCoCo 指标按目标类 `cli.Util` 过滤，并将覆盖行、覆盖边和运行时证据写入状态图；PIT XML 解析为 24 个变异节点、24 个击杀、0 个存活，并把数量、源码行、可靠性和分数写入 `last_mutation` 与 `ExecutionObserved` 状态流。
- 修复 Java 报告缺少 `effective_mutants` 导致状态流错误提示“有效变异体少于 3 个”的问题；当前可靠性为 `normal`，错误提示计数为 0。
- 真实结果：JUnit 16/16 通过，目标类行覆盖率 100%，分支覆盖率 91.67%，方法覆盖率 100%，PIT 变异得分 100%，SFC 96.25%，AE 90%，SFQ 95.42%，质量状态 `quality_high`。
- 状态图回滚快照限制为最近 3 份，文件由约 7.8 MB 降至约 3.8 MB；仍保留当前图、完整节点、原始 JaCoCo/PIT 报告和可视化产物。
- 清理项目内无用产物：`.pytest_cache`、旧 `.coverage`、旧 `.coverage_stateflow.json`、失败下载留下的 Maven `.lastUpdated` 标记；没有删除项目外文件。
- 验证：Java/批量定向回归 36 项通过；全项目 `python -B -m pytest -q` 共 78 项全部通过，用时 522.57 秒。

## 2026-08-20 LangGraph测试状态引导三智能体重构

- 当前唯一有效的顶层架构为：LangGraph运行底座、共享TSG与测试状态中心、TestStateAgent、TestGenerationAgent和TestKnowledgeAgent。旧Planning/Execution/Evaluation三顶层架构仅为历史；代码中的`state_model`名称只作兼容。
- 工作流不是单向管线。TestStateAgent检查共享TSG后可以把任务分配给自己执行状态自修复，也可以安排知识检索、测试生成、覆盖补测、断言增强、Mutation修复、边界构造、验证或停止。
- LangGraph使用SQLite检查点；完整共享TSG不进入工作流快照，只保存路径和版本，避免大对象重复。
- TestKnowledgeAgent实现四维混合检索并记录知识检索/回写事件；失败和无收益经验允许负奖励，不能伪装成成功经验。
- Java主解析器为Tree-sitter Java；189个Java文件全部走Tree-sitter，结构统计为144类、30接口、7枚举、7注解类型。`package-info.java`无测试目标是正常状态。
- 质量成功口径收紧：只有`quality_high`计入成功；要求全部测试通过、覆盖有效、正常行为、有效断言、AE>=0.80，并满足行/分支/SFC/Mutation/TSQ高阈值。
- TSQ公式为0.30*SFC+0.20*AE+0.20*MS+0.15*PV+0.15*NRR。历史SFQ/CSG仅用于兼容旧文件字段。
- 真实样本结果：HumanEval 10/10、行/分支/Mutation 100%、TSQ 98%；TestEval 7/7、行100%、分支90%、Mutation 89.47%、TSQ 94.54%；Java 16/16、行100%、分支91.67%、PIT 100%、TSQ 96.88%。
- 静态检查：项目Python源码88个、Python数据文件374个、Java数据文件189个均通过。
- 测试：最终全量82项全部通过，用时1005.11秒；另有42项结构化、批量和跨数据集测试单独复跑通过。
- 未完成边界：尚未运行全部数据集；当前Java目录为源码快照，不含Defects4J缺陷版/修复版和完整开发者测试，不能直接计算BDR。

## 2026-08-21 换机依赖审计

- 必跑数据集为HumanEval 164个Python文件、TestEval 210个Python文件，以及Java源码快照cli 34、csv 19、lang3 56、gson 80。
- HumanEval只使用标准库；TestEval 210个文件全部导入`sortedcontainers`，换机必须由项目依赖安装该包。
- 已将`sortedcontainers>=2.4,<3.0`统一加入`pyproject.toml`、`uv.lock`、`依赖.txt`、README、数据集清单和换机说明。
- Java POM已覆盖四组源码的外部依赖；换机需要JDK 11+和Maven 3.8+。无网络时必须保留`java_workspace/.m2/repository`，当前约43MB。
- `run_all_datasets.py`会固定发现六组数据集。Python两组文件位于根目录；Java自动递归扫描并跳过无可测试方法的枚举、注解和`package-info.java`。
- 换机依赖完善后，结构化、批量和跨数据集编排43项测试通过；`uv.lock`锁定sortedcontainers 2.4.0。

## 2026-08-21 另一台电脑实际数据目录

- 另一台电脑项目根目录为`D:\wn\multi_evo_py`，数据集根目录为`D:\wn\multi_evo_py\datasets`。
- 目录包含Defects4J/cli、csv、gson、lang3，HumanEval、HumanEvalJava和TestEval。
- 数据集路径现在支持`--datasets-root`、`TSG_DATASETS_ROOT`、兼容变量`STATEFLOW_DATASETS_ROOT`、项目内`datasets`和固定换机路径兜底。
- `HumanEvalJava`及其他自动发现Java目录必须执行JUnit、JaCoCo和PIT；`--skip-java-execution`可显式关闭。
- 旧`DEFAULT_SOURCE_DIR`绝对路径已移除，批量默认HumanEval路径从统一数据集根目录推导。
- 路径、数据集发现、结构化和批量回归45项全部通过。

## 2026-08-24 实时实验台

- 正式前端现位于项目根目录`frontend/`，项目外`TestPagePro/test-data-showcase`不再是运行依赖。
- `monitoring/`是独立执行控制与展示层，负责启动原命令、读取实验产物、实时推送和对话持久化；它不进入智能体工作流，前端和监控服务失败不得改变智能体路由、指标、结果或退出状态。
- `main.py`、`run_agent.py`、`batch_generate_tests.py`和`run_all_datasets.py`四个正式实验入口默认自动打开`http://127.0.0.1:8765`，一个批量命令只打开一次；`--no-ui`或`TSG_UI_ENABLED=0`可关闭。
- 前端只展示测试状态智能体、测试生成智能体、测试知识智能体。专项生成、验证、修复和优化属于测试生成智能体内部能力；状态评估与下一任务规划属于测试状态智能体。
- 用户问答必须由测试知识智能体与已配置LLM共同完成。单元测试概念可写入长期经验的“正在学习”区，但不能未经真实任务验证直接标为“已掌握”；报告查询和无关问题不进入概念知识。单任务报告使用实际值，只有批量总览显示平均值。
- 实时展示不能依赖最终`stateflow_summary.json`。运行期间必须直接读取持续更新的`state_flow_graph.json`及`metadata.last_report`，SSE下一次快照同步图版本、状态事件和已有指标；任务结束后再切换到最终摘要。
- 前端已升级为执行控制与展示层：可从网页调用原有单文件、目录和全数据集命令，终端入口仍保留；前端只负责启动和提取展示数据，不能改变智能体路由、质量计算或测试结果。
- 测试知识智能体问答使用独立持久化多会话。对话名称由首条问题或用户重命名产生，不默认使用当前任务；实验与任务上下文由问题内容自动识别并仅在命中后绑定。
- 测试状态中心按程序状态、测试状态、运行证据和决策状态分层下钻。节点综合展示程序访问、覆盖、断言有效性和变异结果，不能只用`visited`判断测试节点是否已有证据。
- 页面必须展示Python和Java、文件/类/项目/数据集范围、三个智能体内部任务、八个执行阶段、工具链、状态事件、任务实际指标、批量平均指标、失败报告、知识记录和文件产物，且各模块可点击查看真实详情。
- 所有模块详情不得直接向用户展示JSON或结构化数组，应转换为指标卡、比例条、分类分布、标签组、关系视图或事件流；源码和生成测试文件仍使用代码视图。
- 智能体协作页以共享TSG为中心，用三个正式命名的智能体节点、交互箭头、实时事件流和内部能力证据表达协作；卡片应根据真实事件显示具体评估、规划、生成、验证、修复、优化、检索或学习任务。
- 顶部实验控件只负责切换已登记实验，必须明确写出“仅切换展示，不重新运行”；启动新实验属于独立的“启动实验”页面。
- 默认数据视图为“全部实验”，需要同时融合显示运行中、已完成、排队中和失败实验；顶部可筛选全部、正在运行、已跑完、排队中，也可进入单个实验。
- 网页实验采用目录级串行队列。一个`batch_generate_tests.py`或`run_all_datasets.py`进程完整退出后才启动下一个目录；排队记录持久化，实验注册表不得再限制为最近30项。
- 四个终端入口必须在开始时登记“运行中”，并在正常结束、异常或中断时回写“已完成/失败”，避免只靠文件时间猜测状态。
- 前端对话只把问题发送给后端测试知识智能体；模型配置与密钥沿用后端，密钥不得写入`.monitoring`或下发浏览器。
- 智能体卡片的当前动作必须区分事件执行方与接收方，接收方显示“接收：事件名称”；内部能力与事件次数只能统计该智能体实际发出的事件。聚合视图优先采用当前焦点实验的最新动作，不能被旧实验覆盖。
- 所有用户可见名称必须完整使用“测试状态智能体、测试生成智能体、测试知识智能体”，不得使用“状态智能体、生成智能体、知识智能体”等易与内部模块混淆的缩写。
- 超大`state_flow_graph.json`不能作为普通文本内嵌；监控后端应复用实时快照的压缩规则返回测试状态中心专用数据，前端以节点、关系、证据和分层分布展示。
- 可点击详情必须有可读内容或真实数据；尚未产生数据的入口应禁用并明确标注。前端改动后应抽查全部详情类型，并同时检查桌面和移动端横向溢出。
- 打包分发前清空实验登记、历史对话和验证结果目录，但保留`knowledge_store`全部文件。`datasets`、`examples`与测试代码属于运行兼容内容，不按实验结果删除。
- 状态事件中的`iteration`是共享TSG内部状态迭代，不是正式实验轮次；前端必须显示“状态迭代N”，不得使用`RN`。只有任务等级中的`A/B/C/D/E·R1/R2/R3`表示正式实验轮次。
- 测试知识的状态含义固定为：对话概念归纳后是“正在学习”；候选策略缺少正向证据时是“待验证”；被后续真实任务成功检索采用并验证两次后是“已掌握”；被候选验证拒绝或已有明确负向证据时是“应避免”。
- D/E任务监控数据必须优先读取`experiment_outcome`和最终失败报告，至少展示具体原因、原始错误、失败测试、各轮已采用措施、历史经验措施和建议措施，不能只显示等级或空泛分类。
- 正式R2只处理D/E：D按失败类别检索历史失败和措施后重新生成并完整验证，E补齐缺失证据后重启。正式R3统一处理C：保留已通过测试，按最大质量缺口执行最小目标和知识/LLM引导的定向优化，无增益则恢复原测试。每个任务进入新轮次时立即写`experiment_round_progress.json`。

## 2026-08-24 安全缩时实现

- Python内部候选采用两阶段串行评估：迭代阶段按未覆盖行、存活变异行和状态缺口最多筛查8个高价值变异体；形成正式结果前最多完整评估20个。筛查证据只用于修复规划，不参与最终Mutation、TSQ、A-E等级和知识掌握判定。
- 不可运行的D/E候选在基线通过前跳过覆盖和变异；C级后续轮次保留已有可运行测试，根据存活变异位置和最大质量缺口做局部增强；D按历史失败知识重新生成，E补齐缺失证据后重启。
- LangGraph增加最终完整评估节点，保证可用候选必须得到完整证据后才能进入正式状态评估和知识学习。
- Java保持项目间、项目内和评估工具全局串行。JUnit与JaCoCo合并为一次构建，筛查阶段不运行PIT，正式阶段复用验证证据并只补做PIT。
- 每个任务目录使用`evaluation_cache.json`缓存由源码、测试、依赖配置和评估阶段共同确定的证据；输入变化自动失效。完整数据集目录继续直接跳过，未完整目录只续跑缺失、D/E或未完成任务。
- 前端系统信息页展示上述执行策略；“Python最多8个”表示变异候选数量，不表示8个并行任务。系统信息中的旧共享TSG命名已统一为共享TSG。
- 用户要求完成后不再运行耗时的冒烟或回归检查；后续收尾仅做静态命名、配置和文档核对。

## 2026-08-24 旧实验问题吸收与整体收口

- 未解压读取旧归档中的445条经验和TestEval部分实验结果，确认旧版本存在批量摘要停留在R1、单任务已进入后续轮次但前端仍显示R1、D/E失败证据缺失，以及已通过任务被旧状态误归为D等问题。
- 当前批量执行会在每个后续轮次任务结束时立即刷新总摘要，并从单任务摘要恢复等级轨迹；即使进程在单任务写入与批量汇总之间中断，续跑也会重建R2/R3统计。
- 已完成但评级为D/E的R1任务续跑时不再先重复执行R1，而是按原证据直接进入对应后续轮次。C保留并定向优化现有测试，D依据失败知识重新生成，E补齐缺失证据后重启。
- 每次R2/R3执行前保存上一轮最佳测试与报告。后续候选无增益、损坏或进程中断时恢复上一轮有效产物，避免为了提分反而降低可运行性。
- 无执行报告的生成失败统一判为E；只有已有真实执行报告但编译、导入、收集或测试失败时判为D。A/B/C必须具备真实可用执行证据，统计始终满足总任务数等于A+B+C+D+E。
- Java测试采用临时落盘，执行后恢复项目原测试文件并清理本次生成文件；Maven/Gradle旧报告会在评估前清理，Gradle按目标测试类筛选，PIT优先读取目标类XML，避免项目级历史产物串入当前任务。
- 评估缓存加入操作系统、语言运行时、工具版本、Python本地导入闭包以及Java主源码/测试源码依赖指纹。迁移电脑、依赖或源码变化后缓存自动失效，不会复用旧环境证据。
- 从旧归档的445条原始记录中去重归纳16条历史模式，覆盖oracle、运行时、超时、弱质量改进和正向优化经验，已合并到独立`knowledge_store`；原实验结果可以删除，知识不会随结果目录丢失。
- 前端八个页面已逐一实际打开检查，全部可进入对应可视化内容，浏览器控制台无错误。任务范围、轮次、失败原因、既往措施、知识支持次数和真实独立任务数均由后端快照提供，不依赖演示数据。
- 清空实验登记和历史对话，删除通用Java工作区残留的Calculator源码、测试、图数据及Maven编译清单；`java_workspace`只保留构建配置和离线依赖，正式源码与测试在任务执行时临时写入并隔离恢复。
- 论文中的五张系统界面图已由当前八页前端重新截图，展示分发初始状态和保留的长期知识，不再沿用旧导航顺序或旧实验登记。

## 2026-08-25 效率与质量策略最终口径

- Python默认4并行固定指同一数据集目录内4个独立源码任务工作槽，不是4个数据集同时运行。Ollama最多2并发只约束模型推理；其余任务可继续分析和运行工具。全数据集入口仍逐个处理HumanEval、TestEval和各Java目录。
- 长期知识正式支持外置主库参数`--knowledge-store`。换机推荐固定为`D:\wn\evo\test_knowledge_guidance.json`：外部文件不存在时从项目库初始化，存在时绝不覆盖；终端、前端启动、排队恢复、知识页面和知识对话使用同一路径。
- `D:\wn\evo\test_knowledge_guidance.json`是正式实验使用的外置主库。目标电脑可在首次执行时从分发包初始化，也可接收打包电脑预先提炼的历史知识；已有外部文件永不被新代码覆盖。
- 前端通过`batch_runtime.json`展示工作槽、活动任务、等待任务和模型通道；活动任务事件合并展示并标注任务名，单任务共享TSG保持隔离，运行中任务不计为E。
- Python独立任务默认4并行，本地Ollama最多2并发；单任务内部的pytest、coverage和Mutation保持串行。Java数据集、项目和工具链保持串行，不能因5090显卡而并发Maven/Gradle/JaCoCo/PIT。
- Python Mutation采用最多8个筛查、最多20个正式候选；正式阶段复用筛查结果而不是重跑。补丁候选最多6个筛查变异体，并缓存完全相同的候选。
- 当前有效规则覆盖此条旧阶段结论：R1、R2、R3均允许测试生成智能体在同一正式轮次内最多执行3次状态定位修复或状态目标优化；达到A/B、代码没有变化或质量没有增益时提前结束。正式轮次仍只有三轮，R2只处理D/E，R3只处理C，不创建R4。
- A级门槛为行覆盖0.90、分支0.80、SFC 0.75、AE 0.75、可靠Mutation 0.75、TSQ 0.82；B级基础门槛为0.80、0.70、0.65、0.75、0.60和0.70。A/B/C均为有效完整结果。
- 续跑只跳过A/B/C；失败证据完整的D/E按现有等级直接进入后续轮次，只有缺少原因和诊断的残缺旧摘要重跑R1。失败总数固定等于D+E，另行展示D无效和E未完成。
- 真实双任务TestEval R1并行实验由旧925.21秒降到131.50秒，结果A/B；旧策略下C级定向优化用128.32秒由C升B，该优化能力现统一移至R3；D/E原因驱动恢复用142.20秒得到A。
- 本轮定向检查`tests/test_execution_acceleration.py`与`tests/test_batch_generate_tests.py`共32项通过。项目不是Git仓库，无法提供Git差异状态。

## 2026-08-26 真实调用链与动态前端最终口径

- 最终目标不是机械追求覆盖、断言和变异阈值，而是在测试真实可行的前提下尽可能提高质量与效率。A/B/C均要求可运行、正常行为与有效断言；可用质量证据区分层次，Mutation不适用时不虚记高分。
- 正式实验最多R1/R2/R3。R1处理全部任务；R2仅恢复D/E；R3统一优化C。测试生成智能体在每个正式轮次内部可执行最多3次状态定位修复或状态目标优化，达标、无代码变化或无质量增益时立即停止；内部次数不是新的正式轮次，不产生R4。
- 术语固定为“状态定位修复策略”和“状态目标优化策略”。前者定位直接导致测试不可行的因素，后者定位直接限制测试效果的最大适用缺口；两者均调用LLM并保护已有通过测试。
- Java启用LLM时必须真实读取结构化Java上下文、共享TSG测试意图和检索知识生成JUnit测试；必需模型调用失败时形成明确失败，不再静默伪装为确定性成功。Java的Method/Constructor节点已纳入状态健康、规划、缺口查询、证据回写与经验签名。
- 长期知识库支持不可覆盖的多JSON分片读写；对话知识为“正在学习”，证据不足为“待复核”，被真实任务采用并取得可行结果后累计为“已采纳”，但仍持续接受新证据；负向模式为“应避免”。前端和对话按实验绑定知识路径，避免多实验串库。
- 实验总览使用真实状态事件恢复可重复、可跳过的动态执行路由，右侧为滚动事件控制台；删除独立智能体当前活动和批量质量平均值，改为当前任务质量与全任务综合质量。协作台交互采用滚动控制台，共享TSG为六边形拼图能量核心；测试生成过程展示逐任务耗时折线图。
- 网页启动页可为本次实验选择模型、服务地址和密钥，配置直接进入原有后端LLM调用链，不写入浏览器持久化数据。终端入口保持不变。
- 本轮按用户要求未运行数据集、冒烟或回归套件；仅完成Python语法编译检查和前端生产构建，均通过。
- 实验总览最终分工为：左侧“当前任务指标”展示单任务真实指标，右侧“全部任务综合质量”只展示全任务等级加权结果、有效结果数和A-E构成，不再重复当前任务质量。
- 共享TSG核心使用透明容器承载三层六边形能量面、16个分波闪动拼图单元和18个不同半径、速度、方向、尺寸与颜色的轨道粒子，确保外部动画不被六边形裁剪。
- 多智能体协作图不再使用单线连接：测试状态智能体、测试生成智能体、测试知识智能体与共享TSG之间，以及三个智能体相互之间，共使用12条曲线状态通道和9个双向流动粒子，表达真实的多向协作关系。
- 共享TSG视觉采用克制的深青、金属灰与暖金配色；三层TSG轨道及两层外围环以不同速度和方向持续旋转，轨道信标随环运动。三个智能体节点改为错位的不对称六边形结构，底部以14段实时信号波形反映活动状态，不再显示静态进度条。
- 本轮仅执行前端生产构建与浏览器渲染核对：构建通过；页面实际渲染12条通道、9个流动粒子、3组旋转轨道和每个智能体14段波形，控制台无警告或错误。
- 智能体协作节点只保留正式名称、当前动作和状态事件量；职责、上下文、能力计数等信息移入点击详情。徽标与实时波形放大，使节点以运行状态视觉为主。
- 共享TSG改为白色珍珠质感。16块状态拼图根据TSG版本、状态迭代、节点数和已有证据确定不规则点亮位置，最少4块、最多12块，未点亮块仅显示弱轮廓，不再固定满格。
- 本轮前端生产构建通过，浏览器确认白色TSG、4/16初始局部拼图、70像素智能体徽标和25像素实时波形均已生效，控制台无警告或错误。
- 协作通道不能呈现普通两点直线；当前12条通道改为多段S形偏转、交叉和绕行轨迹，节点整条扫描线改为局部环形脉冲与信标点。
- 三个智能体固定采用蓝、紫、绿识别色，共享TSG由这三色动态融合变化；不规则状态拼图必须保留并继续按真实状态局部点亮。
- 协作场背景不使用方格坐标纸。当前改为围绕共享TSG展开的浅色椭圆状态波纹，并叠加极淡的蓝紫绿方向光场；浏览器确认方格层已隐藏、三色融合动画与4/16拼图均生效，控制台无错误。
- 共享TSG拼图必须作为清晰的前景视觉存在，不能被三色融合层或状态文字遮没。当前拼图扩大并提升层级，隐藏核心次要小字，始终随机点亮6至12块且绝不满格；每2.2秒按不规则顺序重新组合蓝紫绿拼块。
- 浏览器连续取样确认拼图点亮集合由`1,2,5,6,9,10`切换为`1,4,7,8,13,14`，随机重组真实生效，控制台无错误。
- 侧栏不再保留独立“系统信息”导航项。底部“系统在线/正在重连”状态块是唯一系统能力入口，点击后展示Python/Java能力、执行与缩时策略、测试范围和质量维度，页面标题统一为“系统在线”。
- 浏览器确认主导航只剩7项；点击“系统在线”后两种语言卡片及执行策略、测试范围、质量维度三个板块均正常显示，入口选中状态正确，控制台无错误。
- 多智能体协作台在协作效果图下方新增“实时状态监控”。监控只读取现有运行快照，展示当前任务的行覆盖率、断言有效性、变异得分和TSQ，最近24个任务的SFC/AE/Mutation信号，三个智能体的当前动作与事件量，以及实验状态、模型、任务并发、活动/排队任务和共享TSG规模；不新增LLM调用或实验负担。
- 论文方法图统一为本项目自身语义：图1为总体框架，图2为测试状态引导机制，图3为三个正式智能体协作结构；源文件同时保留可编辑SVG和高分辨率PNG。界面图顺延为图4至图8，其中图5包含协作效果与实时状态监控。
- 当前论文和界面以7个主导航页面及“系统在线”入口为准；论文内图1至图8的文件链接已逐一确认存在，协作页浏览器实测仅渲染1个实时监控、4个当前指标、3个智能体状态和7个系统信号，控制台无错误。

## 2026-08-26 全局真实执行复核

- 本次不再依据文档或模拟结果下结论。实际运行Python单文件任务，32项测试通过，行/分支/SFC均为1.0、AE为0.9366、Mutation为1.0、15个有效变异体，耗时127.28秒；实际运行带相对导入的Python包级任务也通过完整链路，证明生成测试加载器能够保留包上下文。
- 实际运行Java `SimpleCalculator`完整链路，JUnit、JaCoCo和PIT均返回真实证据：10项测试通过，行/分支/SFC均为1.0、AE为0.9275、Mutation为0.7143、14个有效变异体，耗时20.60秒。按修正后的等级规则结果为B，而不是旧逻辑中的A。
- 修复Java PIT无结果的两个底层原因：通用Maven配置不再硬编码少数数据集包名；正式阶段不再删除筛查阶段已经编译的`target/classes`，PIT可复用编译产物并按当前目标类执行。
- A级现在要求全部适用核心维度达到高质量标准且Mutation证据真实有效；Mutation缺失、无效或仅部分指标较高时不能评为A。TSQ不会再把不可靠Mutation分数当作可靠输入。
- Python测试加载器统一支持相对导入和包内依赖，覆盖计划渲染、候选规范化、最小修复、运行时观察及变异文件执行，不再只适用于孤立脚本。
- 已完成质量结果A/B不再额外调用测试状态LLM评价；C/D/E仍会使用LLM结合质量缺口或失败原因形成修复、优化建议，减少终止任务上的无效模型耗时。
- 完成目录跳过增加源码文件路径、大小和修改时间签名；数据集内容变化后旧`dataset_completion.json`自动失效，不会错误跳过新增或修改任务。
- `run_agent.py`默认轮次参数由旧值1统一为2，因此目录入口与正式批量入口一致执行R1、R2、R3且不产生R4。删除两份从未被运行入口调用且只有`pass`的语言验证器空壳，真实验证统一由`DynamicValidator`承担。
- 当前核心Python模块语法编译通过，前端TypeScript检查和生产构建通过。不能据此承诺任意第三方项目必然成功：本机没有验证Gradle项目，也没有在本机连接换机后的实际Ollama模型，更没有重新跑完整210项TestEval；复杂构造器、外部服务、原生依赖和项目自定义构建仍可能形成D/E，但系统会保留具体失败证据并进入规定轮次。

## 2026-08-30 HumanEvalJava、TestEval、累计计时与数据图表修复

- 旧HumanEvalJava结果163项全部为D且实际执行数为0。根因是每个任务都复制并编译整个数据集，`STRING_TO_MD5`在JDK 11+缺少`javax.xml.bind.DatatypeConverter`后污染所有无关任务。现改为单源码、完整包名隔离，并在通用Maven工作区加入JAXB API。
- 当前迁移目录原本缺少`datasets/HumanEvalJava`，会导致后续全数据集入口漏跑。已从163份`state_flow_graph.json`中的结构化源码恢复全部Java文件，恢复脚本为`scripts/restore_humanevaljava_dataset.py`；163份源码在临时Maven工程中整体编译通过。
- TestEval旧页面Mutation均值0.4018混入17个pytest通过但语义为D的无效任务；A/B/C 186项的旧结果均值为0.4385。质量平均值现只统计A/B/C，D/E仍进入等级与失败统计。
- 可靠Mutation低于0.30不再允许评B，保证其进入C并由R3优化。LLM测试计划会在真实目标上校正oracle，保留多样可运行输入并用确定性边界计划补足；嵌套矩阵、字符串列表和普通列表输入已加强。真实`TestEvatask_021`计划检查得到7个正常场景和1个异常场景，不再只有`[[1]]`。
- 每个任务新增`task_runtime.json`会话计时，累计正式轮次与续跑实际耗时；中断区间截止到最后产物，不计离线空闲。批量摘要、全数据集主摘要和监控快照分别展示单任务、单数据集和总累计时间。
- 实验总览新增六维雷达、A-E环形、覆盖率×Mutation散点、Mutation分布和跨续跑耗时排行，浏览器实际验收后压缩宽屏留白。图表不增加实验计算负担。
- 额外修复中文用户名下任务计时JSON替换回退、唯一零参数函数计划误拒绝和SSE正常断连异常日志。
- 验证：严肃问题回归7项通过；相关后端53项通过、3项无关旧测试排除；前端6项通过；前端生产构建通过；本次9个Python核心文件AST检查通过。完整163/210项实验尚未重新运行，现有runs仍是修复前历史结果，续跑后才会产生新指标。

## 2026-08-30 多数据集可视化与报告分组收口

- 全数据集主摘要会展开为7个可独立选择的数据集视图：TestEval、HumanEval、HumanEvalJava及4个Defects4J子集；过期且目录不存在的旧实验登记不再进入页面。
- “实验总览”统一标为Python + Java、项目级 + 文件级，单数据集视图保留自身语言。页面不再出现“修复后的数据视图”等开发过程用语。
- 实验总览和单数据集均展示数据集结果矩阵，包含有效结果、行覆盖、Mutation、AE、TSQ和A/B/C/D/E；总览使用711项真实历史任务，任务与报告页按数据集和等级双重筛选。
- 覆盖率与变异散点图已加入AE：横轴为行覆盖、纵轴为Mutation、圆点大小代表AE，提示信息同时给出三项数据。
- 测试状态中心新增证据完整度、状态节点和D/E失败分布图；测试生成过程新增R1/R2/R3流量、处理路径、最终等级、数据集矩阵和逐任务耗时图。阶段、工具、状态事件等长文字改为默认折叠明细。
- 后端任务记录增加数据集标识，保证同名任务不会跨数据集混淆；总览、运行中、已完成、排队中及各数据集复用同一批真实实验产物。
- 验证：严肃问题回归8项通过；监控相关12项通过、1项已知无关旧测试排除；全数据集入口4项通过；前端6项通过；前端生产构建通过；浏览器实测总览、TestEval、测试状态中心、测试生成过程和任务与报告均正常，控制台无警告或错误。

## 2026-08-30 数据集选择、历史计时与状态页面二次收口

- 实验范围菜单简化为“实验总览 + 各数据集”，数据集区域拥有独立滚动容器；浏览器实测菜单高618像素、列表可视430像素、内容595像素且`overflow:auto`，底部Defects4J_gson可点击并正确进入独立结果页。
- 数据集层级不再全部写成项目级+文件级：TestEval为Python文件/函数/类级，HumanEval为Python文件/函数级，HumanEvalJava为Java文件/类级，Defects4J为Java类/项目级。实验总览只在页首保留一次Python+Java与项目/文件总范围。
- 旧批量摘要只记录续跑会话的几秒钟，现从711个任务的`runtime_seconds`恢复完整历史耗时：Python按当时4个任务槽折算，Java保持串行，数据集与总实验取历史累计和现有会话记录中的较大值。当前总累计为15:05:51，TestEval为03:39:21，HumanEvalJava为04:54:33。
- HumanEvalJava现有163个测试文件已被本地路径重新识别，但163项仍是修复前的D级无效执行，不能虚构覆盖率、Mutation、SFC或TSQ。页面标为“待重新验证”，结果矩阵显示163个测试文件及待复验指标；必须用修复后的Java链路重新运行才会产生新有效指标。
- “实时执行过程”从实验总览移动到测试状态中心；状态中心新增SFC状态覆盖与TSQ综合质量卡片。“内部能力图”移入协作时间线下方的折叠详情，默认不占页面高度。
- 监控快照增加同一实验构建锁，避免页面首次加载时HTTP快照和SSE同时重复解析711个大JSON；首次缓存由多份并行解析收敛为单份构建。
- 验证：严肃问题回归8项通过；监控相关13项通过、1项已知无关旧测试排除；前端6项通过；前端生产构建和Python AST检查通过。浏览器实测选择器底部数据集点击、独立结果页、状态中心SFC/TSQ、执行过程迁移、能力图折叠及15:05:51累计总时长均生效，控制台无警告或错误。

## 2026-08-31 可视化易读性、HTML报告与生成方法入口收口

- “覆盖率 × 变异得分 × AE”改为“测试质量分布”：坐标轴显示0/25/50/75/100刻度，以75%为界分成高覆盖高变异、变异待增强、覆盖待增强、两项均待增强四个区域；横轴、纵轴和AE圆点大小均有明确说明，区域卡片可点击查看对应任务。
- 任务耗时折线图改为真实累计耗时统计与排行：展示平均值、中位数、P90、最长任务，并列出18项耗时较长任务的横向条形图；点击仍进入单任务详情。
- 测试知识引导库删除无用的“知识形成闭环”展示。整体页面使用更浅、更低饱和的蓝灰、青灰和暖灰配色；数据集结果矩阵的既有配色保持不变。
- 任务与报告页的“测试状态中心”文件卡改为“HTML测试报告”。点击后只显示`state_flow_graph.html`测试状态报告和`source_coverage.html`源码覆盖报告两个真实链接；后端增加受实验目录约束的只读HTML报告入口，并使用浏览器沙箱与内容安全策略隔离报告。
- HTML报告打开不再先构建711项完整运行快照。目录内报告直接通过授权检查后返回，实测`state_flow_graph.html`为HTTP 200、22,464字节、约0.01秒。
- 启动页原按钮改名为“MutiTSGAgent方法生成”，旁边新增“ValiFixTest方法生成”样式按钮；现有实验启动逻辑未改变，新增按钮暂不绑定运行逻辑。
- 可视化网格明确指定宽屏、平板和手机布局，避免散点图扩大后产生空白列。前端6项测试与生产构建通过，`monitoring/server.py`语法编译通过；监控测试13项通过，另有1项旧知识状态用词测试仍期待`mastered`而当前正式口径为`adopted`，与本次界面修改无关。

## 2026-08-31 浅色视觉、分页报告与切换性能收口

- 实验总览标题下方不再重复展示语言、层级与完成状态；全站采用已确认的冰川蓝、薄荷绿、杏桃色浅色体系，正文和边框保持足够对比。
- 质量指标统一使用`LC / BC / SFC / AE / MS / TSQ`，详情中保留中文解释。质量散点图明确显示LC横轴、MS纵轴和AE圆点大小，右侧只保留A/B/C三种颜色说明，不再出现四区说明与三色圆点不对应的问题。
- 启动页将两种生成方法放入同一方法区。MutiTSGAgent按钮在缺少必填目录时明确提示原因；ValiFixTest按钮可点击并说明当前尚未绑定独立执行命令，未冒充已有实验逻辑。
- 任务与报告页每页10条，知识记录每页18条，均提供上一页、下一页、当前页与总条数。知识记录新增“适用用途”，可直接识别断言、覆盖、变异、编译依赖、异常、状态引导等问题类别。
- 历史失败报告路径会映射到当前实验目录中的真实`task_failure_report.html`，浏览器实测可在右侧面板完整显示HTML，不再报“outside the selected experiment”。
- 数据集切换取消HTTP快照与SSE重复加载：已完成实验不再保持大体积状态流，知识库仅在知识页加载；总览数据会在客户端即时生成数据集预览，同时后台补充精确快照。
- 后端运行快照将巨大知识历史压缩为必要字段，HumanEval响应由约4.85MB降至约0.41MB；实验目录展开和数据集卡片加入基于文件更新时间的缓存，实测Defects4J_csv相同快照请求由约2.6秒降至0.01–0.04秒。
- 验证：前端TypeScript生产构建通过，前端6项测试通过，Python监控模块语法编译通过；浏览器逐项确认总览副标题删除、三类图例、任务分页、失败HTML、知识用途与分页、两种生成方法入口和数据集即时切换均生效。

## 2026-08-31 全代码源码审查与运行链收口

- 按源码逐段审查实验入口、Python/Java执行器、R1/R2/R3工作流、续跑计时、质量指标、知识持久化、监控缓存、路径授权、任务队列和前端状态流，不以运行日志代替代码检查。
- 修复已完成实验快照缓存一小时不失效的问题：缓存现在跟踪结果摘要、运行时、完成标记和单任务关键文件版本；已完成结果被补齐或修复后无需等待一小时即可刷新。运行中和排队中的数据集卡不使用长期缓存，避免实时进度停滞。
- 修复“存在状态图就判为运行中”的错误，完成任务不再污染正在执行数量、实时事件和状态中心。中断计时恢复时对并发替换中的产物文件容错，不会因单个文件瞬时消失而阻断续跑。
- 修复Windows中文用户路径下`replace`失败造成注册表、对话、候选测试、评估缓存和知识记录保存失败的问题；状态图和批量摘要改为优先原子替换、失败时可靠降级写入。长期知识保留多进程分片作为真实来源，同时生成可直接迁移和查看的主JSON。
- 修复Java质量判定：异常断言专用测试不再冒充正常行为测试，恒真/存在性断言继续判为弱断言，`ParameterizedTest`、`RepeatedTest`等JUnit 5测试会被正确计数。修复Python测试类中的`test_`方法此前不参与语义质量分析的问题。
- 修复内部筛查过早进入正式评估的问题：低质量可用候选会先按最大缺口执行允许的内部修复，达到质量目标、停止条件或修复上限后再进行完整Mutation正式评估；最终A-E和知识学习仍只使用正式结果。
- 知识状态统一为“正在学习、待复核、已掌握、应避免”。重复出现和正奖励不再自动升级，真实成功验证累计达到要求后才成为“已掌握”；页面、报告和测试知识智能体问答使用同一口径。
- 修复Ollama新旧客户端构造参数兼容、服务重启后排队任务丢失原模型/服务地址、任务进程日志句柄与已结束进程残留问题。PIT工作区补回Defects4J各包深度的默认测试匹配，单任务运行仍由精确类名覆盖。
- 前端数据切换请求会主动取消已过期的大响应，避免快速切换时旧总览请求继续占用网络和解析资源。
- 验证：Python全量回归中127项直接通过，唯一剩余项是旧测试对当前正式中文命名、外置知识路径和定向修复策略的断言，更新后该完整工作流单项通过；合计128项验证链通过。Python全模块编译通过；前端TypeScript生产构建和6项测试通过。重启本地服务后，浏览器实测实验总览、HumanEvalJava独立页、15:05:51累计时长、163项任务明细、知识库“已掌握”状态及分页均正常，控制台无警告或错误。

## 2026-08-31 修改日志与论文系统界面同步

- `修改日志.md`新增“全代码检查、持久化修复与论文系统界面同步”，记录缓存、运行状态、中文路径持久化、Python/Java语义质量、分层正式评估、知识状态、Ollama/队列和前端请求取消等修复及验证结果。
- 论文第4章按当前真实实现更新：实时执行过程归入测试状态中心；实验总览改为累计时间、并行状态、数据集卡、六维质量和任务耗时；任务与报告改为数据集结果矩阵、10项分页与两个HTML报告；知识库统一为正在学习、待复核、已掌握、应避免及18项分页。
- 论文图4至图8已从当前真实七数据集实验页面重新截图，分别对应实验总览、三智能体协作、测试状态中心、数据集结果矩阵和测试知识引导库。截图显示7个数据集、711项任务、413项有效结果、15:05:51累计时长和HumanEvalJava待重新验证状态，不虚构修复后的实验指标。

## 2026-08-31 论文全文与方法图代码一致性重构

- 论文第3章不再沿用旧概念推演，所有节点类型、边类型、五维缺口、8步缺陷传播、分层评估、A-E阈值、调度得分、补丁门、混合检索权重和知识生命周期均逐项映射到当前源码。
- TSQ正式口径固定为条件归一化公式：SFC 0.30、AE 0.20、PV 0.15、NRR 0.15；可靠full MS适用时加入0.20，否则用0.80分母重新归一化。screening MS不能进入正式TSQ、A-E或知识掌握判定。
- 算法1表达数据集R1/R2/R3与单轮LangGraph内部最多3次修复/优化的两层关系；算法2表达单次最小范围定位、局部操作、真实语义/质量补丁搜索和无退化门；算法3表达普通经验与对话概念的两套检索权重、90天衰减和0.20总分阈值。
- 方法图1至图3的SVG和PNG均已重绘：图1突出正式轮次分支与轮内闭环，图2突出共享TSG证据更新与五维缺口调度，图3突出三个顶层智能体责任边界和LangGraph条件节点。后续论文修改必须保持这三图与第3章算法、公式和代码同步。

## 2026-08-31 图形化方法图与现有实验结果成稿

- 方法图1至图3采用图标、节点、轨道、五边形缺口、分支和证据符号做抽象化表达，图中不再承担正文说明；SVG是可编辑源，PNG是论文插图。
- 论文实验数据只能来自现有七个batch摘要和主摘要。复核证据文件记录源文件SHA256、最优轮次恢复规则和指标计算规则，禁止为直接LLM、Pynguin、EvoSuite或组件关闭实验虚构数据。
- 当前实际证据：711项任务，最终A/B/C/D/E为176/129/108/292/6；R1有效386，R2恢复27后有效413；R3共处理153项并改善92项，其中45项由C升级为A/B。
- 实验章节已使用Markdown表格呈现七数据集、语言分组、R2/R3、三轮消融、R3指标增益和TSQ分量消融。HumanEvalJava旧163项D必须持续标为待复验。
- 参考用户既有ValiFixTest期刊稿和状态图会议稿后，实验表固定为表1至表8，表题在上、数据居中、正文在表后分析。公式与算法的最新格式见2026-09-01记录。

## 2026-08-31 方法图参考原则与最新结构

- 用户明确允许参考其《开题终稿1130-王娜》和《论文2》的绘图设计。可借鉴的是分区、层次、中间工件、决策节点、反馈回路与状态变化表达，不应复制原图中的具体文字、图标组合或流程内容。
- 当前图1固定表达任务建模、共享TSG轮内协作、R1/R2/R3正式轮次和持久化证据层；图2固定表达状态—断言—变异体关系、R⁺/R⁻证据矩阵、8步缺口传播和断言—变异联合增强；图3固定表达感知分解、知识检索、生成执行、评估回流四阶段任务交接。
- 三图源文件位于`论文图片/方法框架`，有效文件分别为`图1_方法总体框架`、`图2_断言变异联合增强机制`和`图3_多智能体协作流程`，SVG为可编辑源，PNG为1600×960论文插图。以后修改代码或算法时，必须同步核对内部最多3次、正式R1/R2/R3、SFC/AE/MS质量门、结构化交接字段和知识状态口径。

## 2026-09-01 论文研究主线与格式口径

- 三个研究问题固定为：问题1研究共享TSG如何统一目标与执行证据并引导任务；问题2研究测试状态、测试生成和测试知识三个智能体如何通过职责分离、任务交接、条件路由和经验回流形成自主协作并避免冲突；问题3研究方法的指标表现、跨数据集效果和正式轮次贡献。
- 三项贡献固定对应：共享TSG状态引导机制；绑定任务签名、状态版本和证据引用的多智能体协作协议；七数据集711项任务的多维实验评价。断言—变异联合增强是状态引导与智能体协作的核心质量任务，状态定位与候选恢复只属于可执行性保障。
- 正文保留18个必要公式，统一使用可直接阅读的Unicode表达和式（1）至式（18）编号，不再使用HTML标签、`$$`或LaTeX命令。算法固定为3个代码行式伪代码，不使用表格：算法1三轮与轮内状态引导，算法2断言—变异多目标增强，算法3状态感知记忆检索。
- 实验RQ固定为：RQ.1跨语言跨层级总体效果，RQ.2断言与变异增强效果，RQ.3指标体系完整性与稳定性。任何结论必须继续保留HumanEvalJava待复验和缺少同环境外部基线的证据边界。
- 三张方法图必须保持用途互斥：图1只表达总体框架；图2表达断言—变异正负证据关系、缺口传播和联合增强细节；图3表达Q_t、K_t、(T_{t+1},O_t)及评估工件在三个智能体间的任务交接和记忆回流。不得再次把三图都画成总体架构或静态智能体卡片。
