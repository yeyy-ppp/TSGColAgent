# 项目核心功能、方法与创新点确认

## 方法定位

本项目实现基于LangGraph的测试状态引导机制（TSG）。LangGraph是工作流运行底座，负责节点执行、条件路由、循环、SQLite检查点和恢复；共享TSG、测试状态中心、证据语义、缺口传播、测试意图、质量指标和修复约束由项目自行定义。

共享TSG是测试领域状态结构，不等同于LangGraph的工作流状态。LangGraph状态只保存共享TSG引用和当前决策，完整数据由测试状态中心独立持久化。

## 非线性闭环

```text
测试状态智能体初始化或检查共享TSG
  -> 状态异常：测试状态智能体自修复并重新规划
  -> 状态有效：测试知识智能体检索
  -> 测试生成智能体生成/修复、执行并验证结果
  -> 测试状态智能体依据验证证据评估质量、更新共享TSG并计算状态缺口
  -> 测试知识智能体学习结果
  -> 条件路由到继续检索、继续生成、状态自修复或结束
```

系统不是固定的单向流水线。测试状态智能体负责决定下一步由哪个智能体执行什么具体任务，并可以把任务分配给自己进行共享TSG诊断和自修复。

## 三个顶层智能体

### 测试状态智能体

- 分析Python/Java源码并建立共享TSG；
- 检查语言、结构化上下文、可测试目标和状态事件完整性；
- 共享TSG损坏时重新分析源码并自修复；
- 接收覆盖、断言、运行类型、异常、Mutation和修复证据；
- 计算覆盖、断言、变异、异常、类型缺口并传播；
- 选择主导缺口、优先目标状态和下一项具体任务；
- 综合执行与验证证据评估SFC、AE、MS、PV、NRR和TSQ，并完成A-E分级；
- 决定继续生成、知识检索、修复、验证、降级或结束；
- 不修改测试文件。

### 测试生成智能体

- 接收结构化TestIntent与知识上下文；
- 生成pytest或JUnit 5候选；
- 构造正常、空值、边界、异常、左右不对称和类型相关输入；
- 调用pytest/JUnit、coverage/JaCoCo、Mutation/PIT和运行时观察工具；
- 执行覆盖补测、断言增强、Mutation敏感测试和边界测试；
- 按断言、行、测试方法、测试类、测试文件逐级定位状态定位修复范围；
- 候选通过临时执行和不退化检查后才更新正式测试；
- 内部完成候选与执行结果验证，产出覆盖、断言、运行类型、异常和Mutation证据；
- 是唯一可以修改测试文件的顶层智能体，不负责最终质量分级和下一智能体决策。

### 测试知识智能体

- 管理当前任务短期经验和跨任务长期经验；
- 根据语言、缺口、状态类型、参数类型、结构复杂度和目标词构造查询；
- 使用语义相似、结构匹配、历史质量和时间衰减四维评分；
- 返回有效输入分区、断言模式、修复策略和失败规避提醒；
- 记录检索结果是否被采用以及采用后的质量变化；
- 不修改测试文件和共享TSG质量指标。

## 测试状态中心结构

程序状态包括Module、Package、Class、Interface、Enum、Record、AnnotationType、Function、Constructor、Branch、Loop、Call、Return、Raise、Throw、Exception和End。

测试与证据状态包括TestCase、Assertion、Execution、RuntimeState、RuntimeType和Mutation。

关系包括defines、flow、call、return、throw、Execute、Produce、Depend、Observe、TypeFlow、Trigger、RepairTarget、Kill和Survive。

事件包括ContextExtracted、InitialGraphBuilt、TestIntentPlanned、KnowledgeRetrieved、TestGenerated、ExecutionObserved、CoverageObserved、MutationObserved、ValidationPassed/Failed、RepairScopeLocated、RepairApplied、KnowledgeUpdated、TestStateRepaired和CompletenessReached。

## 双语言实现

Python使用标准AST。Java使用Tree-sitter Java AST，兼容降级解析只在AST失败时启用。Java支持类、接口、枚举、记录、注解类型、泛型、继承、重载、Override、分支、循环、调用、返回和抛出结构。

Python工具链为pytest、coverage.py、运行时观察和轻量Mutation；Java工具链为JUnit 5、Maven/Gradle、JaCoCo和PIT。两种语言最终产生相同结构的执行证据并回写共享TSG。

工具验证采用“内部筛查、最终完整评估”的两阶段策略。Python筛查阶段最多串行执行8个状态相关变异体，完整阶段最多选择20个并复用筛查结果，只补做尚未执行的变异体；Python独立任务默认4并行，本地模型请求最多2并发。Java任务与工具链保持串行，将JUnit与JaCoCo合并为一次构建，并只在稳定候选上运行PIT。筛查结果只指导测试状态智能体规划，不能直接成为正式Mutation与等级；最终报告和知识学习必须读取完整评估。输入哈希缓存只复用源码、测试和依赖配置均未变化的证据，因此不会跨版本混用结果。

## 质量指标与门槛

```text
SFC = 0.45*节点覆盖 + 0.35*可追踪边覆盖 + 0.20*分支状态覆盖
AE  = 0.45*断言具体性 + 0.25*正常行为比例 + 0.15*观察映射 + 0.15*断言多样性
MS  = killed / (killed + survived)
TSQ = 0.30*SFC + 0.20*AE + 0.20*MS + 0.15*PV + 0.15*NRR
```

真实行/分支覆盖作为SFC下限，同时保留静态图组件明细，避免静态边匹配不足低估执行证据。

高质量默认要求：行覆盖0.90、分支覆盖0.80、SFC 0.75、AE 0.75、可靠Mutation 0.75、TSQ 0.82、PV=1、NRR=1。B级基础门槛为行覆盖0.80、分支覆盖0.70、SFC 0.65、AE 0.75、可靠Mutation 0.60和TSQ 0.70。有效变异体少于3个时单独标记可靠性。

最终分级为A高质量、B良好、C可用、D无效和E未完成。A/B/C计入有效完整结果；D/E不计入，并展示具体失败原因和此前解决措施。B允许一个非关键指标偏低，C要求真实可运行、存在正常行为和有效断言。Mutation不可用或有效变异体不足时单独标注可靠性，不虚记为0分。

实验固定最多三轮：R1运行全部任务；R2只恢复D/E，仍为D/E的任务形成最终失败报告；R3统一优化全部C，包括R1的C和R2恢复得到的C。每轮均先检索测试知识，再规划和执行；最终报告展示等级、参与轮次和等级轨迹，并保留各轮最佳有效结果，不存在R4。

## 核心创新点

1. 将源码结构、测试意图、执行观察、断言、Mutation和修复证据统一为可演化共享TSG。
2. 状态智能体根据主导缺口和阈值差距安排下一智能体及具体任务，而非固定顺序调用。
3. 状态智能体具有自诊断、自修复和重新规划能力。
4. 测试生成智能体独占测试修改权并负责验证，测试状态智能体独占质量评估、分级和跨智能体规划权，降低职责冲突。
5. 四维知识检索同时考虑语义、结构、质量与时间，并学习失败经验。
6. 状态定位修复以真实执行、覆盖、Mutation、TSQ和NRR作为候选门禁。
7. LangGraph提供通用编排可靠性，TSG提供测试领域中的状态语义和决策算法，创新边界清晰。

## 当前验证边界

本次重构使用`examples/sample_java/SimpleCalculator.java`完成一次真实端到端验证：JUnit全部通过，行覆盖和分支覆盖均为100%，AE为92.75%，Mutation为71.43%，TSQ为92.84%，最终等级为`B·R1`。任务同时生成共享TSG、状态流、JaCoCo/PIT证据、批量报告、完成标记和测试知识引导库。尚未重新执行Python与Java全量数据集，因此不能把单样本验证表述为全量实验结论；Defects4J缺陷版/修复版切换和BDR全量实验结果也仍待正式实验补充。

换机运行还会自动发现`D:\wn\multi_evo_py\datasets\HumanEvalJava`。该目录按Java数据集进入完整JUnit、JaCoCo和PIT执行；其他顶层Python或Java项目目录也按源码数量推断语言并执行对应工具链。
